# 项目健康度报告 - wxgameslocal（文明火种）

> 生成日期：2026-07-02 | 评估人：WorkBuddy

---

## 总体评分：B（良好，存在需关注的问题）

| 维度 | 评分 | 说明 |
|------|------|------|
| 测试覆盖率 | A | 1698 单元测试全通过；但 84 个架构测试失败 |
| 代码质量 | B+ | Lint 有 15 个新错误，187 条历史抑制 |
| 安全性 | C | 明文密码文件，1 个中等漏洞 |
| 工程规范 | B | Prettier 全通过，但 39 个文件未提交 |
| 磁盘与垃圾 | C | `.local-logs` 占用 3.7GB，大量备份文件 |
| 依赖健康 | B | 少量可疑依赖，1 个中等安全漏洞 |

---

## 一、基本信息

| 项目属性 | 详情 |
|----------|------|
| 项目名称 | wxgameslocal / 文明火种 (Civilization Fire) |
| 仓库地址 | https://github.com/18301724526/WXGames |
| 技术栈 | Node.js (Express + better-sqlite3) + 前端 Canvas/ECS |
| 当前分支 | `codex/refactor-tutorial-guide-architecture` |
| 总源文件 | 492 个 JS 源文件 + 266 个测试文件 = 758 |
| Git 提交数 | 1488 |
| 未提交变更 | 39 个文件（含大量文件删除和修改） |

---

## 二、测试状况

### 2.1 单元测试（npm test）

✅ **1698/1698 全部通过，0 失败**

这是项目最健康的方面。测试覆盖了前后端的核心逻辑，尤其是 shared/ 模块的测试质量很高。

### 2.2 架构测试（npm run test:architecture）

⚠️ **1203 通过 / 84 失败 (通过率 93.5%)**

失败的测试主要集中在一个区域：

- **`CanvasTerritoryActionHandlers.test.js`**：因 `CanvasTerritoryActionHandlers is not defined` 导致全部失败
- **`CanvasGameShell.test.js`**：多个异步操作、路由、对话框测试失败（多数因依赖链断裂）

> **根本原因**：当前分支正在进行 god-file 拆分重构。大量 Facade 文件被删除（`CanvasGameRendererCoreFacades.js`、`CanvasBattleFacade.js`、`WorldMapLayoutFacade.js` 等 16 个文件标记为 D），但相关引用的测试尚未更新适配。

---

## 三、代码质量

### 3.1 ESLint

⚠️ **15 个新的 lint 错误**

集中在两个文件：
- `frontend/js/platform/CanvasGameRenderer.js`（10 个错误）：未使用的变量 `error`、`options`、`node`、`view`
- `frontend/js/platform/renderers/WorldMapCanvasRenderer.test.js`（2 个）：未使用的 `fs`、`ROOT`
- `frontend/js/platform/renderers/WorldMapLayerCanvasRenderer.js`（3 个）：未使用的 `offsetX`、`uiState`、`state`

### 3.2 ESLint 历史抑制

⚠️ **187 条抑制规则分布在 90 个文件中**

| 规则 | 抑制数量 |
|------|----------|
| `no-unused-vars` | 156 |
| `no-empty` | 31 |

这是一个逐步积累的技术债。建议设定抑制清理的目标（如每月减少 10%）。

### 3.3 Prettier

✅ **所有文件格式一致，通过检查**

---

## 四、安全性

### 4.1 🔴 严重：明文密码文件

**`E:\Human\wxgame\password.txt`** 存在明文密码。虽然 `.gitignore` 中已配置忽略，但文件仍存在于磁盘上。

> **建议**：立即删除该文件，或将其内容迁移到安全的密码管理器。如果是部署所需，使用 `.env` 文件并确保已加入 `.gitignore`。

### 4.2 npm 安全审计

⚠️ **1 个中等严重度漏洞**

根 `package.json` 的依赖审计发现 1 个 moderate 级别漏洞（具体 advisory 未展开）。建议运行 `npm audit fix`。

### 4.3 依赖审查

根 `package.json` 中存在一些不太寻常的直接依赖：

| 包名 | 用途判断 | 建议 |
|------|----------|------|
| `ssh2` | SSH 远程连接 | 确认是否仍在使用 |
| `asn1` / `bcrypt-pbkdf` / `tweetnacl` | 通常为 ssh2 的传递依赖 | 可能是误安装为直接依赖 |
| `cpu-features` / `nan` / `buildcheck` / `safer-buffer` | 同样疑似传递依赖 | 检查是否必需 |
| `bitecs` | ECS 框架（游戏核心） | 正常 |
| `playwright-core` | 浏览器自动化核心 | 正常（配合 playwright devDep） |

---

## 五、磁盘与文件管理

### 5.1 🔴 严重：`.local-logs/` 目录占 3.7 GB

大量 H5 性能测试截图和历史日志堆积。`.gitignore` 已配置忽略但从未清理。

> **建议**：运行清理（保留最近 1-2 周的日志，删除更早的）。

### 5.2 ⚠️ 后端 server.js 有 11 个备份文件

```
server.js.backup
server.js.backup.1778603673
server.js.backup.events.1778610854
server.js.backup.events2.1778611015
server.js.bak
server.js.bak.1778665304
server.js.bak.1778665480
server.js.bak.20260512161550
server.js.bak.20260512172431
server.js.pre-tick
server.js.step1.bak
```

这些备份文件来自 5 月中旬的重构。当前 `server.js` 已经从 30-49KB 缩减到 8.4KB。旧备份不再需要。

> **建议**：清理这些 `.bak`/`.backup`/`.pre-tick` 文件。它们已在 `.gitignore` 中。

### 5.3 数据库备份

- `civilization.db`（7.1MB）— 主数据库
- `civilization.db.bak.20260514_001323`（4.1MB）— 可删除
- `civilization.db.bak.20260514_012734`（388KB）— 可删除
- `backend/.local-playtest.sqlite`（92KB）— 本地测试数据库
- `database.db` 空文件 — 可删除

---

## 六、Git 状况

| 指标 | 状态 |
|------|------|
| 未提交变更 | 39 个文件 |
| 当前分支 | `codex/refactor-tutorial-guide-architecture` |
| 远程分支 | 5 个（部分已过时） |
| 本地 stash | 无 |

当前分支正在进行一个大规模的 god-file 拆分重构，涉及 16 个文件的删除和 15 个文件的修改。这些变更尚未提交。

---

## 七、文档状况

✅ 文档丰富：

- `docs/` 下有 52 个 Markdown 文件，涵盖架构、设计、开发日志、重构计划
- `PROGRESS.md` 记录了 march 重构（6 步）和 fix-fog、march-identity 的完整历史
- `march-autonomous-manual.md` 和 `march-identity-manual.md` 作为手动操作手册

---

## 八、改进建议优先级

### P0 - 立即处理

| # | 问题 | 操作 |
|---|------|------|
| 1 | `password.txt` 明文密码 | 删除或迁移至安全存储 |
| 2 | `.local-logs/` 占用 3.7GB | 清理旧日志（保留最近 7 天） |

### P1 - 本周处理

| # | 问题 | 操作 |
|---|------|------|
| 3 | 84 个架构测试失败 | 完成 god-file 拆分后更新相关测试引用 |
| 4 | 15 个 lint 错误 | 修复未使用变量（给变量加 `_` 前缀或删除） |
| 5 | 39 个文件未提交 | 拆分后提交当前重构分支的变更 |
| 6 | 11 个 server.js 备份 | 清理 `backend/server.js.*` 备份文件 |

### P2 - 本月处理

| # | 问题 | 操作 |
|---|------|------|
| 7 | 187 条 lint 抑制 | 设定清理计划，每月减少 10-15% |
| 8 | 可疑的直接依赖 | 审查 `ssh2`/`asn1`/`tweetnacl` 等并移除不必要的 |
| 9 | 1 个 npm 中等漏洞 | 运行 `npm audit fix` |
| 10 | 旧数据库备份 | 删除 5 月的 `civilization.db.bak.*` 和空 `database.db` |

---

## 九、亮点

1. **测试文化优秀**：1698 单元测试全通过，266 个测试文件覆盖核心逻辑
2. **架构演进有序**：god-file 拆分为 11 步、march 重构分 6 步，每次都有 commit 记录
3. **共享代码质量高**：`shared/` 模块（worldMarchCore、battleSimCore 等）有良好测试覆盖
4. **文档齐全**：docs/ 下有完整的架构、设计、重构文档
5. **代码格式统一**：prettier 检查 100% 通过

---

*本报告由 WorkBuddy 在 2026-07-02 自动生成*
