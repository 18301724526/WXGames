# 6月23日开发日志

本文档从 2026-06-23 起持续维护，用于记录当天测试分支上的战斗渲染职责拆分、问题边界和验证结果。后续同日相关变更继续追加到本文档，不另起临时说明。

## 2026-06-23 战斗相机渲染职责拆分

现象：实体战斗场景缩放或拖拽时，角色单位会跟随相机变化，背景图保持全屏固定 cover 绘制，导致单位看起来在背景上滑动，缩放越大越明显。

边界判断：

- 问题属于前端 canvas 战斗渲染职责拆分，不涉及后端战斗会话、战斗模拟、API 或输入指令流。
- `BattleCameraPolicy` 已经负责相机数学，问题不是相机公式本身，而是背景层没有接入同一套战场世界坐标到屏幕坐标的 transform。
- HUD、底部军令按钮、结算弹层和按钮热区仍属于屏幕 UI 层，不进入 battle camera。

职责拆分结果：

- `BattleCanvasRenderer.getEntityBattleRenderContext` 统一生成实体战斗的 `layout`、`stage`、`arena`、`fit`、`camera` 和 `transform`。
- `drawEntityBattleBackground` 和 `drawEntityBattleField` 共享同一个 render context。
- 背景图在 stage clip 内按照 `arena * transform.scale` 和 `transform.offsetX/offsetY` 绘制。
- 单位继续按照同一个 `transform` 绘制世界坐标，避免背景和单位进入不同坐标系。
- `drawEntityBattleTopHud`、`drawEntityBattleControls`、`drawEntityReplayControls` 和 `drawEntityBattleResult` 保持屏幕坐标。

验证记录：

- `node --test frontend/js/platform/renderers/BattleCanvasRenderer.test.js`
- `node --test frontend/js/platform/renderers/BattleCanvasRenderer.test.js frontend/js/domain/BattleCameraPolicy.test.js`
- `node scripts/verify-refactor-plan-doc.js`
- `git diff --check`

测试服部署门禁补充：

- 推送 `140f094b` 后，测试服 CI 先被昨晚交互战斗提交留下的 Prettier 格式问题阻断。已使用 Prettier 3.8.4 对 CI 点名的 `BattleSimService.js`、`WorldCombatEncounterService.js`、`WorldCombatSessionService.js`、`world-combat-interactive-architecture.md` 和 `BattleCameraPolicy.js` 执行格式化，不改 CI 规则。
- 后续完整 node tests 暴露 `backend/tests/WorldExplorerService.test.js` 旧断言仍按“resolved seeded encounter 不重生”的语义判断 active encounter。当前业务契约来自 `dbe3e7df`：seeded `hostile_force_capital_ridge` resolved 后会立即 respawn 为 active，recentReports 保留已完成战报。已仅更新测试契约，不改变后端 respawn 业务逻辑。
- `node --test backend/tests/WorldExplorerService.test.js`

## 2026-06-23 多语言文本协议首轮收口

目标：把用户可见文案从渲染/投影代码里拆出，建立代码只引用语义 key、语言配置负责各语种展示的统一入口。首轮不做全仓字符串替换，避免把测试 fixture、协议枚举、日志、内容数据名和 vendor 文本误收进 UI i18n 范围。

职责边界：

- `LocaleTextRegistry` 负责维护按 locale 分组的稳定文案表，当前包含 `zh-CN` 默认表和 `en-US` 对照表。
- `LocaleText` 负责运行时语言选择、`t(key, params)` 参数替换和缺失 key fallback。
- 渲染层只通过 `t()` 读取用户可见 UI 文案，不再在 HUD 分支里创造英文 fallback。
- `WorldActorProjection` 对未命名敌军只输出 `nameKey/labelKey`，不再把默认英文名写进投影数据；后端或内容配置明确传来的 encounter `name` 仍作为内容名展示。
- 本轮范围只迁移已确认的可见英文点：行军/进攻 HUD、敌军默认文案、士兵数量、军政世界视图空态/回中按钮/格子计数、地图地点 fallback 标记。

首轮结果：

- 新增 `frontend/js/config/LocaleTextRegistry.js` 与 `frontend/js/domain/LocaleText.js`。
- H5 `frontend/index.html` 和 minigame `frontend/minigame/game.js` 已按加载顺序加入语言 registry/runtime。
- `WorldMarchHudCanvasRenderer`、`WorldMapMilitaryViewRenderer`、`WorldMapStaticEntryRenderer` 已接入 locale runtime。
- `scripts/run-architecture-smoke.js` 已把 locale registry/runtime 和对应测试纳入架构门禁。

验证记录：

- `node --test frontend/js/config/LocaleTextRegistry.test.js frontend/js/domain/LocaleText.test.js frontend/js/domain/WorldActorProjection.test.js`
- `node --test frontend/js/platform/renderers/WorldMarchHudCanvasRenderer.test.js frontend/js/platform/renderers/WorldMapMilitaryViewRenderer.test.js frontend/js/platform/renderers/WorldMapStaticEntryRenderer.test.js`

测试服门禁收口：

- 服务器部署门禁在 ESLint 与 Prettier 通过后进入 suppressions budget 阶段。该阶段要求 `tmp/eslint-suppressions.generated.json` 必须是本轮 lint 生成的新基线，不能沿用测试服工作目录上一轮残留文件。
- `scripts/test-server-ci-gate.sh` 在执行 `npm run lint:baseline:ci` 前清理 `tmp/eslint-suppressions.generated.json` 与 `tmp/eslint-suppressions.base.json`，只处理临时生成物，不放宽 ESLint 规则、不改 suppression 预算。

## 2026-06-23 多语言文本协议第二批：主运行时 UI

目标：继续在测试分支 `codex/battle-core-test-server` 上扩展统一语言模块，优先覆盖玩家进入游戏后第一层会看到的运行时 UI 壳文案。代码仍只引用稳定语义 key，不把 action type、路由、后端协议字段、城市名、人物名、事件标题、技能名等内容数据翻译成 UI 配置。

本轮迁移范围：

- `LocaleTextRegistry` 增补系统、顾问、资源、地图命令、地图加载、世界站点、出征配置、城市改名、版本更新等 `zh-CN/en-US` 对照 key。
- 顶栏与地图命令：`ResourceTopBarCanvasRenderer`、`MapCommandCanvasRenderer` 改为通过 `LocaleText.t()` 渲染资源名、人口、顾问/日志/设置、地图 dock、分城/事件按钮和命令面板标题。
- 系统 UI：`SystemCanvasRenderer` 与 `CanvasGameShellSystemUi` 统一登录、加载、网络重连、设置、确认框、日志、重置确认、输入用户名/密码等默认文案。
- 覆盖层 UI：`OverlayCanvasRenderer` 统一命名弹窗、奖励弹窗、资源详情标题、资源名、产出/消耗/净增长行。
- 顾问 UI：`AdvisorCanvasRenderer` 与 `ShellPresenter` 统一顾问标题、空态、前往/稍后按钮、日志空态、请求日志空态、势力/领土数量 fallback。
- 世界站点 UI：`WorldSitePresenter`、`WorldMapSiteOverlayRenderer`、`CanvasGameAppGuideUi`、`CanvasTerritoryActionHandlers` 统一站点归属/状态/指标、占领/入城/改名/出征按钮、出征配置、战报 fallback、城市改名 prompt。
- 更新提示：`H5UpdateRuntimeAdapter` 的强制更新 confirm/canvas prompt 改为读取统一语言 key。
- 世界地图空态：`WorldMapLayerCanvasRenderer` 的地图加载提示和同步提示改为语言 key。

明确边界：

- 本轮不迁移 `frontend/tools/*`、debug overlay、vendor、测试 fixture、资产 manifest 描述、日志诊断文本。
- 本轮不迁移建筑、科技、事件、任务、编队、战斗面板等业务内容模块的大量展示文案；这些模块需要按各自 presenter/配置边界分批做，不能混入主 UI 壳迁移。
- 后端/配置显式传来的 `name/title/summary/skillName/report.summary/message` 仍作为内容数据展示，不由 UI i18n 覆盖。

验证记录：

- `node --test frontend/js/config/LocaleTextRegistry.test.js frontend/js/domain/LocaleText.test.js frontend/js/platform/renderers/ResourceTopBarCanvasRenderer.test.js frontend/js/platform/renderers/MapCommandCanvasRenderer.test.js frontend/js/platform/renderers/SystemCanvasRenderer.test.js frontend/js/platform/renderers/OverlayCanvasRenderer.test.js frontend/js/ui/H5UpdateRuntimeAdapter.test.js frontend/js/state/UIStatePresenter.test.js`
- `npm.cmd run test:architecture`
- `git diff --check`

## 2026-06-23 多语言文本协议第三批：业务面板 UI 外壳

目标：继续在测试分支 `codex/battle-core-test-server` 上推进统一语言模块，把上一批明确暂缓的建筑、科技、事件、任务/攻略、军队编队与战斗 HUD 的 UI 外壳文案纳入 `LocaleTextRegistry`。本轮仍保持代码只引用稳定语义 key，内容数据本身不改为语言 key。

本轮迁移范围：
- `LocaleTextRegistry` 增补 `home.*`、`building.*`、`tech.*`、`event.*`、`task.*`、`guidebook.*`、`military.*`、`battle.*`、`tab.*` 与通用分隔符 key 的 `zh-CN/en-US` 对照。
- `HomePresenter` 统一游戏日期、人口上限提示、人口成长状态、城市规划 fallback、城市切换器默认标签与 meta 文案。
- `BuildingPresenter` 与 `BuildingCanvasRenderer` 统一建筑分类、建造/升级/满级/资源不足/引导锁定、成本、效果行、维护行、宜居压力、规模、军营训练行、建筑面板标题和翻页按钮。
- `TechPresenter`、`TechCanvasRenderer` 与 `TechTreeCanvasRenderer` 统一科技状态、按钮、行标签、路线/前置科技/空态/统计栏/详情兜底，以及列表和标签分隔符。
- `EventPresenter` 统一事件资源名、buff/士兵/需求格式、奖励/消耗/惩罚行、倒计时提示、空态、处理事件按钮和多选提示。
- `TaskGuidePresenter` 统一任务 tab/空态/action fallback 与攻略默认分类文案。
- `MilitaryPresenter` 与 `ArmyFormationEditorCanvasRenderer` 统一默认部队名、训练提示、侦察状态/方向/action、编队弹窗标题、已选提示、主副标记、补兵/确认/保存/翻页和空名人提示。
- `BattleScenePresenter` 与 `BattleCanvasRenderer` 统一回放战斗状态/结果/队名 fallback/战法状态，以及实体战斗顶栏、军令按钮、技能状态、自动开关、回放按钮和结算弹层。
- `TabBarCanvasRenderer` 统一底部 tab 文案。

明确边界：
- 不迁移玩家自定义城市名、编队名、人物名、技能名、战报正文、事件标题/描述/服务器下发选项 label、科技名、建筑名和建筑配置描述；这些仍是内容数据或用户数据。
- 不迁移调试 overlay、测试 fixture、vendor、manifest 描述、后端日志/诊断文本。
- 名人系统的大量技能说明、属性说明、品质/来源文案仍属下一批独立业务模块，不混入本轮。

验证记录：
- `node --test frontend/js/config/LocaleTextRegistry.test.js frontend/js/domain/LocaleText.test.js frontend/js/state/UIStatePresenter.test.js`
- `node --test frontend/js/platform/renderers/BuildingCanvasRenderer.test.js frontend/js/platform/renderers/TechCanvasRenderer.test.js frontend/js/platform/renderers/ArmyFormationEditorCanvasRenderer.test.js frontend/js/platform/renderers/BattleCanvasRenderer.test.js frontend/js/platform/renderers/TabBarCanvasRenderer.test.js frontend/js/platform/renderers/TechTreeCanvasRenderer.test.js`
- `node --check` 覆盖本轮新增/修改的 registry、presenter 与 renderer 文件。
- `git diff --check`

本地工具说明：
- 本地 `npx prettier --write ...` 触发 npm registry 解析失败，未联网拉取依赖；本地 `node_modules/.bin/prettier` shim 也不存在。没有改 Prettier/CI 规则，没有手工放宽门禁。最终部署仍以测试服 `scripts/test-server-ci-gate.sh` 的 `npm ci`、ESLint、Prettier check、suppressions budget、完整测试和架构门禁为准。

## 2026-06-23 多语言文本协议第四批：地图链路与运行时反馈补漏

目标：继续回答“别的地方呢”的覆盖问题，扩大到玩家实际会触达但前三批未完全收口的城市管理壳、任务/攻略壳、世界地图目标选择 fallback、地形 label 流转，以及命令执行后的运行时提示。仍然只走 `LocaleTextRegistry`/`LocaleText`，不新增临时翻译表，不改 CI 或项目规则。

本轮迁移范围：
- `LocaleTextRegistry` 增补 `home.city.*`、`guidebook.planning.*`、`task.center.*`、`world.targetPicker.*`、`home.planning.terrain.*` 和 `command.*` 的 `zh-CN/en-US` 对照。
- `CityCanvasRenderer` 统一城市列表、分城管理、城市 tab、军事入口、编队入口、默认城市/分城标签和 meta fallback。
- `GuideTaskCanvasRenderer` 统一攻略规划卡、任务中心标题、可领取统计、任务空态、奖励空态和任务标题 fallback。
- `WorldMapInputActionMap`、`WorldMapPickingModel`、`WorldMapSelectionResolver` 统一目标选择器在站点/单位重叠时的候选标签和副标题 fallback。
- `TileMapAssetManifest` 为玩家 HUD 会读取的基础地形加入 `labelKey`，`WorldTileMapTileNormalizer` 在生成 tile view state 时把 `labelKey` 解析为当前语言的 `terrainLabel`。
- `GameCommandService` 与 `CanvasGameAppCommands` 统一研究、切城、建造、接纳/放弃名人、应用方针、时代推进、行军开始/失败/返回/停止等运行时 fallback 提示。

明确边界：
- 后端返回的 `result.message`、玩家自定义城市名、编队名、人物名、战报正文、事件标题/描述、服务器下发选项 label、科技/建筑/技能内容名仍按内容数据处理，不由 UI i18n 覆盖。
- `TileMapAssetManifest` 中河岸、海岸、过渡、水面等模板 asset label 目前主要是资源/debug 元数据；除非证明它们进入玩家主 UI，否则不批量转 key，避免把美术资源描述混进语言模块。
- `DebugOverlaySnapshot` 的 `FPS`、`Map Bake`、`Visibility`、`Input` 等为默认关闭的诊断 overlay，本批继续列为开发诊断边界，不并入玩家 UI 文案迁移。
- 测试 fixture、vendor、工具脚本、日志诊断文本不作为本批 UI 文案迁移目标。

验证记录：
- `node --check frontend/js/config/LocaleTextRegistry.js frontend/js/platform/GameCommandService.js frontend/js/platform/CanvasGameAppCommands.js`
- `node --test frontend/js/platform/GameCommandService.test.js`
- `node --test frontend/js/config/LocaleTextRegistry.test.js frontend/js/domain/LocaleText.test.js frontend/js/state/UIStatePresenter.test.js`
- `node --test frontend/js/state/presenters/WorldTileMapTileNormalizer.test.js frontend/js/domain/WorldMapInputActionMap.test.js frontend/js/domain/WorldMapPickingModel.test.js frontend/js/domain/WorldMapSelectionResolver.test.js frontend/js/platform/renderers/CityCanvasRenderer.test.js frontend/js/platform/renderers/GuideTaskCanvasRenderer.test.js`
- `node --test frontend/js/platform/renderers/CivilizationCanvasRenderer.test.js frontend/js/platform/renderers/FamousCanvasRenderer.test.js frontend/js/platform/renderers/MilitaryCanvasRenderer.test.js frontend/js/platform/renderers/EventCanvasRenderer.test.js frontend/js/platform/renderers/CityPeopleCanvasRenderer.test.js frontend/js/platform/renderers/TutorialCanvasRenderer.test.js`
- `node --test frontend/js/platform/renderers/BuildingCanvasRenderer.test.js frontend/js/platform/renderers/TechCanvasRenderer.test.js frontend/js/platform/renderers/ArmyFormationEditorCanvasRenderer.test.js frontend/js/platform/renderers/BattleCanvasRenderer.test.js frontend/js/platform/renderers/TabBarCanvasRenderer.test.js frontend/js/platform/renderers/TechTreeCanvasRenderer.test.js`
- `npm.cmd run test:architecture`
- `git diff --check`

本地验证说明：
- PowerShell 直接执行 `npm run test:architecture` 会被本机执行策略拦截 `npm.ps1`；已使用同一项目脚本入口 `npm.cmd run test:architecture` 重跑，架构门禁 1093/1093 通过，并包含最终 `git diff --check`。
