# 素材清理记录 v0.1

## 0.1.186 unused asset cleanup

本次清理只处理已经确认不再被当前实现引用的素材和临时产物，不新增美术路线，不重做海岸，不改变已确认的正确河道 UV 方案。

### 已清空

- `tmp/`：清空本地临时出图、mask、接触表、预览图、日志等产物，并加入 `.gitignore`。
- 旧战斗整图序列帧：删除 `frontend/assets/art/battle/soldier-player-sheet.png` 与 `frontend/assets/art/battle/soldier-enemy-sheet.png`。当前战斗使用 `frontend/assets/art/battle/units/{player,enemy}/{idle,move,attack,die}/01-04.png` 拆分序列帧。
- 旧领土占位图：删除 `frontend/assets/art/territory-*-cutout.png`。当前世界地点使用 `frontend/assets/art/world-site-*-cutout.png`。
- 旧河流叠加尝试素材：删除 `tile-river-straight*`、`tile-river-junction-water*`。
- 旧河流模板尝试素材：删除 `river-template/tile-river-template-*.png` 与 `river-template/tile-river-template-ai-*.png`。

### 保留

- 名人系统三层立绘素材：`frontend/assets/art/famous-person/layers/fp-layer-v3-*`。
- 战斗拆分序列帧：`frontend/assets/art/battle/units/**`。
- 当前正确河道 UV 素材：15 张 `frontend/assets/art/tile-map/river-template/tile-river-bank-uv-*.png` 与 `frontend/assets/art/tile-map/tile-water-river-loop.png`。
- 当前 tile 测试页还在使用的基础地形、装饰、沙漠过渡和世界地点素材。

### 代码配套

- `CanvasGameRenderer` 移除对旧战斗整图 sprite sheet 的兜底路径，只保留拆分帧路径；拆分帧不存在时退回程序占位绘制。
- `CanvasGameRenderer` 预加载列表移除旧 `territory-*` 资源。
- 前端缓存串更新为 `asset-cleanup-v1`。
