# 20260528 名人立绘切图交接

## 当前结论

名人立绘 v3 现在只保留三层：`outfit`、`face`、`hair`。实验台和游戏内渲染已经统一为同一套布局逻辑，并且缩放锚点已经改为 512 画布中心，避免缩放时按左上角漂移。

本地实验台：

```text
http://127.0.0.1:8080/tools/famous-portrait-lab.html
```

如果本地服务没开，运行：

```powershell
node scripts/local-preview-server.js
```

## 源整图

可重新切图的整图已经提交到：

```text
frontend/assets/art/famous-person/source-sheets/
```

原始绿幕整图：

```text
source-outfit-sheet.png
source-face-sheet.png
source-hair-sheet.png
```

已扣透明整图：

```text
transparent-outfit-sheet.png
transparent-face-sheet.png
transparent-hair-sheet.png
```

优先从 `transparent-*.png` 重新切。三张透明整图是 RGBA PNG，背景 alpha 为 0，并且透明区域 RGB 已清为 0，避免切图工具误读隐藏绿幕颜色。

## 切片目标

正式游戏资源目录：

```text
frontend/assets/art/famous-person/layers/
```

目标文件固定为 30 张：

```text
fp-layer-v3-outfit-01.png ... fp-layer-v3-outfit-10.png
fp-layer-v3-face-01.png   ... fp-layer-v3-face-10.png
fp-layer-v3-hair-01.png   ... fp-layer-v3-hair-10.png
```

每张必须保持：

```text
512x512 PNG
透明背景
同一内部坐标规格
```

不要按每个素材自己的可见像素自动裁掉空白。这里最重要的是“同规格、同锚点”，否则实验台调好的参数进游戏还是会错。

## 实验台操作

实验台左侧是参数区，右侧固定预览区。右侧一屏内同时显示：

```text
完整预览
游戏头像区域
卡片实显
10 套规格检查
```

`套装编号` 会快速切换同编号的衣服、脸型、发型；每层文件下拉可以单独切换，用来检查混搭或定位某张问题图。

本地替换 PNG 后，不需要重启服务，点实验台里的 `刷新图片` 按钮即可重新加载。

## 注意事项

当前工作中已经发现旧切片有残留和切错边的问题，所以后续以 `source-sheets` 下的透明整图重新切为准。

`frontend/assets/art/famous-person/layers/fp-layer-v3-outfit-01.png` 已经有本地修改，提交时也会带上。回家后如果继续改切片，先确认 `git status`，避免覆盖自己刚切好的文件。

## 验证命令

切图或布局改完后至少跑：

```powershell
node --test frontend/tests/famous-portrait-lab.test.js frontend/tests/shared-canvas-renderer.test.js
```

如果要全量回归：

```powershell
node --test frontend/tests/*.test.js backend/tests/*.test.js
```
