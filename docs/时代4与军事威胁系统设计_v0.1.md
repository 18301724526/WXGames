# 时代4与军事威胁系统设计 v0.1

## 目标

时代4用于承接城邦时代后的外部压力：城邦不再只是内部建设，而是开始面对周边部落、商路、边界与秩序问题。第一版不做大地图战争，只做“士兵产生实际用途”的军事威胁闭环。

```text
城邦时代建造兵营
→ 兵营自动训练士兵
→ 进入古典时代
→ 威胁事件进入事件队列
→ 玩家选择应对方式
→ 后端按士兵/防御结算奖励、损失或 buff
```

## 设计原则

- 后端驱动：时代条件、威胁生成、选项、检定、士兵伤亡和资源变化全部由后端决定。
- 单位简化：第一版仍只使用“士兵”，不区分守卫、战士、驻防、出征。
- 轻惩罚：威胁失败不会导致文明倒退，只造成少量资源或士兵损失。
- 不做地图：第一版通过事件队列表达外部冲突，不做地图占领、敌军移动或 PVP。
- 复用事件页：威胁事件使用事件红点和事件详情弹窗，不新增复杂战斗 UI。

## 时代4定义

| 字段 | 设计 |
|---|---|
| 时代名 | 古典时代 |
| 进入条件 | 食物 900、木材 500、知识 260、士兵 3 |
| 核心变化 | 外部威胁开始进入常规循环 |
| 解锁建筑 | 工坊、学院、神庙 |
| 军事定位 | 士兵从“可见状态”变成“可用于结算的安全资源” |

士兵条件不是资源消耗，不会在进阶时扣除。它只要求玩家已经建造兵营并完成最小规模训练。

## 状态结构

```js
threatEventState: {
  nextAt: "2026-05-17T08:30:00.000Z",
  lastGeneratedAt: "2026-05-17T08:25:00.000Z",
  generatedCount: 0,
  recentTemplateIds: []
}

military: {
  soldiers: 4,
  soldierCap: 5,
  defense: 4,
  trainingProgress: 18,
  trainingIntervalSeconds: 30,
  defensePerSoldier: 1
}
```

## 威胁生成规则

- 只在 `currentEra >= 4` 后生成。
- 事件类型为 `threat`。
- 威胁事件队列上限为 2 个，避免玩家压力过大。
- 第一版固定每 360 秒检查生成一个威胁事件。
- 最近 2 个威胁模板不重复；模板不足时允许回退。
- 威胁事件和常规事件共享事件页红点，但有独立生成节奏。

## 威胁选项规则

威胁事件选项可以包含：

- `requirements`：显示/校验所需士兵或防御。
- `successEffects`：满足条件时应用。
- `failureEffects`：不满足条件时应用。
- `effects`：不做检定，直接应用。

第一版检定规则保持确定性：

```text
如果当前 defense >= requiredDefense 且 soldiers >= requiredSoldiers
  应用 successEffects
否则
  应用 failureEffects
```

## 第一批威胁模板

| 模板 | 条件 | 成功 | 失败 |
|---|---:|---|---|
| 边境试探 | 防御 2 | 获得 30 食物、8 知识 | 损失 30 食物 |
| 夜间火光 | 防御 4 | 获得 25 木材、10 知识 | 损失 1 士兵、20 木材 |
| 盗匪勒索 | 士兵 3 | 获得 60 食物 | 损失 45 食物；也可选择交付 25 食物直接化解 |

## 前端展示

- 事件列表卡片通过 `event.type === "threat"` 增加威胁样式。
- 事件弹窗继续展示后端下发的 `label` 和 `preview`。
- 军事页展示后端下发的士兵、防御、训练进度。
- 时代进阶条件中如果包含 `soldiers`，文明页显示“士兵 当前/要求”。

## 延后内容

- 主动出征
- 地图占领
- 多兵种
- 装备和金属资源
- 敌方单位与战斗动画
- 士兵驻防/出征拆分
- 战争科技树

第一版只把“士兵有用”这件事打通。后续系统稳定后，再考虑把威胁事件扩展成远征、据点和边境系统。
