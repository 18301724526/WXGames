# 生产工程化路线图 / Production Engineering Roadmap

日期 / Date: 2026-06-09

状态 / Status: active

目的 / Purpose:

P0-P11 已经把项目从历史耦合里拉出来：玩法、渲染、运行时、权威命令、配置、随机权威和稳定积木治理已经有明确边界。下一阶段不是继续堆功能，而是建立长期运营所需的生产工程化能力，让项目在 CI、部署、监控、备份、性能、配置发布和稳定积木晋升上都有硬门禁。

This document is the production-engineering authority for the post-refactor phase. It defines the guardrails that prevent the project from sliding back into architecture mud.

## 1. 当前结论 / Current Conclusion

- P0-P11 architecture refactor is complete for the current scope.
- Candidate modules are not automatically stable; stable promotion still follows `docs/stable_block_promotion_matrix_2026-06-09.md`.
- `npm run test:architecture` is the baseline local gate before commit/deploy; GitHub Actions and server deploy both call the same pre-deploy gate.
- Browser visual playtests are required only when a change touches real player input, Canvas visibility, tutorial highlights, online paths, deploy behavior, or backend action feedback.
- The next risk is operational: a project can be architecturally clean locally but still fail in production because CI, deploy, rollback, monitoring, backup, and config governance are weak.

Current deploy fact:

- WXGames / 文明火种 deploys through the server-side bare repo at `/home/git/wxgame.git`.
- Pushing `main` to that repo triggers `hooks/post-receive`, checks out to `/www/wwwroot/h5`, runs `/www/wwwroot/h5/deploy.sh main`, publishes `frontend/` into the H5 web root, rsyncs `backend/` into `/opt/wxgame-workspace/backend`, and restarts PM2 app `server`.
- `/opt/wxgame-workspace/backend` is a runtime copy, not the deploy Git source. Do not infer "push does not deploy" only because that runtime directory is not a Git worktree.
- H5 update prompts are driven by `/api/version` `deploymentId`, which hashes the deployed source state while ignoring local SQLite/log/runtime artifacts.
- `/api/version` supports `ETag` / `If-None-Match` and reads deploy manifest metadata when available.
- `deploy.sh` writes release identity to `/opt/wxgame-workspace/.wxgame/current-deploy.json`, appends `/opt/wxgame-workspace/.wxgame/deploy.log`, and keeps `.wxgame-deploy-version.json` in the frontend web root. It also rewrites the published H5 `index.html` local script/style `v` parameters to `deploy-<commit>`, then re-runs the frontend manifest guard against that exact version, so update-prompt reloads fetch a single coherent frontend release instead of mixing cached old JS with a new HTML shell. Before PM2 restart it runs the retired world-explorer ready-state cleanup and publishes the current config release, so production `CONFIG_RELEASE_GATE=required` validates a matched active release instead of discovering drift after the API process starts.
- `scripts/verify-deploy-hook.sh` is the read-only server hook verification entry. It checks `/home/git/wxgame.git`, executable `hooks/post-receive`, expected work tree, deploy script wiring, and current deploy commit reachability.
- `scripts/rollback-deploy.sh <branch|tag|commit>` is the explicit rollback entry. It resolves the target in the server bare repo, calls `deploy.sh` with the target commit, and appends rollback evidence to `/opt/wxgame-workspace/.wxgame/deploy.log`.
- `scripts/backup-runtime-state.sh` creates runtime backups under `/opt/wxgame-workspace/backups` by default: SQLite save DB, shared config, deploy state, manifest, archive, checksum, and retention pruning.
- `scripts/restore-runtime-state.sh <backup.tar.gz>` is the explicit runtime restore entry. It refuses to run without `WXGAME_RESTORE_CONFIRM=restore-runtime-state`, creates a pre-restore backup by default, verifies checksum when present, stops/restarts PM2 when available, and restores shared config plus SQLite DB; deploy state restore is opt-in with `RESTORE_DEPLOY_STATE=1`.
- `scripts/install-runtime-backup-cron.sh` installs or updates the marked cron entry `WXGAME_RUNTIME_BACKUP`, defaulting to `17 3 * * *`, `/opt/wxgame-workspace/backups`, `/opt/wxgame-workspace/.wxgame/backup.log`, and 14-day retention.
- `scripts/verify-runtime-backup.sh` verifies the latest runtime backup archive, checksum, age, manifest, shared/deploy-state entries, and SQLite DB entry.
- 2026-06-11 host evidence closed the current P12-004 backup/restore loop: production cron is installed with the `WXGAME_RUNTIME_BACKUP` marker, manual backup `/opt/wxgame-workspace/backups/wxgame-runtime-20260611T145310Z-drill.tar.gz` verified, non-production restore drill completed under `/opt/wxgame-workspace/restore-drill/20260611T145324Z`, production health stayed OK, and post-required-gate backup `/opt/wxgame-workspace/backups/wxgame-runtime-20260611T152149Z-post-config-required.tar.gz` includes `deploy-state/config-release/configReleases.json` and `deploy-state/config-release/configActiveRelease.json`.
- Server hook anomaly follow-up on 2026-06-11 found that `/www/wwwroot/h5` lacked root/backend gate dependencies, so the pre-deploy architecture gate saw `590 / 586 / 4` instead of `609 / 609 / 0`. `scripts/pre-deploy-gate.sh` now defaults to auto-installing gate dependencies when `node_modules` is missing or lockfile hashes change, while still allowing `WXGAME_GATE_INSTALL=never` for intentionally pre-provisioned environments. Later deploy evidence at `08639bab086d5d87ebb7445a043ffb72cc88754c` confirmed server architecture gate 613/613 pass.
- `backend/services/PerformanceCapacityBudget.js` is the backend runtime capacity budget boundary for API/action latency, request/response body size, save size, total world-map tile count, mission count, and world-map window/chunk size.
- `frontend/js/ecs/projection/WorldMapPerformanceBudget.js` also checks renderer frame work budgets: visible entries, actors, hit targets, frame pixels, active chunks, and per-chunk entries.
- `scripts/profile-h5-performance.js` is the repo-local H5 browser profiling evidence command. `npm run profile:h5-performance` serves the real frontend with stub API routes, runs mobile and desktop Playwright viewports, records navigation/resource timing, long tasks, RAF samples, canvas signals, screenshots, and writes `.local-logs/h5-performance/<runId>/profile.json`. `npm run profile:h5-phone-sim` adds 2026 phone low/mid/flagship simulation profiles through Chromium CPU throttling, mobile viewport/DPR/touch, browser-visible CPU/memory injection, V8 heap caps, and SwiftShader/low-end GPU flags.
- H5 asset preload currently waits for image resources only; world-map tile metrics/mask/dry-template cache prewarm is scheduled after preload through `worldMapRenderer.scheduleWorldTileCachePrewarm()` as a device-tiered background task. Low/mid mobile-class devices apply water/active-exploration refresh floors through `CanvasGameShellWorldMapRuntimePolicy`, shared by `CanvasGameAppRenderScheduler`, so ready-after-first-render map animation does not repaint at desktop cadence. This keeps `assets:preload` from blocking on full map cache prewarm while preserving a synchronous `deferPrewarm:false` option for focused tests or explicit callers.
- `backend/services/config/ConfigPipeline.js` and `scripts/validate-config-pipeline.js` are the current P12-007 local config release gate. `npm run config:validate` validates registered config registries and `npm run test:architecture` compares them against `docs/config_registry_snapshot_2026-06-11.json` so content changes require a matching registry version bump. `backend/services/config/ConfigReleaseService.js` adds the current audit-only preview/publish/rollback/runtime-status layer, admin `/api/admin/config-releases*` endpoints, `/api/health.configRuntime`, startup release gate policy, and `frontend/tools/config-release-console.html`; production release state defaults to `/opt/wxgame-workspace/.wxgame/config-release/` so backup archives include release history and the active pointer. `backend/services/config/ConfigRuntimeLoader.js` builds a read-only payload bundle only after the active release matches the current registry. Production deploy now publishes the checked-out config snapshot before PM2 restart; admin publish remains an explicit audit console path, not gameplay hot loading. `backend/services/config/GameplayConfigRuntime.js` is now the explicit gameplay facade for game, building, era, tutorial, and tech-tree config consumption, using the active release bundle when ready and module fallback only in warn/off observe modes.
- 2026-06-11 host evidence closed the current P12-007 config-release loop: release A `20260611T150609576Z-68505e575ff6-bb42d010` and B `20260611T150609717Z-c084d0e8c91e-b227985c` were published through the admin API, B -> A rollback produced `20260611T150910544Z-68505e575ff6-b7fd69a6`, active was restored to B through rollback record `20260611T150910561Z-c084d0e8c91e-7a01510b`, and production `CONFIG_RELEASE_GATE=required` restarted healthy with `configRuntime.status=matched`, loader ready, and `gameplay.source=active-release-bundle`.
- P12-006 runtime/dependency/admin-auth risk is reduced for current scope: production Node was upgraded from `18.20.8` to `20.20.2`, PM2 was reinstalled under Node 20, and `better-sqlite3@12.10.0` was rebuilt against the new runtime. `backend/package.json` now declares `node >=20.0.0`; unused backend `uuid` was removed; transitive `qs` is locked to a fixed version; `npm run security:audit` blocks unexpected/fixable backend audit findings. The only current allowed residual is `xlsx` high severity with no npm-audit fix, compensated by constrained task-definition XLSX import parsing. `scripts/verify-production-security-config.js` / `npm run security:production` and `scripts/rotate-production-secrets.sh` now define the auditable production secret/deploy/server-access review mechanism: production must prove strong independent `JWT_SECRET` and `OPS_JWT_SECRET`, bcrypt ops password hash, bumped `OPS_SESSION_VERSION`, explicit `ADMIN_USERS`, restricted `CORS_ORIGINS`, required config release gate, named server/deploy credential owners, and no Git remote embedded plaintext passwords. Remaining P12-006 work is executing that rotation/review on the actual host and storing the generated redacted evidence.
- P12-009 current ops-console and ops-agent scope is implemented: `backend/services/OpsControlService.js`, `backend/services/OpsAuthService.js`, `backend/routes/opsRoutes.js`, `backend/middleware/maintenanceMiddleware.js`, and `frontend/tools/ops-console.html` expose an independent ops-admin operations console for server/PM2/health/deploy/config/metrics/player/log/audit status, soft maintenance mode, and audited PM2 restart. The ops dashboard health card defaults to a `local-process` summary assembled from version, observability, config runtime, loader, and gameplay runtime state, avoiding synchronous self-probes; `OPS_HEALTH_URL` is reserved for explicit external health probes. The ops console uses `/api/admin/ops/login` plus `cf_ops_token` and does not depend on player login tokens. Hard stop/start is handled by the separate `backend/ops-agent/server.js` control plane after host installation: it defaults to `127.0.0.1:3101`, reuses independent ops-admin auth, controls only the configured PM2 app, writes an agent audit log, and is installed/restarted through `scripts/install-ops-agent-pm2.sh` plus optional `/ops-agent/` reverse proxy.
- P12-010 current multiplayer-sync hotfix scope is implemented: `server.js` is now an API gateway without active-player world tick ownership, `backend/world-worker.js` runs `WorldWorkerService` as PM2 app `wxgame-world-worker`, heartbeat writes go through `PresenceService` with persistence throttling, `AuthService` has a short player-existence cache for heartbeat bursts, `DatabaseRuntime` applies WAL plus bounded SQLite `busy_timeout` consistently across gateway and worker processes, and `scripts/loadtest-bot-heartbeat.js` provides controlled BOT login/heartbeat load testing with an 80% target-utilization threshold. World AI/global expansion is no longer part of default player runtime advancement; it requires explicit `{ advanceWorldAi: true }` from a dedicated authority path.
- 2026-06-12 multiplayer world-map authority scope is implemented for the `test1` blocking root cause: `WorldMapAuthorityRepository` moves materialized world tiles into `global_world_chunks` / `global_world_tiles`, player visibility into `player_world_visibility`, and keeps `game_states.worldMap.tiles` empty on disk. Global base terrain is random-but-fixed by shared world seed, canonical coordinate, and generation algorithm version; first reveal only commits/caches that deterministic result plus dynamic payload, so later players hydrate the same tile after visibility exists and account reset/respawn cannot change base terrain. Legacy hidden/AI per-player tiles are stripped instead of promoted into global authority, preventing old `test1`-scale hidden maps from bloating player saves and runtime normalization. `WorldExplorerRoutePlanner` still attaches direction/event/AOI generation context for route previews and dynamic content, but that context must not decide base terrain, water, or transition terrain.
- Local Windows development now uses Git Bash for shell validation; `scripts/check-shell-scripts.js` runs `bash -n` over project-owned deploy/rollback/hook/backup/restore/cron/verify scripts and is part of `npm run test:architecture`.

## 2. 反泥潭规则 / Anti-Mud Rules

1. Local pass does not mean online safe. CI must run the same architecture gate before merge/deploy.
2. Deployable does not mean operable. Each release needs version identity, health checks, rollback steps, and deploy logs.
3. No metrics means uncontrolled. Backend health, API latency, error count, game action failures, and frontend load failures must be observable.
4. Backups are not real until restore is tested. Save data and config data must have restore drills.
5. Config changes are product releases. Schema, registry metadata, version, diff, validation, and rollback are required for config/table changes.
6. Stable blocks are closed for feature iteration. New features extend through adapters, registries, strategies, events, config, or neighboring files.
7. Visual/user-flow changes require evidence. Tutorial, Canvas hitTargets, highlights, deployment paths, or online resource loading must include screenshot/playtest evidence.
8. A task is not complete if nobody can operate it later. Every production-facing mechanism needs a runbook entry or a documented command.

## 3. P12 - 生产工程化 / Production Engineering

| ID | 优先级 / Priority | 目标 / Goal | 交付物 / Deliverables | 回归 / Regression |
| --- | --- | --- | --- | --- |
| P12-001 | P0 | CI architecture gate | Add a CI workflow or equivalent server-side command that runs syntax checks, focused candidate/stable tests, stable block guard, official doc guard, and `git diff --check`. | CI pass + `npm run test:architecture` |
| P12-002 | P0 | Release and deploy governance | Define release identity, deploy command, restart boundary, health check, rollback command, and deploy log location without disturbing unrelated running services. | deploy dry-run or documented command verification |
| P12-003 | P0 | Observability and alerts | Add health endpoint coverage, backend error/action-failure metrics, API latency checks, frontend asset/load failure capture, and alert thresholds. | health check test + observable sample output |
| P12-004 | P0 | Backup and restore | Define save/config/database backup schedule, retention policy, restore command, and a restore drill checklist. | restore drill on non-production target |
| P12-005 | P1 | Performance and capacity | Add budgets for world-map snapshots, renderer frame work, backend action latency, API payload size, save size, and large-map chunk/window behavior. | performance budget tests + sampled browser profiling when visual paths change |
| P12-006 | P1 | Security and access control | Harden admin/config endpoints, deployment credentials, server access, API auth assumptions, and secret handling. | security checklist + focused auth tests |
| P12-007 | P1 | Config pipeline | Turn config/table edits into validated releases with schema checks, registry version comparison, diff output, preview, publish, and rollback. | config validation tests + registry diff output |
| P12-008 | P1 | Stable promotion CI | Make stable promotion machine-checkable: candidate observation notes, public contract, extension path, reopen exceptions, tests, responsibility-index coverage, and manifest update must be present. | stable guard + responsibility-index verification |
| P12-009 | P2 | Operations runbook | Create a concise operating manual for deploy, rollback, log inspection, health checks, backup/restore, config publish, and emergency disable switches. | runbook command review |

Current P12 progress:

- P12-001 current scope is implemented: `.github/workflows/architecture-gate.yml` and `scripts/pre-deploy-gate.sh` run the shared `npm run test:architecture` baseline; `scripts/run-architecture-smoke.js` also auto-discovers `*Contract.test.js` / `*.contract.test.js`, and shell script syntax is guarded by Git Bash / `bash -n`.
- P12-002 current scope is implemented for release identity, deploy evidence, hook verification, and rollback entrypoints: query current deployment with `cat /opt/wxgame-workspace/.wxgame/current-deploy.json`, inspect recent deploys with `tail -n 20 /opt/wxgame-workspace/.wxgame/deploy.log`, verify runtime with `curl -fsS http://localhost:${PORT:-3000}/api/health`, verify hook wiring with `bash scripts/verify-deploy-hook.sh`, and rollback with `bash scripts/rollback-deploy.sh <branch|tag|commit>`.
- Remaining P12-002 work is running `scripts/verify-deploy-hook.sh` and one non-destructive rollback drill on the actual host, then recording the resulting deploy-log evidence.
- P12-003 current backend scope is implemented: `ObservabilityService` records a bounded in-memory window for non-skipped API routes, including status counts, per-path latency/failure stats, recent p95/max latency, slow requests, 5xx server errors, and `/api/game/action` / task-claim action failures.
- P12-003 current frontend ingest scope is implemented: `POST /api/client-events` accepts allowlisted frontend load/asset failure events, and `H5LoadTrace` reports phase/API/asset preload failures through `GameAPI.reportClientEvent()` as best-effort telemetry.
- P12-003 current operator surface is implemented: `GET /api/health` includes an `observability` summary, and authenticated admin `GET /api/metrics?pathLimit=20&eventLimit=20` returns API metrics, recent client events, client event type counts, and threshold alert codes.
- Remaining P12-003 work is persistent metrics export or APM integration, real alert delivery, alert/runbook drills, and any measured sampling decision for currently skipped hot-path routes.
- P12-004 current scope is implemented and host-verified: run `bash scripts/backup-runtime-state.sh` on the host to write `wxgame-runtime-*.tar.gz` plus `.sha256`, install the daily cron with `bash scripts/install-runtime-backup-cron.sh`, verify backup health with `bash scripts/verify-runtime-backup.sh`, and restore on a non-production target with `WXGAME_RESTORE_CONFIRM=restore-runtime-state ALLOW_RESTORE_WITHOUT_PM2_STOP=1 bash scripts/restore-runtime-state.sh <backup.tar.gz>`. 2026-06-11 evidence includes installed cron marker, verified archive `/opt/wxgame-workspace/backups/wxgame-runtime-20260611T145310Z-drill.tar.gz`, restore drill target `/opt/wxgame-workspace/restore-drill/20260611T145324Z`, and post-required-gate archive `/opt/wxgame-workspace/backups/wxgame-runtime-20260611T152149Z-post-config-required.tar.gz` containing config release state under deploy-state.
- P12-005 current budget-contract scope is implemented: `PerformanceCapacityBudget` checks backend API/action latency, payload size, save size, mission count, world-map size, and window/chunk capacity; `WorldMapPerformanceBudget` checks frontend renderer frame work and chunk/window budgets; `ObservabilityService` emits `PERFORMANCE_BUDGET_EXCEEDED`; `GameStateRepository` persists the latest save budget summary in `saveMetadata.performanceCapacity`.
- 2026-06-14 input/replay evidence budget scope is implemented: `WorldMapPerformanceBudget.checkInputIntent()` and `PerformanceCapacityBudget.checkCommandEvidence()` gate command diagnostics so replay evidence stays compact enough for H5/Canvas 30fps baseline work and does not smuggle renderer snapshots, full response payloads, timeline/AOI, or world-map tile arrays into logs. `WorldMapInputIntent.inputId` plus monotonic `clientSequence` now gives each routed map tap a stable diagnostic identity across local operation logs, API request/error logs, command authority metadata, replay summaries, H5 shell forwarded-action metadata, minigame/compat runtime-tap diagnostics, and compat app tap-entry breadcrumbs; failed world-march action evidence is regression-locked end to end from `CanvasActionController` to `GameAPI`; world-map runtime taps, shell forwarded actions, and compat app runtime taps now preserve async rejections instead of logging false success, so operation logs remain trustworthy for multiplayer replay diagnostics.
- P12-005 current local browser profiling scope is implemented: `npm run profile:h5-performance` runs the real H5 entry against a local stub API and writes repeatable performance evidence. 2026-06-11 sample `.local-logs/h5-performance/2026-06-11T08-15-05-203Z/profile.json` passed hard budgets; desktop passed cleanly, while mobile passed with a warning because 23 long tasks exceeded the warning budget of 12.
- P12-005 current simulated phone profiling scope is implemented: `npm run profile:h5-phone-sim` runs 2026 low/mid/flagship phone approximations. The 2026-06-11 pre-optimization baseline `.local-logs/h5-performance/2026-06-11T08-40-57-526Z/profile.json` passed hard boot/render/resource budgets with warnings and showed low-end `assets:preload` reaching 100% at 12.60s but ending at 27.44s because world-map cache prewarm was still synchronous. After deferring tile cache prewarm and applying mobile water-refresh floors, `.local-logs/h5-performance/2026-06-11T09-23-29-025Z/profile.json` passed hard budgets with low-end ready 14790ms, navigation load 12495ms, RAF p95 250ms, and 82 long tasks; mid ready 8920ms with 19 long tasks; flagship ready 4175ms.
- Remaining P12-005 work is timing-warning calibration/reduction after first render, production sampling threshold calibration, capacity smoke data for large saves/maps, and explicit decisions for any budgets that should become deploy-blocking instead of observability-only. Physical true-device profiling is replaced for now by documented local simulation because no device is available, but this simulation is not equivalent to real phone thermals, drivers, or browser variance.
- P12-007 current validation, audit, admin-tool, runtime-drift observability, startup release gate, read-only runtime bundle loader, explicit gameplay consumption, and host drill scope is implemented: `npm run config:validate` builds a config registry snapshot for game, era, tutorial, battle, tech tree, building, and task definitions; `docs/config_registry_snapshot_2026-06-11.json` is the current baseline; `npm run test:architecture` runs `scripts/validate-config-pipeline.js --baseline docs/config_registry_snapshot_2026-06-11.json` to catch schema/hash/entry/version drift and missing recommended version bumps. `ConfigReleaseService` and authenticated admin `/api/admin/config-releases`, `/active`, `/runtime-status`, `/preview`, `/publish`, and `/rollback` persist release history in `/opt/wxgame-workspace/.wxgame/config-release/` in production, expose the active release pointer, and report active-vs-current registry drift for audit; `/api/health.configRuntime` exposes the compact status, gate policy, and loader readiness. Server startup initializes `GameplayConfigRuntime`: production defaults to `required`, development/test defaults to `warn`, and operators can set `CONFIG_RELEASE_GATE=required|warn|off`. `ConfigRuntimeLoader` builds a hash-checked read-only payload bundle after gate match, and core gameplay code consumes game/building/era/tutorial/tech config through `GameplayConfigRuntime` with fallback only in observe modes. 2026-06-11 host evidence includes publish A/B, rollback B -> A, active restoration to B, and required-gate healthy restart on commit `08639bab086d5d87ebb7445a043ffb72cc88754c`. Remaining P12-007 work is extending the same explicit runtime facade pattern to any future config families.
- P12-006 current security/access-control scope is implemented for runtime version, dependency audit triage, ops-admin session hardening, XLSX import risk containment, and auditable production rotation checks: production now runs Node `20.20.2`, PM2 is installed under Node 20, and `better-sqlite3@12.10.0` has been rebuilt. Unused backend `uuid` was removed, `qs` is locked to the fixed audit version, and `scripts/check-backend-security-audit.js` / `npm run security:audit` now allow only the documented `xlsx` residual while failing unexpected or newly fixable vulnerabilities. `OpsAuthService` now rejects weak production ops secrets/plaintext passwords, supports `OPS_SESSION_VERSION` token rotation, rate-limits failed ops login attempts, and `opsRoutes` audits failed/rate-limited logins. `TaskDefinitionImportParser` constrains XLSX imports by size, sheet count, row/column count, formula rejection, dangerous-header rejection, and sanitized row construction while `xlsx` has no npm-audit fix. `scripts/verify-production-security-config.js` writes redacted evidence for secret strength, ops/session rotation, admin/CORS/config gate posture, server/deploy credential ownership, and Git remote password hygiene; `scripts/rotate-production-secrets.sh` is the guarded host-side entry to update `.env`, bump `OPS_SESSION_VERSION`, verify evidence, and optionally restart PM2. Remaining P12-006 work is running the host-side rotation/review with real production credentials and replacing embedded HTTP deploy passwords with a credential-managed deploy identity.
- P12-009 current operations console scope is implemented: `/tools/ops-console.html` always opens, logs in through independent ops-admin `POST /api/admin/ops/login`, then calls `/api/admin/ops/dashboard`, `/maintenance`, and `/restart` with `cf_ops_token`; dashboard health defaults to `local-process` state instead of synchronously curling the same backend, while `OPS_HEALTH_URL` remains an explicit external probe override. Maintenance mode is a soft stop that blocks player login/register/reset, gameplay, and building APIs while leaving admin/health/version/metrics reachable. Hard stop/start is now represented by a separate `backend/ops-agent` process and `/ops-agent` console panel: `POST /pm2/start`, `/pm2/stop`, and `/pm2/restart` are authenticated with ops-admin JWT, pinned to one configured PM2 app, audited, and installed with `scripts/install-ops-agent-pm2.sh`; remaining work is host-side install/reverse-proxy verification and operational drill evidence.
- P12-010 current multiplayer-sync hotfix scope is implemented: the gateway process no longer runs world background sweeps, the deploy script starts/restarts `wxgame-world-worker`, ops dashboard exposes world-worker PM2 status plus presence summary, SQLite runtime access is centralized through WAL/busy-timeout `DatabaseRuntime`, heartbeat no longer persists `lastActiveAt` per request, BOT accounts remain disabled unless `ENABLE_BOT_ACCOUNTS=1`, `BOT_ACCOUNT_COUNT`, and `BOT_ACCOUNT_PASSWORD` are set, and player saves no longer own the global world-map body.
- P12-008 current stable-promotion CI scope is implemented: `docs/stable_block_manifest_2026-06-09.json` now requires `promotionEvidence` for every stable block, including matrix review, observation notes, public contract, extension path, reopen exceptions, and node/npm regression commands. `scripts/check-stable-blocks.js` validates promotion evidence, responsibility-index stable entries, ASCII `Status:` parsing, and `candidatePromotionQueue` consistency, while `scripts/check-stable-blocks.test.js` is registered in `npm run test:architecture`. Remaining P12-008 work is promoting real candidate modules only after they have survived enough feature iteration and have complete evidence.

## 4. 推荐推进顺序 / Recommended Order

1. P12-001 CI architecture gate - current scope done
2. P12-002 release and deploy governance - current hook/rollback script scope done
3. P12-003 observability and alerts - current in-memory metrics and frontend ingest scope done
4. P12-004 backup and restore - current script and host drill scope done
5. P12-005 performance and capacity - current budget-contract, local profiling, simulated phone profiling, and startup cache-prewarm attribution scope done
6. P12-007 config pipeline - current local validate/snapshot/diff gate, audit-only publish/rollback/runtime-status endpoints, admin console, health drift summary, startup release gate, read-only runtime bundle loader, gameplay runtime facade consumption, and host publish/rollback/required-gate drill done
7. P12-009 operations runbook - current ops console plus minimum external ops-agent scope done; host install/reverse-proxy/drill evidence remains follow-up
8. P12-006 security and access control - current Node 20, dependency audit triage, ops session hardening, XLSX containment, production security evidence checker, and guarded rotation entry scope done; actual host secret rotation/deploy-credential replacement evidence remains follow-up
9. P12-008 stable promotion CI - current promotion-evidence and responsibility-index guard scope done

The order is deliberate: first prevent bad changes from entering, then make deploys reversible, then make runtime health visible, then protect persistent data.

## 5. 每步完成定义 / Done Definition

Each P12 item is done only when:

- The owner command/file/runbook is documented.
- The rollback surface is documented.
- `npm run test:architecture` passes unless the item is explicitly documentation-only and the doc guard passes.
- If the item changes deploy, browser, Canvas, tutorial, hitTarget, backend action, or online resource behavior, browser/playtest evidence is captured.
- The responsibility index is updated if a new module, script, workflow, or official document is introduced.

## 6. 大厂对标定义 / Large-Studio Parity Definition

For this project, "large-studio parity" does not mean copying a huge enterprise stack. It means the small-team version of the same engineering invariants:

- deterministic architecture gates before release
- documented deployment and rollback
- observable production health
- restorable persistent data
- bounded performance budgets
- validated config releases
- backend-authoritative gameplay results
- stable blocks that extend without casual internal edits
- screenshot/playtest evidence for player-visible flows

When these invariants are present, the project is no longer maintained by memory and luck. It is maintained by repeatable systems.

## 7. 下一步 / Next Step

Continue with multiplayer runtime verification before broad feature work: deploy the gateway/world-worker split, confirm `/api/health` and `/api/version` recover, then enable BOT credentials explicitly and run staged `scripts/loadtest-bot-heartbeat.js` passes toward the 80% target-utilization threshold. Keep P12-006 production rotation/review, P12-009 ops-agent reverse proxy drill, and durable alert delivery as follow-up work, but do not reintroduce world ticks into the API gateway.
