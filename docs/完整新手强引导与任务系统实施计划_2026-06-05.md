# 完整新手强引导与任务系统实施计划

日期：2026-06-05  
当前阶段：P0.6 英文单位资源目录迁移方案已完成，后续按本文件逐项推进
硬性流程：每个实现任务完成后，必须执行真实浏览器测试、单元测试、代码检查，并推送 GitHub `origin` 与服务器私服 Git `private`

## 原始需求记录

> 然后接下来补完后续引导，进入主城之后，初始时代送个民居，引导用户建造民居，建造之后又是出现顾问立绘spine，对话框，告诉玩家类似我们建造了民居，时代向前迈进了一步这类的话，然后引导用户点击对话框关闭对话，然后引导用户点击文明，点击进阶，进阶之后顾问再出来，就是恭喜玩家这类的话，然后说让玩家去任务看看可以领取奖励，然后引导用户点击任务，点击任务之后引导用户完成第一个主线任务，名字你取一个，任务表用excel来做，要求是我后期改任务奖励，任务名称，任务描述等可以直接改表，然后你写一个一键导表提交到服务器的软件，不要用命令行，对策划不友好，，第一个主线任务的奖励就是建造农田的必备材料和进阶文明的必备材料总和，领取奖励之后，继续强引导让用户建造农田，建造农田之后顾问继续说些什么，然后引导用户点击文明，进阶文明，进阶文明之后顾问再出来说似乎发生了什么事件，然后引导用户点击事件，然后完成森林低语，获得木材，获得了木材之后引导用户建造伐木场，建造伐木场之后引导继续完成第二个主线任务，第二个主线任务奖励就是进阶到下一阶段文明的材料，然后顾问说一些什么现在已经可以产出木材巴拉巴拉之类的话，然后引导用户进阶时代，一路引导到用户可以侦察土地，侦察第一次必定让用户看到一座空城，然后引导用户占领空城，引导用户为空城改名，引导用户为文明取个名字，然后游戏正式开始，控制权交还给用户，之前都是强引导，然后你必须要把开放的每一个系统，包括名人都要引导用户操作一遍，对了侦察要改，要引导用户编队，配置一位名人，然后让名人出城侦察，侦察逻辑是用户选择位置，然后队伍往那个地方走，一边生成迷雾内的地形一边解锁迷雾，注意为了防止性能和前端卡顿，在用户发起侦察的时候，确定路线的时候，后端就要计算生成了地图，并且发送给前端，前端只负责表演演出，跟着名人队伍的小人揭开迷雾，名人队伍再地图上的显示就用一开始引导的士兵序列帧就行，对了那个序列帧你应该要换个英文名的文件夹，中文可能会出问题，文件夹你需要设计一下结构，因为现在这个序列帧只是基础枪兵的移动序列帧，后续我还会补充待机，攻击，死亡等不同的序列帧，然后兵种也会有很多，所以你文件夹结构需要设计好，然后引导最后以引导用户打开科技，打开科技之后顾问出来说话，告诉玩家科技点影响文明发展进程，侧重点等等内容，然后点击对话框关闭，游戏正式开始，科技树让玩家自己选择，不用强制点，你需要把游戏的所有功能，进程链等全部扫出来，然后设计引导流程，目的就是让用户熟悉目前的所有功能，功能包括但不限于方针，手动配置人才，寻访名人这类的小功能，然后在进行任务的同时要拆分并解耦目前的项目架构，允许必要重构，保证单文件尽量不超过500行，单一职责，符合开闭原则，并且补充测试，先写任务文档，分步骤写，文档内写清所有要做的事情，p0，P1,P2这样列清楚，然后文档内标注进度，比如完成P0.1任务的时候，就在P0.1标题后面加一个已完成，然后下面任务详情里需要写清楚如何完成的，做了什么，每一个都这样，然后写文档的时候把我现在发的这些指令一起复制进文档，写完文档之后对照复制进文档中的我的指令，反复对比直到所有需求都覆盖了，如果需求有不清晰的地方，在文档内标注，然后自己设计一个对游戏来说合理且贴近我表达的大概意思的方案，理解了吗？理解了我就去睡觉了，你干活吧，每一个任务完成的时候都必须配备真实的浏览器测试，单元测试，代码测试，善用TDD模式推进任务

追加硬要求：

> 每一步完成都必须推送双远端，GitHub和我的服务器私服git

## 当前代码扫描摘要

- 引导：`backend/services/TutorialService.js` 当前将教程统一归一为 `completed`，`validateAction()` 全放行。这是完整强引导需要重建的核心。
- 首屏入城动画：`frontend/js/tutorial/TutorialIntroOverlay.js` 已有士兵行走、入城淡出、顾问 Spine 和输入拦截基础，可作为后续强引导视觉基座。
- 前端输入：`frontend/js/platform/CanvasGameShell.js` 已有 `isTutorialInputActive()`、`isTutorialActionAllowed()`、`blockCanvasModal`、科技树交互路由和地图拖动拦截逻辑，后续要扩成统一强引导输入门。
- 动作分发：`backend/actions/GameActionRegistry.js` 已覆盖 `build`、`advanceEra`、`claimEvent`、`assign`、`research`、`seekFamousPerson`、`setArmyFormation`、`startExplore`、`claimExplore` 等动作。
- 任务：`backend/services/TaskCenterService.js` 仍是占位，`claimTask()` 返回 `TASK_SYSTEM_DISABLED`。任务 Excel、奖励计算、任务进度和领取链路都要新建。
- 建筑与时代：`shared/buildingConfig.json` 和 `backend/config/EraConfig.js` 已提供建筑成本、时代解锁和进阶成本。任务奖励必须从这些配置动态计算，避免手写死值。
- 事件：`backend/services/EventService.js` 与 `backend/actions/ClaimEventAction.js` 已有事件领取链路，森林低语可作为教程必经事件重新接入。
- 侦察与地图：`backend/services/WorldExplorerService.js` 已有路线、plannedTiles、逐步揭示和客户端 mission 输出；`backend/services/TerritoryService.js` 仍保留旧方向侦察、占领、命名等领土逻辑。新侦察应优先使用 `WorldExplorerService` 的后端预生成方向。
- 名人与方针：`backend/services/FamousPersonService.js` 已有寻访、候选、属性成长、侦察型 archetype；`backend/services/TalentPolicyService.js` 已有系统方针、自定义方针和人口分配预览。
- 编队：`backend/actions/GameActionRegistry.js` 已有 `setArmyFormation` 动作入口，`frontend/js/platform/renderers/ArmyFormationEditorCanvasRenderer.js` 已有编辑器表现基础。
- 科技：`frontend/js/platform/interactions/TechTreeInteractionModel.js` 与 `frontend/js/state/presenters/TechPresenter.js` 已有科技树拖动、缩放、详情与研究入口，最终引导只打开科技并讲解，不强制研究。
- 资源路径：士兵序列帧已从中文路径迁移到 `frontend/assets/art/units/spearman/move`，并由 `UnitSpriteManifest` 统一生成路径；后续侦察队伍可复用同一单位动画 manifest。
- 测试现状：仓库已有 `node:test` 单测，前端 renderer 和后端服务覆盖较多；根目录 `npm test` 与后端 `npm test` 仍是占位或 pending，需要先补统一测试入口。
- 远端现状：`origin` 是 GitHub，`private` 是服务器私服 Git。当前本地和 `private/main` 对齐，`origin/main` 曾落后，需要后续每步推送双远端。

## 设计原则

1. 后端拥有真实模拟、任务进度、奖励发放、教程状态、侦察路线、地图生成和合法性校验。
2. 前端只负责输入、表现、动画、HUD、对话和已下发数据的逐步演出。
3. 强引导期间，除当前允许目标外，所有点击和拖动都必须拦截。
4. 任务、奖励、文案、引导步骤尽量数据化，避免散落在 renderer 中。
5. 每个 JS 文件尽量不超过 500 行；超出时按服务、状态机、renderer、presenter、interaction model 拆分。
6. 每个实现任务采用 TDD：先写能失败的单元或浏览器行为检查，再实现，再回归。
7. 前端 JS 改动必须更新 `frontend/index.html` 对应脚本缓存号，避免服务器继续加载旧文件。
8. 每个任务完成必须推送 `origin main` 和 `private main`，服务器行为必须实际检查。

## P0 任务

### P0.1 任务文档与需求覆盖审计（已完成）

如何完成：

- 新建本文件，复制原始需求和追加双远端推送要求。
- 扫描教程、任务、侦察、名人、方针、编队、科技、建筑、事件、前端输入和测试现状。
- 将需求拆成 P0/P1/P2，并给每项列出完成方式、测试方式和验收标准。
- 对不清晰需求列出解释和默认设计方案。

验证：

- 代码测试：`git diff --check`
- 单元测试：文档任务不改运行代码，后续实现任务必须补对应单测。
- 浏览器测试：文档任务不改运行界面，后续实现任务必须做真实浏览器验证。
- 推送：本任务提交后必须执行 `git push origin main` 与 `git push private main`。

### P0.2 双远端与测试门禁固化（已完成）

如何完成：

- 已新增 `scripts/run-node-tests.js`，自动发现并运行 `backend/tests` 与 `frontend/js` 下的 `*.test.js`。
- 已把根目录 `npm.cmd test` 从占位命令改为全量现有 `node:test` 聚合入口。
- 已把 `backend/package.json` 的 `npm test` 从 pending 改为后端单测入口。
- 已在本文档固定每步门禁：`node --check` 目标文件、相关 `node --test`、必要的全量回归、真实浏览器测试、双远端推送。
- 后续前端 JS 改动必须检查并更新 `frontend/index.html` 缓存号。

验证：

- 代码测试：`node --check scripts/run-node-tests.js`
- 单元测试：`npm.cmd test`
- Git 验证：`git status --short --branch`
- 推送：`git push origin main` 与 `git push private main`

### P0.3 重建后端教程状态机基础（已完成）

如何完成：

- 已将 `TutorialService` 从“永远 completed”恢复为 facade，不再默认关闭教程。
- 已新增 `backend/config/TutorialFlowConfig.js`，集中管理教程 step、事件推进映射和暂时放行动作。
- 已新增 `backend/services/TutorialProgressService.js`，负责创建初始状态、归一化、动作校验、手动推进、命名事件推进和老存档兼容。
- 已保留旧存档兼容：带 `disabled: true` 或 `completed: true` 的教程状态仍归一为完成并自由放行，避免线上老账号被突然锁死。
- 已修复 `/api/game/action` 执行动作后丢弃 `result.tutorial` 的问题，动作返回的新教程状态现在会持久化并返回给客户端。
- 已新增 `tutorialAdvance` 动作入口，用于后续前端/教程控制器按配置推进非业务型教程步骤。

验证：

- 单元测试：`node --test backend/tests/TutorialProgressService.test.js backend/tests/GameRoutesTutorial.test.js`
- 全量测试：`npm.cmd test`
- 代码测试：`node --check backend/config/TutorialFlowConfig.js backend/services/TutorialProgressService.js backend/services/TutorialService.js backend/routes/gameRoutes.js backend/actions/GameActionRegistry.js`
- 浏览器测试：P0.3 仅改后端地基，提交部署后用服务器页面 smoke 验证前端仍能启动；完整新账号强引导浏览器链路在 P1.1 起逐段验收。

### P0.4 强引导输入门统一（已完成）

如何完成：

- 已在 `CanvasGameShell.showTutorialHighlight()` 中支持 `allowedAction`，普通教程高亮现在只放行指定 action。
- 已新增 `matchesTutorialAllowedAction()` 与 `isTutorialHighlightActionAllowed()`，把高亮输入匹配逻辑从普通可点区域中拆出来。
- 已保持入城 intro 自有白名单逻辑，同时让普通教程 highlight 对地图拖动、科技树拖动和普通拖动全部拦截。
- 已补充 `CanvasGameShell.test.js` 覆盖非匹配 action 被拦截、目标 action 可执行、强引导期间 drag 被拦截。
- 已更新 `frontend/index.html` 中 `CanvasGameShell.js` 缓存号，确保服务器部署后浏览器能加载本次输入门修复。

验证：

- 代码测试：`node --check frontend/js/platform/CanvasGameShell.js frontend/js/platform/CanvasGameShell.test.js`
- 单元测试：`node --test frontend/js/platform/CanvasGameShell.test.js frontend/js/platform/renderers/TutorialCanvasRenderer.test.js frontend/js/platform/renderers/CanvasSurfaceRenderer.test.js`
- 全量测试：`npm.cmd test`
- 代码差异检查：`git diff --check`
- 浏览器测试：提交推送并部署后，用 Playwright 访问 `http://47.116.32.216/` 验证页面加载、canvas 存在、控制台无本次输入门导致的启动异常。

### P0.5 任务 Excel 数据模型和导表入口设计（已完成）

如何完成：

- 已新增 `backend/config/defaultTaskDefinitions.json` 作为服务端默认任务定义，包含民居、第一份主线物资、伐木场后续物资三条主线基础任务。
- 已新增 `backend/services/TaskDefinitionService.js`，支持 JSON 和 `.xlsx/.xls` 导入、字段归一、重复 ID 校验、奖励公式解析和运行时定义持久化。
- 奖励公式已支持 `buildCost:<buildingId>`、`advanceCost:<era>`、`upgradeCost:<buildingId>:<level>`，第一个主线奖励已按“农田建造成本 + 时代 1 进阶成本”动态计算，第二个主线奖励已按“时代 2 进阶成本”动态计算。
- 已新增 `backend/routes/adminRoutes.js`，提供任务定义读取、导入预览、导入提交和 Excel 模板下载接口。
- 已将 `TaskCenterService` 从空占位改为读取服务端任务定义，按玩家当前建筑、时代、事件和教程状态计算 active/claimable/completed。
- 已为存档新增 `taskProgress.claimed` 兼容字段，后续 P1 奖励发放和领取幂等可以直接接入。
- 已新增 `frontend/tools/task-table-uploader.html`，策划可在页面里选择 Excel/JSON、预览校验、提交服务器、刷新当前服务器任务表、下载模板，无需命令行。
- 已安装后端 `xlsx` 依赖用于服务器解析 Excel；前端不信任本地 Excel 结果，只负责上传文件。

验证：

- 代码测试：`node --check backend/services/TaskDefinitionService.js backend/services/TaskCenterService.js backend/routes/adminRoutes.js backend/repositories/GameStateRepository.js backend/server.js backend/tests/TaskDefinitionService.test.js backend/tests/GameRoutesTutorial.test.js`
- 单元测试：`node --test backend/tests/TaskDefinitionService.test.js backend/tests/GameRoutesTutorial.test.js backend/tests/GameStateServiceSplit.test.js`
- 全量测试：`npm.cmd test`
- 代码差异检查：`git diff --check`
- 浏览器测试：提交推送并部署后，用 Playwright 登录测试账号，访问 `http://47.116.32.216/tools/task-table-uploader.html`，验证页面加载、可读取当前服务器任务定义、可用 Excel 预览并导入。

### P0.6 英文单位资源目录迁移方案（已完成）

如何完成：

- 已将当前 `frontend/assets/art/士兵/移动` 的 11 帧迁移到英文结构，并用 SHA-256 校验确认新旧文件一致。
- 已采用结构：

```text
frontend/assets/art/units/
  spearman/
    move/
      001.png
      002.png
```

- 后续多兵种结构：

```text
frontend/assets/art/units/{unitId}/{animationId}/{frame}.png
```

- 已新增 `frontend/js/config/UnitSpriteManifest.js`，统一维护 `unitId`、`animationId`、`frameCount`、`frameDurationMs` 和帧路径生成。
- 已更新 `CanvasPreloadAssetManifest` 和 `TutorialCanvasRenderer`，通过 manifest key 取 `spearman/move`，不再硬编码中文 URL。
- 已更新 H5 `frontend/index.html` 与小游戏 `frontend/minigame/game.js` 加载顺序，并更新缓存号。
- 已删除旧中文路径下的 tracked PNG，后续新增待机、攻击、死亡、多兵种时都落在 `assets/art/units/{unitId}/{animationId}/{frame}.png`。

验证：

- 单元测试：`UnitSpriteManifest.test.js` 和 `CanvasPreloadAssetManifest.test.js` 覆盖 `assets/art/units/spearman/move/001.png` 到 `011.png`。
- 渲染测试：`TutorialCanvasRenderer.test.js` 覆盖入城士兵从英文路径取图、停留第一帧、入城淡出。
- 浏览器测试：提交推送并部署后，用 Playwright 访问服务器入城引导 URL，验证页面加载、canvas 存在、英文路径 001 帧可访问。
- 侦察队伍动画会在 P1.5 接入后用同一 `UnitSpriteManifest` 做真实流程验证。

## P1 任务

### P1.1 入主城后的民居引导（未开始）

如何完成：

- 解决“送民居”和“引导建造民居”的语义冲突：默认设计为后端送“民居建造材料/蓝图”，不直接自动建成，让玩家亲手点击建造。
- 新账号进入主城后，教程 step 推进到 `build_house_prepare`。
- 后端确保当前步骤能建民居：补足材料、允许教程期临时解锁或调整时代解锁规则。
- 前端强引导打开建筑/主城建筑面板，高亮民居建造按钮。
- 建造成功后显示顾问 Spine 对话：说明民居建成、文明向前迈进一步。
- 点击对话框关闭后推进到文明进阶引导。

验证：

- 单元测试：后端补给只发生一次、民居建造后 step 推进、非法跳步被拒。
- 浏览器测试：新账号从入城到建民居，顾问出现，非目标点击被挡。

### P1.2 第一次文明进阶和任务领取（未开始）

如何完成：

- 引导点击文明 tab，再高亮进阶按钮。
- 成功进阶后显示顾问祝贺对话，并提示去任务领奖。
- 引导点击任务入口，打开任务面板。
- 主线任务一名称暂定：`安居的火种`。
- 主线任务一完成条件：建成民居并完成第一次文明进阶。
- 主线任务一奖励：农田建造必备材料 + 下一次文明进阶材料总和，全部由配置公式计算。
- 领取后关闭任务或推进到农田引导。

验证：

- 单元测试：任务一完成条件、奖励公式、重复领取拒绝。
- 浏览器测试：文明进阶后任务入口可点，领取奖励后资源增加并推进。

### P1.3 农田、第二次文明进阶、森林低语、伐木场（未开始）

如何完成：

- 领取任务一后强引导建造农田。
- 建造农田后顾问说明粮食产出和文明扩张基础。
- 引导点击文明并进阶。
- 进阶后顾问提示发生事件。
- 引导点击事件入口，完成特殊事件 `森林低语`，获得木材。
- 获得木材后引导建造伐木场。
- 建造伐木场后引导完成主线任务二。
- 主线任务二名称暂定：`林间的梁木`。
- 主线任务二奖励：进阶到下一阶段文明的材料，按 `EraConfig` 动态计算。
- 顾问说明木材已经可以稳定产出，然后引导继续进阶时代。

验证：

- 单元测试：事件领取推进、木材奖励、伐木场建造、任务二奖励公式。
- 浏览器测试：从任务一领奖到伐木场建成，所有非目标操作被挡。

### P1.4 引导到可侦察土地并送侦察型紫卡名人（未开始）

如何完成：

- 在教程到达侦察前，后端给玩家一名随机侦察型紫卡名人。
- 使用 `FamousPersonService` 的 scout archetype，新增可控的教程发放入口，避免和普通寻访混在一起。
- 顾问引导打开名人系统，查看名人卡。
- 引导打开编队，配置这名侦察型名人到队伍。
- 引导保存编队。

验证：

- 单元测试：教程发放只发生一次、品质为紫卡、能力倾向为侦察、编队保存持久化。
- 浏览器测试：名人页可见该名人，编队卡可配置并保存。

### P1.5 新侦察：后端预生成路线和地图，前端只演出（未开始）

如何完成：

- 重做侦察入口：玩家选位置，提交目标和编队。
- 后端在确定路线时一次性计算路线、plannedTiles、首次侦察保底空城，并返回给前端。
- 前端不在动画中实时生成地形，只按后端返回的 route 和 plannedTiles 做逐步揭迷雾演出。
- 地图上的队伍小人使用英文目录下的 `spearman/move` 序列帧。
- 首次侦察必定发现一座空城，空城信息由后端持久化。
- 侦察完成后进入占领引导。

验证：

- 单元测试：路线合法性、后端预生成 tile、首次保底空城、前端不本地生成真实地形。
- 浏览器测试：选择目标后队伍沿路线移动，迷雾按步骤揭开，控制台无黑屏/异常。

### P1.6 占领、城市改名、文明命名（未开始）

如何完成：

- 引导点击首次侦察发现的空城。
- 引导占领空城，后端校验空城可无战斗占领。
- 引导为空城输入名称。
- 引导为文明输入名称。
- 命名完成后进入最后的科技引导。

验证：

- 单元测试：占领空城、城市命名长度/非法字符、文明命名持久化。
- 浏览器测试：从空城发现到占领和两次命名，全流程可走完。

### P1.7 科技系统收尾讲解并交还控制权（未开始）

如何完成：

- 引导打开科技。
- 顾问 Spine 对话说明科技点、文明发展进程和路线侧重点。
- 玩家点击对话框关闭。
- 教程标记 completed，控制权正式交还玩家。
- 科技树不强制研究，让玩家自由选择。

验证：

- 单元测试：最后 step 完成后教程状态为 completed，动作校验恢复自由。
- 浏览器测试：科技页可拖动/缩放，关闭对话后所有系统可自由操作。

## P2 任务

### P2.1 全系统覆盖引导补齐（未开始）

如何完成：

- 以当前开放系统为准，补齐所有系统的引导点。
- 必须覆盖：科技、文明、名人、任务、事件、建筑、方针、手动配置人才、寻访名人、编队、侦察、占领、命名。
- 对非主线强制节点，采用“强引导一次操作 + 顾问短说明”的方式，不强迫玩家做永久性错误选择。
- 方针引导默认只做一次预览和应用系统方针；手动人才配置引导只调整少量人口并可还原；寻访名人引导只做一次免费或教程专用寻访。

验证：

- 单元测试：每个系统 step 都有 allowed action、完成条件和跳步保护。
- 浏览器测试：从新账号开始，逐步打开并操作所有系统，最终完成。

### P2.2 策划友好的任务导表软件（未开始）

如何完成：

- 提供不依赖命令行的图形化导表工具。
- 推荐实现：后台管理网页或 `frontend/tools/task-table-uploader.html`，支持选择 Excel、预览差异、校验错误、确认上传、服务器返回导入摘要。
- 后端提供受保护接口，如 `/api/admin/tasks/import`，保存导入版本、导入人、时间和校验报告。
- 导表不直接改运行中玩家进度，只更新任务定义；玩家任务进度按 taskId 兼容。

验证：

- 单元测试：Excel 字段缺失、奖励 JSON 错误、重复 taskId、版本回滚。
- 浏览器测试：真实上传 Excel，服务器任务中心读取新名称、新描述、新奖励。

### P2.3 架构解耦和 500 行治理（未开始）

如何完成：

- 每完成一个功能模块，先检查涉及文件行数和职责。
- 将教程拆为配置、后端状态机、奖励服务、前端 presenter、前端 renderer、输入策略。
- 将任务拆为定义导入、进度计算、奖励发放、客户端 assembler、任务面板 renderer。
- 将侦察拆为路线服务、地图生成服务、mission 服务、前端演出模型、前端 renderer。
- 保持 renderer 不直接推导业务规则，只消费 view state。

验证：

- 代码测试：`node --check` 涉及文件。
- 单元测试：各拆分模块契约测试。
- 手工检查：新增或修改文件尽量小于 500 行；若临时超过，记录拆分计划。

### P2.4 服务器真实验收与回归记录（未开始）

如何完成：

- 每个任务完成后提交并推送 `origin main` 与 `private main`。
- 若任务影响服务器页面，必须访问 `http://47.116.32.216/` 对应验证 URL。
- 使用测试账号或从数据库找测试账号进行真实流程验证。
- 记录浏览器控制台错误、截图和关键步骤。

验证：

- `git log --oneline -1`
- `git rev-list --left-right --count origin/main...main`
- `git rev-list --left-right --count private/main...main`
- 服务器页面实际加载最新缓存号或 commit verify 参数。

## 推荐新手强引导流程

1. 入城动画结束，进入主城。
2. 后端赠送民居建造材料/蓝图，引导建造民居。
3. 顾问说明民居建成，点击对话框关闭。
4. 引导文明页，点击进阶。
5. 顾问祝贺，引导去任务。
6. 打开任务，领取主线一 `安居的火种`。
7. 引导建造农田。
8. 顾问说明粮食和发展。
9. 引导文明进阶。
10. 顾问提示事件。
11. 引导事件，完成 `森林低语`，获得木材。
12. 引导建造伐木场。
13. 引导完成主线二 `林间的梁木`。
14. 顾问说明木材产出，引导继续进阶。
15. 到达侦察开放点。
16. 引导打开名人，获得并查看侦察型紫卡名人。
17. 引导编队并配置名人。
18. 引导选择侦察目标，后端生成路线和地图。
19. 前端播放队伍行走和迷雾揭示。
20. 首次侦察发现空城。
21. 引导占领空城。
22. 引导为空城改名。
23. 引导为文明命名。
24. 引导打开科技。
25. 顾问讲解科技点和发展路线。
26. 关闭对话，教程 completed，控制权交还玩家。

## 不清晰点与默认设计

- “送个民居，引导用户建造民居”：默认不直接送已建成民居，而是送建造材料/蓝图，让玩家完成第一次建造操作。
- “初始时代建民居”与当前配置中民居 era 1 解锁存在冲突：默认使用教程临时解锁或调整早期配置，最终以后端校验为准。
- “一边生成迷雾内地形一边解锁迷雾”与“后端确定路线时就生成地图”并不矛盾：后端提前生成并下发 plannedTiles，前端按动画逐格揭示，看起来像边走边开雾。
- “所有开放系统”会随版本变化：每次实现前先扫描当前导航、动作分发和 renderer，维护覆盖清单。
- “任务导表软件”默认做图形化网页工具，而不是命令行脚本；如果后续需要桌面应用，可在网页工具稳定后再封装。

## 覆盖审计

| 原始需求 | 覆盖任务 |
| --- | --- |
| 补完进入主城后的强引导 | P1.1 到 P1.7 |
| 民居、农田、伐木场建造引导 | P1.1、P1.3 |
| 顾问 Spine 对话和点击关闭 | P1.1、P1.2、P1.3、P1.7 |
| 文明进阶引导 | P1.2、P1.3 |
| 任务系统 Excel 和可改奖励/名称/描述 | P0.5、P2.2 |
| 一键导表提交服务器，不用命令行 | P0.5、P2.2 |
| 主线任务一和任务二奖励公式 | P1.2、P1.3 |
| 森林低语事件 | P1.3 |
| 引导到可侦察土地 | P1.4、P1.5 |
| 首次侦察必定空城 | P1.5 |
| 占领空城、城市命名、文明命名 | P1.6 |
| 名人、方针、手动人才、寻访名人等系统全覆盖 | P2.1 |
| 侦察改为名人编队出城 | P1.4、P1.5 |
| 后端预生成路线和地图，前端只演出 | P1.5 |
| 地图队伍使用士兵序列帧 | P0.6、P1.5 |
| 中文资源目录改英文并可扩展多动作多兵种 | P0.6 |
| 最后打开科技讲解，不强制选择科技 | P1.7 |
| 架构拆分、单一职责、开闭原则、单文件尽量小于 500 行 | P2.3 |
| 每个任务真实浏览器测试、单元测试、代码测试、TDD | P0.2 与各任务验证段 |
| 每步完成推送 GitHub 和服务器私服 Git | P0.2、P2.4 |

## 进度日志

- 2026-06-05：P0.1 完成。已扫描当前代码中的教程、任务、侦察、名人、方针、编队、科技、事件、建筑和测试入口，建立本实施计划。
- 2026-06-05：P0.2 完成。新增统一 Node 测试聚合脚本，根目录 `npm.cmd test` 可执行现有后端和前端单测，后端 `npm test` 不再是 pending。
- 2026-06-05：P0.3 完成。恢复后端教程状态机基础，拆出教程配置和进度服务，保留旧存档兼容，并修复动作结果里的教程状态持久化。
- 2026-06-05：P0.4 完成。普通教程高亮接入 `allowedAction`，强引导期间非目标点击和所有拖动被统一拦截，并更新前端缓存号等待服务器部署验证。
- 2026-06-05：P0.5 完成。新增服务端任务定义、Excel/JSON 导表解析、管理接口、前端导表工具和任务中心定义读取，任务奖励公式可引用建筑成本和时代进阶成本。
- 2026-06-05：P0.6 完成。士兵移动序列帧迁移到英文单位目录，新增 `UnitSpriteManifest` 统一动画路径，更新预加载和教程渲染引用并删除旧中文资源路径。
