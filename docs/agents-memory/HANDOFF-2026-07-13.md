# 交接包(2026-07-13 凌晨,Fable 监督者 → owner 白天 codex 作业)

## 20:30 最新交接（本节优先于下方凌晨快照）

- 当前已验证代码基线：`3c009575b92446087ada6e6d93898a22071efd6a`。
- step18 reward-close 分派修复、step23 独立高亮层及 HUD/non-HUD 阻塞早退清理均已完成，并经 GLM/KIMI 双席复审通过。
- T19 唯一一次无消融 playtest 已越过 step23，首次真实阻断于 step27 `scoutWorldPanelOpened`。
- XHigh 已确定性定位：当前主根因为 `PRODUCT`。`TutorialHostContext.advanceTo` 在 `applyApiState` 之后才同步 Controller；应用响应时同步发布的 `state.changed` 仍观察 step26，旧规则重入并通过 `clearWorldMarchTarget:true` 清空刚写入的目标。
- `scripts/playtest-online-tutorial.js` 使用已退休的 `worldMarchTarget.pickerOpen` 判断 formation picker，是独立潜伏的 `HARNESS` 第二根因；不是 T19 当前首次阻断。
- 下一步不是派 Sol。先并行执行仓库根目录 `NEXT_TASKS/01-review-glm-step27.ps1` 与 `NEXT_TASKS/02-review-kimi-step27.ps1`，取得两份独立根因复核后再裁修复单。
- XHigh 报告：`tmp/xhigh-step27-world-march-answer.md`；预期复核报告：`tmp/review-glm-xhigh-step27.md`、`tmp/review-kimi-xhigh-step27.md`。

本目录 = Claude 侧记忆的仓库镜像(2026-07-13 04:00 快照,53 文件;晚间 Claude 回归后以 Claude 侧为准)。本文件 = 当前战线与明天可直接执行的任务队列。

## 当前状态(全部已验证)

- **挂死时代已落幕**:11 层病灶全清,T14/T15 零挂死。修复链 12 个本地 commit(未 push):f3e2caed → 66f1acc9 → 0438d140(S8 草稿) → 9384da37 → 706422f5 → 27cc1ff3 → 07a7b6c4 → bf97f111 → 71271293。备份锚点分支 `backup/2026-07-12-eight-fixes`。
- **S8 已裁决**:docs/architecture/northstar-s8-adjudication-2026-07-13.md(十题全过,类型 6 种,query 零新增)。owner 北极星:"教程和游戏不耦合,谁没了谁都能正常跑"。
- **T15(最新全程跑)**:无挂死,max-actions(260)耗尽停在 step12 specialEventTabOpened;era2 advanceEra 已过。**动作分布:openEvent 被重复点击 ~244 次无效**(run 目录 .local-logs/online-tutorial/2026-07-12T19-16-10-292Z,1464 个 highlight-openEvent 文件)——step12 软空转,是下一个要修的 bug。

## 明日第一单:sol xhigh 开放定位首考(直接贴 codex,建议 xhigh)

```
你是独立排查员,开放定位一个未解 bug。仓库 F:\AI Project\WXGamesLocal,main HEAD(71271293)。只读代码与证据+可新建未跟踪探针脚本;严禁改产品代码、严禁 commit/push、严禁公网;唯一答卷 tmp/xhigh-openevent-answer.md。

现象(运行时事实):教程 playtest 无挂死推进到 step12(specialEventTabOpened),高亮出现在 openEvent(森林事件)上,harness 对其重复点击 ~244 次,每次点击后步不推进、事件面板状态不满足推进条件,直到 260 动作预算耗尽。证据:.local-logs/online-tutorial/2026-07-12T19-16-10-292Z/(1464 个 highlight-openEvent 前后文件可逐一比对 before/after)。该步属未迁移 legacy 步(TutorialGuideFlowRegistry era2-open-forest-event 一族,参考 docs/architecture/northstar-s8-coverage-draft-2026-07-12.md 的 R09/R10 行,只读)。
可复现(如需):后端 PORT=3721 DB_PATH=tmp/xh.db node backend/server.js;预览 LOCAL_PREVIEW_PORT=8721 LOCAL_PREVIEW_API_BASE=http://127.0.0.1:3721 node scripts/local-preview-server.js;PLAYTEST_USERNAME=test1 PLAYTEST_MAX_ACTIONS=120 node scripts/playtest-online-tutorial.js --target=local --game-url=http://127.0.0.1:8721/ --api-base=http://127.0.0.1:3721/api(任何环节 3 分钟无输出=挂死自杀进程;跑完杀进程)。
任务:①openEvent 点击后的完整链路走到哪、为什么步不推进(文件/函数/行+机制)②after 状态里哪些字段与推进条件矛盾③最小修复建议(不实施)。附运行时证据、置信度(无运行时证据上限 70)、排查复盘。约 45 分钟未收敛则写当前最佳假设+证据即停。
```

## 答卷验证(廉价席,答卷出来后再贴)

按"定位=xhigh,验证=廉价席"分工:把 xhigh 的修复建议做成机械应用单(worktree 隔离,模板参考 tmp/verify-ultra-V2 那单的格式:钉 commit、插指定改动、跑 playtest、报里程碑)。worktree 配方铁律:git worktree add --detach + PowerShell New-Item Junction 联结**根 node_modules 和 backend/node_modules 两处**;删联结用 cmd rmdir 禁 rm -rf;题面写明禁 npm install/ci。

## 队列(按序)

1. step12 openEvent 定位+修复+T16 全程(PLAYTEST_MAX_ACTIONS 提到 400);
2. 通关后:E5 官方判据(确定化双跑 transcript 投影 diff 空 + witness==0 + npm test/smoke ℹ 原文)→ S7 收官;
3. S9a 批量迁移开工(依据 S8 裁决 + 56 行草稿表,分批 1 任务 1 commit,判据沿 S7:投影 diff+逐规则核销);
4. Luna 入队考(下次机械验证单与 terra 同卷双席);
5. 杂项:仓库根 `nul` 杂物文件待删;tmp/ 里 sol 报告已入库的惯例要收敛(报告别 commit);DeepSeek key 建议轮换。

## 纪律速查(codex 单模板七要素)

一任务一 commit;codegraph 优先禁大面积 rg;测试数字 ℹ 原文禁转述;监督者文档只读;做完即停等审查;附建议推理档位;禁 spawn 子 agent。环境红线:禁 --target=remote、禁 47.116.32.216。验证跑腿归 terra/Luna 不归 sol(反空烧)。分歧一律对等仲裁(廉价席判别实验),任何席位结论无证据特权。

## 04:50 增补(深夜末班更新,以此为准)

- T16:step6→step18 全通(森林事件 1开1领、lumbermill、era3),零挂死;停点=step18 领奖关窗后 highlight null(closeRewardReveal/claimTaskReward 未接稳定后通知,修法同 6e99c94a)。
- 新增 commit:6e99c94a(事件段通知契约)、预部署清理与门禁登记两笔;npm test 2493/2493 绿;smoke 仅剩 command-owner 家族 3 个策展棘轮(行号漂移,见代理章程队列 3)。
- **当日指挥权移交:docs/agents-memory/ACTING-SUPERVISOR-sol-max.md**(把它整个贴进 sol reasoning=max 新会话)。派单标准:docs/agents-memory/DISPATCH-PLAYBOOK.md。
- 私服部署由 Claude 在夜间执行并监控(结果见 DEPLOY 一节或晨间聊天记录)。

## DEPLOY(2026-07-13 04:52Z 完成,Claude 夜间执行)

- **已上线**:deployedCommit=364c6f8a(今晚全部 22 commit),deployedAt 2026-07-12T20:51:33Z;健康核验:version/login/game state 全 200。
- **关键坑**:post-receive 钩子只部署分支 codex/refactor-tutorial-guide-architecture,推 main 不触发!部署命令=git push private main:codex/refactor-tutorial-guide-architecture(推 main 本身安全无副作用,仅备份)。push 客户端可能超时,超时后用 git ls-remote private 核实已落地。
- 部署日志在服务器 /opt/wxgame-refactor/.wxgame/push-deploy.log;失败自动回滚(hardlink 快照)。
