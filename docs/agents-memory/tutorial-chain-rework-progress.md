---
name: tutorial-chain-rework-progress
description: "Overnight autonomous run (2026-07-03/04): tutorial chain rework A1/A2/B/C phase specs + live status; push every phase to private AND local(WSL); user returns tomorrow expecting latest code."
metadata: 
  node_type: memory
  type: project
  originSessionId: 2992377e-0795-4820-a4df-9cbb32be926b
---

USER MANDATE (2026-07-03 night): autonomously finish ALL phases, commit + push EACH phase to `private` AND `local` (both auto-deploy on push; local hook now deploys any codex/* branch; space private pushes ≥20min). User sleeps; wants latest code at work tomorrow. Design rules in [[soldier-economy-design-intent]].

Branch: codex/refactor-tutorial-guide-architecture.

**COMPLETE (2026-07-03 day): HEAD `754f1585`, private+WSL deployed. The playtest walks the ENTIRE new-player tutorial start→`completed` (EXIT=0, tutorialCompleted:true, finalStep 40, 33 beats): homestead→house→farm→era2→event→lumbermill→barracks-supplies→barracks build→first-army(1000)→scout-officer→famous→formation+auto-replenish→formation save→world march→first city(picker/conquer/name)→talents→famous seek→final tech→completed. Baseline 1864.** All 6 discovery fixes + Task #15 (first-city picker) + the naming-snapshot harness fix landed. Also merged parallel Codex ABCD command-pipeline commits (`369a4fe6`,`db2eb4b5`).

- **A1 DONE** (`cf5ab395`, pushed+deployed both): assignment food charge removed; refund + no-return-to-reserve kept (veterans-camp interim); tests 1827.
- **A2 DONE** (`47cdf336`, pushed+deployed both, tests 1828): gate + grants + floor plumbing gone; settlement commits 0 (message/locale fixed); combat expedition floor=1 front+back; TerritoryStateNormalizer persisted-mission floor also dropped (0-commit survives reload); enemy-side roles untouched. FLAGGED for future content decision: TerritoryShared ×100 reload re-scale still hits sub-100 COMBAT commits. isGuidedFirstCitySettlement presenter exemption deleted (existed only for the gate).
- (original A2 spec, executed): delete player-side MIN_EXPEDITION_SOLDIERS gate (TerritoryAction:26), tutorial 100-soldier scaffolding (ensureTutorialFirstCityClaimSoldiers/ensureTutorialSettlementSoldiers/getTutorialSettlementSoldierFloor + floor plumbing in normalizeMilitaryState — remove entirely, no dormant plumbing), settlement commits 0 soldiers (TerritoryConquestMissions:72 + :101 message), frontend mins (combat expedition floor=1; empty-site occupy enabled at 0). DO NOT touch enemy-side roles (TerritoryStateNormalizer floors, TerritoryShared scale, CombatTargets/RoutePlanner defaults). 5 old test pin-sites update + 4 new pins. Verify → commit → push both.
- **B DONE** (`b8805152`, pushed+deployed both, tests 1839): shared/tutorialFlowConfig.js single source (STEP_ORDER 37 names; TUTORIAL_STEPS = name→name; helpers stepIndex/stepName/payloadStepName/compareSteps/stepAtLeast/stepAtMost/stepBefore/stepEquals with legacy clamp/NaN semantics); currentStep persists NAME (normalizeTutorialState lazy-migrates numbers on every load); tutorialAdvance API accepts names+numbers; ~70 comparison sites converted; standard rules on factories (makeTabOpenRule/makeBuildRule/makeTaskClaimPairRules); blocking guard scripts/check-tutorial-step-contract.js in smoke (no step tables / raw comparisons outside shared+2 readers); tutorial-flow-config registry 2.0.0→3.0.0 (clientGate ids now names). TaskCenterService reads gates from shared directly (stale release bundle can't blank).
- **C DONE** (`376377d9`, pushed+deployed both, tests 1851): STEP_ORDER 41; chain era3Advanced→barracksSuppliesClaimed→buildingsTabOpenedForBarracks→barracksBuilt→firstArmyClaimed→scoutFamousGranted(claim); homestead task replaces house grant at cityEntered; TaskRewardClaimer soldiers branch (grants.firstArmy BEFORE normalize; fresh floor via TutorialSelectors.getFirstArmyReserveFloor until scoutFormationSaved, then re-clamp to cap — post-save residual reserve 300 + formation 1000, pinned) + famousPerson branch (grantScoutFamousPerson core kept in TutorialGrantService); barracks-only build window in validator; tab windows split at scoutFamousGranted; task-definitions 0.2.0, tutorial-flow-config 3.1.0; 11 zh/en locale keys; playtest walks new chain with hard asserts. NOTE: numeric saves never loaded since B re-map ~4 beats earlier (acceptable dev-stage; name saves unaffected). Playtest run vs WSL pending at checkpoint (tmp/playtest-new-chain.log).
- **C LIVE-VERIFIED via playtest** (fixes `05ed05cf`/`58db1c3e`/`16d460aa`/`52a8a34f`, all pushed+deployed both, baseline 1855). Walking the whole new chain drove out FOUR real fixes:
  - `05ed05cf`: shared/tutorialFlowConfig.js top-level `const api` collided in the browser global scope (Identifier already declared, invisible to node tests) — IIFE-wrapped; NEW permanent gate in check-frontend-script-manifest.js fails on top-level const/let/class collisions between classic scripts (self-tested with fixtures).
  - `58db1c3e`: TaskCenterAssembler orders claimable>active>completed — the 4 new tasks pushed the guided claim button below the panel fold so the highlight had no target and the guide stalled. Pinned by TaskCenterAssemblerOrdering.test.js.
  - `16d460aa`: barracks build guide fires house-style (showBuildingGuide force-opens buildings tab from any surface) — the claim leaves the player on world-map home where the two-beat command-panel design had no highlight target.
  - `52a8a34f` **(slice-11 world-march 403 ROOT CAUSE + fix)**: MilitaryService built formation-member validPersonIds from ONLY the flat `gameState.famousPeople`. That flat copy was kept populated by the per-load ensureScoutFamousPersonGrant which C deleted; now only persisted `famousPersons.people` holds the scout, so normalize stripped the scout from formation slot 1 → hasTutorialScoutFormation false → 403 TUTORIAL_BLOCKED, even though the client correctly showed the scout deployed with 1000 soldiers. Fix `collectValidPersonIds` unions BOTH roster collections at all 3 MilitaryService sites; proven on the live persisted save (normalize keeps the scout, gate passes). Forensics that cracked it: run the real TutorialSelectors + GameStateNormalizer on the server's /game/state response — passes before normalize, fails after → normalize is the culprit → validPersonIds source mismatch. This IS the 403 the user reserved for manual verify: now root-caused, not just observed.
  - playtest harness (scripts/playtest-online-tutorial.js) modernized for the walk: 8F modal snapshot probes (isBlockingPanelSnapshotOpen/getTargetPickerSnapshot/…), apiBase path strip (WSL /wxgame-test-api), ui-owner-first field reads, object-field comparator skip. Committed with the fixes.
- **`52a8a34f` roster union was a RED HERRING** (defensive, harmless, kept) — the ACTUAL 403 root cause is **`3b80c4d9`**: `getFormationSnapshot` (TutorialSelectors) read top-level `gameState.military.formations` FIRST, but `setCityMilitary` writes to `cities[cityId].military` (top-level left a stale empty-member sibling). Persistence is COLUMN-per-field (game_states table: separate `military`/`cities`/`famousPeople`/`tutorial` columns; `/game/state` VIEW projects cities→top-level so it looked fine, masking the split). Fix = city-first precedence matching getCityMilitary. Proven: loaded raw persisted columns + ran real selector+normalizer, false→true. Direct curl march after fix → 403 GONE (now `EXPLORE_TARGET_TOO_FAR` = normal distance check = gate PASSED). Playtest reached `scoutExploreStarted` (march fired).
- **ALL C CHAIN BEATS PROVEN** via playtest: homestead claim→house→farm→era2→event→lumbermill→barracks-supplies claim→barracks build→first-army claim (1000 soldiers)→scout-officer claim→famous→formation+auto-replenish→formation save→**world march fires**. HEAD/WSL/private all `9949da67`.
- **#15 FIXED (`cb57870c` product + `a16b2f5c` harness, deployed both)**: post-march first-city `openWorldSite` vs `openWorldTargetPicker` transient. User chose "guide through the picker". Product: (1) `matchesTutorialAllowedAction` (CanvasGameShell, single copy — App has none) accepts an openWorldTargetPicker carrying the guided site so the overlap click isn't blocked; (2) `renderFirstCityDiscovered` + new `TutorialGuideController.getWorldTargetPickerSiteCandidate` highlight `chooseWorldTarget` for the site candidate when the picker is open; falls back to direct openWorldSite once the actor clears (picker needs ≥2 candidates). zh/en `tutorial.highlight.chooseFirstCitySite`. Live-verified end-to-end: picker open→choose→picker closes, site selected, guide advances to conquer. Playtest walk now passes openWorldSite→chooseWorldTarget→claimConquest→**firstCityOccupied** (3 steps past the old stall). NEXT deeper stop is naming-submit at firstCityOccupied→firstCityNamed (pre-existing slice-10 content, harness waitForFunction timeout — separate, not investigated). Baseline 1864. HEAD `a16b2f5c`.
- (superseded) ONE OPEN ITEM: post-march beat `openWorldSite-56` — after the scout marches to the first-city tile, the scout actor overlaps the site so the hit target becomes `openWorldTargetPicker` (multi-candidate) instead of the `openWorldSite` the `firstCityDiscovered` tutorial highlight expects. This is pre-existing first-city-discovery tutorial content (slice-10 tutorial territory the user reserved for manual verify), exposed now that the march actually completes. Not investigated/fixed tonight (out of the barracks/army/replenish scope). Likely fix: firstCityDiscovered highlight should accept openWorldTargetPicker on the target tile, OR the guide should resolve the picker. FLAG for the user.
- FORENSICS TOOLING for next time: persisted saves are per-column JSON in `game_states` (WSL DB `/root/wxgame-test/backend/civilization.db`, WAL-locked over 9P — copy db-only after a pm2 restart checkpoint, or read columns via node:sqlite). The `/game/state` API response is a VIEW (projects cities.military→top-level), NOT the raw persisted shape — run selectors on the raw columns to see what validateAction sees.
- (original C spec, executed): chain era3Advanced→barracksSuppliesClaimed→buildingsTabOpenedForBarracks→barracksBuilt→firstArmyClaimed→scoutFamousGranted(claim-triggered); houseGuideReady claim-triggered (homestead task); 4 tasks (homestead/barracks supplies buildCost formulas; first_army soldiers 1000 + grants.firstArmy + fresh floor until scoutFormationSaved; scout_officer famousPerson reward reusing extracted grant core); TaskRewardClaimer 2 new branches; barracks build event; validator/tab windows; FlowRegistry factory rules + 3-branch formation (补兵 middle: scout in members && draft<=0 → autoReplenishArmyFormation highlight); locale keys; playtest walk extended; task-definitions 0.2.0; tutorial-flow-config 3.1.0. After C: verify → commit → push both → run playtest vs WSL → final report.
- (original B spec, executed): shared/tutorialFlowConfig (dual-mode file like shared/formationDeploymentEligibility.js; wire manifest+game.js before consumers): ORDERED step-name array = single source (numbers become derived indexes), event→step map, client-gate names, task→step map. Backend TutorialFlowConfig.js + frontend TutorialGuideStepPolicy.js become thin readers (kills the duplicated numeric maps). **Persist tutorial.currentStep as NAME** (option i): internal comparisons stay numeric indexes; convert at load/save boundary (normalizeTutorialState accepts old numbers via one-shot mapping with the CURRENT pre-insertion map + names); tutorialAdvance API accepts names. Rule FACTORIES for standard guide shapes (tab-open / build-X / open-task-center+claim) driven from config; custom rules stay hand-written keyed by name. ZERO behavior change; existing tutorial suites must pass unchanged; tutorial-flow-config registry version bump if entries change.
- **C (after B)**: split C1 backend / C2 frontend.
  C1: defaultTaskDefinitions.json 0.1.0→0.2.0 + 4 tasks: main_homestead_supplies (house build cost; replaces ensureHouseGuideResources), main_barracks_supplies (formula buildCost:barracks), main_first_army (NEW soldiers reward branch, 1000, EventService {type:'soldiers'} pattern → city.military.soldiers), main_scout_officer (NEW famousPerson reward type; replaces ensureScoutFamousPersonGrant). TaskRewardClaimer branches for both new reward kinds. New steps in shared config order: buildingsTabOpenedForBarracks, barracksBuilt (+claim windows reuse neighbors). BuildingActionService barracks→'barracksBuilt'; TUTORIAL_EVENT_STEPS + CLIENT gates; maybeAdvanceTutorialAfterClaim wiring; NEW explicit army-grant floor in normalizeMilitaryState (1000 while grants.firstArmy && stepIndex < scoutFormationSaved; fresh impl, A2 deleted old floor); validator window checks (validateScoutFormationAction etc.).
  C2: FlowRegistry rules via factories (homestead claim, barracks tab/build, army claim pair, officer claim) + 补兵 branch in renderScoutFormationMemberOrSave (scout in members && draft[scout]==0 → highlight autoReplenishArmyFormation, allowedAction {type:'autoReplenishArmyFormation'}; button exists ArmyFormationEditorCanvasRenderer:214) + StepPolicy canOpenTab windows.
- **Final**: update scripts/playtest-online-tutorial.js to walk the new chain; run it against WSL deployment as live-verify proxy; full gates; report to user with per-phase commits.

House regime every phase: characterization/RED-first tests, LF sed, prettier on new files, mode-symbol ban in new frontend files (9 symbols), gates = run-node-tests + architecture-smoke + script-manifest + mode-ownership-spine + lint + lint:baseline:check + git diff --check. Agent does mechanics → I independently verify → commit → push private + local.
