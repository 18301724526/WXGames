---
name: frontend-ecs-bridge-retirement
description: "Frontend ECS Batch 8 (Bridge Retirement) COMPLETE: 8A-8E retired + deployed; 8F blockingPanel EXECUTED and live in codex/pvpve-systems (confirmed by 2026-07-09 inventory) — ModalStore is the panel-state single source."
metadata: 
  node_type: memory
  type: project
  originSessionId: 1b60935f-8215-46d5-9378-8f488ca88b3d
---

The frontend ECS migration (branch `codex/refactor-tutorial-guide-architecture`, pushed to the `private` remote which auto-deploys; see [[local-dev-env]] / [[github-push-https]]) reached **Batch 8 (Bridge Retirement)**: retiring each modal's legacy `this.X` mirror so the ECS modal owner + renderer snapshot are the sole source, via `CanvasModalSnapshotAdapter` helpers (open/close/get/isOpen) replacing the per-modal bridge wrappers.

**Done + deployed, each `Ready for Migration Owner Review` (NOT marked Completed):** 8A naming, 8B confirmDialog, 8C rewardReveal, 8D event (extended guard catches `setIfChanged`/patch-key idioms; EventController's own `activeEventId` claim cursor stays), 8E targetPicker (Option B snapshot-direct; tip `8252085d`). Each slice = snapshot adapter helpers + remove bridge wrappers + a `check-frontend-ecs-<modal>-mirror-retirement.js` blocking guard + delete the obsolete per-modal ownership guard; verified by FULL `npm test` (see [[deploy-lint-gate]] — full suite ≠ `test:architecture` subset) + a 3-lens adversarial review (it has caught real HIGH regressions the suite missed).

**8F blockingPanel — EXECUTED & LIVE (2026-07-09 inventory confirmed: 12 panels in ModalStore, mirror fields deleted, `buildRendererPanelFacts` lives in `CanvasModeOwnershipRuntime.js` — old `CanvasModeOwnershipBridge.js` name is GONE; guard online; doc State=Ready for Migration Owner Review).** Original detail below (historical): It split `modal:blockingPanel` into 12 per-panel `modal:<panel>` subtypes (the locked FULL per-panel model — a single panelKey was insufficient because `activeCommandPanel==='tech'` and `techDetailOpen` provably coexist, now pinned by an Axis-3 simultaneity test). It is the ONLY slice touching the ECS mode-resolver core (ModeKeys mask + CAPTURE_PRIORITY + ModeResolver BLOCKING_MODAL_KEYS [11: 10 show-stars + techDetail, commandPanel excluded]) + regenerating the runtime bundle (`npm run build:ecs-runtime`; `EcsModeRuntimeBundle.js` is generated — never hand-edit). New guard `check-frontend-ecs-blocking-panel-mirror-retirement.js` (receiver-agnostic write detection); old ownership guard removed. **Non-obvious gotchas the plan under-specified:** `tutorialAdvisorDialogue` + `armyFormationEditor.open` are NOT panels but the old umbrella folded them into blocking detection — preserve as explicit OR terms (bridge) + a `NON_PANEL_CLOSEABLE` residual (`CanvasActionController.closePanels`, alongside the `closeEventSnapshot` side-effect); `deriveModeFacts.techTreeActive` read the raw `activeCommandPanel` mirror (flip to the `modal:commandPanel` payload). Verified: full `npm test` 1701/0, architecture-smoke + both guards 0, lint/format clean, all LF, 3-lens review (0 behavior regressions). Plan doc: `docs/development_logs/2026-06-27-frontend-ecs-batch-8f-blocking-panel-removal.md`. **8F completes Bridge Retirement.**
