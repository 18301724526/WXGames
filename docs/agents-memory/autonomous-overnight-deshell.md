---
name: autonomous-overnight-deshell
description: "Active overnight autonomous task — execute LOOP 1 frontend ECS de-shell (cuts 1-6) per the v2/v3 blueprint, tests-as-oracle, gate-green per commit, user asleep."
metadata: 
  node_type: memory
  type: project
  originSessionId: a4c6f5cf-d730-48f3-aa71-7fc073e953fe
---

User (2026-06-30 night) said: "今晚全部搞定，我去睡觉了" — execute the frontend de-shell autonomously, finish then let reviewers review (one-shot model, no mid-way human verify). User's north star restated: **Unity/UE decoupling — one System has many scripts, each script = one responsibility = one place to look when it breaks.** Maps to: **one store = one responsibility = one file.** User also declared **green gates ≠ healthy architecture** (the rot happened with all gates green), so do NOT over-invest in guards; invest in real decoupling.

**Scope tonight = LOOP 1 (frontend de-shell), spec = [single-source-foundation-blueprint.md](../../docs/architecture/single-source-foundation-blueprint.md):**
- 刀1 Fog: `ecs/owner/FogOwner` → `ecs/projection/FogProjection` pure fn (keep getLastFogOwner() name + schema='fog-owner-v1' field → zero test churn). 1 prod consumer (CanvasGameShellWorldMapLayerRuntime) + 1 read (CanvasGameShell:268).
- 刀3 Modal: delete `ecs/mode/ModalWorld` → `state/ModalStore` (single truth: open/token/payload/callbacks, NO `open` mirror). Renderer reads ModalStore. modalMask via openModal direct write OR ModalStore-read (mode degrade).
- 刀2 Battle: delete `ecs/owner/BattleOwner` → `state/BattleStore` (scalars+blob). `deriveActiveOverlay` pure fn. NOT into ModeState. Registry MUST be load-wired in index.html + game.js.
- 刀5 Mode: ModeState/ModeWorld/deriveModeFacts→updateModeWorld mirror chain → `selectMode(stores)` pure selector + `baseMode` owned in `state/UiModeStore`.
- 刀4 WMO: delete `ecs/system/WorldMarchOptimisticState` → `state/optimistic/{CommandBuilder,PendingStore,Reconciler}`; reconcile returns {nextExplorer, storePatch}; caller writes state.
- 刀6 host.state chassis: collapse host/lastGame dual-write to single point + StateWriter (with wholesaleReplace() dual entry). 43 writes / 17 files / fix the renderReadOnly in-place mutation.

**Decisions locked from 7 review rounds:**
- worldMap/territory shared-store = legit CQRS publish (derive-write only, no read-back) → LEAVE IT. Not a mirror.
- "many-writer ≠ many-truth": resources and military state can have many writers while still committing to one canonical server field. Writer count alone does not prove mirror disease; the actual risks were ambiguous legacy owners, frontend mirrors, and ECS shells.
- CityService legacy kill (刀7) real blast radius = **23 call sites / 50+ locations / 9 backend + 7 frontend files / 43-col INSERT drops 5 / ~40 client legacy reads** (NOT ~10). This + 12-domain write-encapsulation = **LOOP 2 (backend), separate session, render-independent.**
- Guards woven per-cut (not a final 刀9). Guard holes to fix when built: single-writer regex must catch wholesale `X.state={...}`; layer-dep checks run on SOURCE pre-bundle (esbuild inlining erases boundaries); globalThis dynamic write needs runtime assertion not static guard.

**STATUS (overnight run done):** 刀1-5 ALL DONE + each adversarially verified clean + gate green. Commits: 刀1 Fog e4133ca0, 刀4 WMO 7c4bf1aa, 刀2 Battle 383149d2, 刀3+5 Modal&Mode 597aa59f (on ba6934c9/a16fa306/6de028da). `ecs/owner/` dir GONE; ModalWorld + WorldMarchOptimisticState deleted; new state/{BattleStore,ModalStore,optimistic/*}. Branch green: npm test 1720/0, smoke exit0, lint/format/diff clean. NOT pushed. Note: battle cut DELETED check-frontend-ecs-battle-owner guard (re-add as single-source guard later). **SINGLE-SOURCE REFACTOR COMPLETE + LOCKED (2026-07-01).** All real 套壳/mirrors removed + verified + pushed + deployed: 刀1-5 (ecs shells→state stores), 刀6 host.state→StateWriter (83df7ca4), 刀7 CityService legacy kill→cities[id] sole truth (c605656b), dead-code cleanup (f9d0452d: Building{System,Validator,Effects}), 刀9 red-line guard locks it in (0e28ac49: scripts/check-frontend-single-source-redline.js — bans ecs owner shells / globalThis.Ecs*Owner / `<host>.state=` outside StateWriter; registered in smoke, mutation-tested). HEAD 0e28ac49, gate green npm test 1730/0. REMAINING (all optional/lower-priority, none are 套壳): remove bitecs framework (WorldClock/ModeWorld/FogVisibility→plain/SoA — non-套壳 misfit)【已作废：bitecs 后被钦定为唯一 ECS 标准，见 bitecs-ecs-standard.md，禁止移除】, drop DB vestigial columns (resources/buildings/population/military, needs migration), backend write-encapsulation (multi-writer single-truth, low harm). NEXT user-facing: live-verify the deployed cuts.

**刀6 DONE (83df7ca4, pushed):** StateWriter single write point; adversarial-verify CAUGHT a real load-order browser bug (optimistic eagerly bound StateWriter, loaded after it → null → first march throws; gate green via require-fallback masked it), auto-reverted; I restored + fixed (StateWriter loads before optimistic in index.html/game.js + new script-manifest order-pair guard) + amended. Gate green npm test 1730/0. NEEDS live-verify: state writes/currentTab/militaryView/optimistic march. **REMAINING:** bitecs-framework-removal (WorldClock/ModeWorld/FogVisibility/ModeComponents → plain/SoA — these are NOT 套壳, minor misfit, low priority); LOOP 2 backend (CityService 50+ kill / 12-domain multi-writer governance); red-line single-source guard (刀9). Morning live-verify target: fog/modal/battle/march on test server.

**DEPLOY STATUS (unresolved, server-side):** pushed 172d4539 to private+origin. Found+fixed a PRE-EXISTING deploy blocker: test-server-ci-gate.sh line 52 `lint:baseline:check --base main` failed because 15 ecs/ files (office domain→ecs move) had `no-unused-vars` suppressions drifting from main; root-caused = unused `catch (error)` boilerplate → renamed to `catch (_error)`, pruned orphaned suppressions, rebuilt bundle (commit 172d4539). Verified EVERY server-gate step passes locally end-to-end (npm ci --dry-run 0, lint 0, format 0, baseline:ci 0, baseline:check --base main 0, npm test 1720/0, architecture 0, backend check 0, diff clean). DEPLOY SUCCEEDED (confirmed later): test server now serves deploy-172d45398997 = the 5 de-shell cuts are LIVE. The line-52 fix WAS the real blocker; the deploy was just slow (~20min: 2×npm ci + 1720 tests + smoke), not broken — my "won't deploy" was premature polling. So: server-gate green + deploy works. Live-verify fog/modal/battle/march/panels on the test server.

**Method:** sequential (cuts 2/3/5 share ModeState/registries/bundle — coupled). Each cut: implement → `npm test` + `node scripts/run-architecture-smoke.js` + `npx eslint .` + `npx prettier --check .` + `git diff --check` all green → commit. NEVER commit red. LF endings (Edit writes CRLF → `sed -i 's/\r$//'` then verify diff --check). Bundle changes need `npm run build:ecs-runtime` + LF-normalize. The refactor branch was GREEN (1716 tests) at commit 6de028da. Push only if user asks (last push was explicit).
