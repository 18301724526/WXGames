# 6月11日重构与问题交接

日期：2026-06-11

状态：daily production-engineering handoff

关键机制提交：

- `08725064e201d3f1611962260b05a06d4871851c feat: add production engineering gates`
- `08639bab086d5d87ebb7445a043ffb72cc88754c fix: keep config pipeline independent of runtime gate`

对应远端：

- 服务器 `private/main`: 已部署到 `08639bab086d5d87ebb7445a043ffb72cc88754c`
- GitHub `origin/main`: 已同步到 `08639bab086d5d87ebb7445a043ffb72cc88754c`

## 一、交接结论

2026-06-11 的主要工作已经从“继续修局部问题”推进到“把生产工程化底座落进仓库并纳入门禁”。本次提交覆盖 CI/部署门禁、部署回滚、观测指标、运行时备份恢复、性能容量预算、配置发布流水线、玩法配置 runtime facade、管理员配置发布工具、前端加载追踪、世界地图分层契约和相关文档同步。

当前验证结果是通过的：

- `npm.cmd run test:architecture` 通过，613/613 tests 全绿。
- 服务器 deploy gate 通过，613/613 tests 全绿。
- `node scripts/validate-config-pipeline.js --baseline docs/config_registry_snapshot_2026-06-11.json` 通过，7 个 config registry 无 drift。
- `python C:/Users/Administrator/.codex/skills/encoding-safe-review/scripts/scan_encoding_risks.py ...` 通过，无编码风险。
- P12-007 聚焦测试通过：配置 runtime 19 tests，教程/世界探索/GameState 48 tests。
- P12-004 runtime backup cron、手动备份、备份校验和非生产恢复演练已在真实 host 完成。
- P12-007 config release publish/rollback drill、`CONFIG_RELEASE_GATE=required` 生产启动门禁和只读 active-release bundle 已在真实 host 完成。
- P12-006/P12-009 续更：生产 Node 已从 `18.20.8` 升级到 `20.20.2`，PM2 已在 Node 20 下重装，`better-sqlite3@12.10.0` 已重建；仓库新增 `/tools/ops-console.html` 和 `/api/admin/ops/*`，支持运维总览、软维护停服、审计日志和受审计 PM2 restart。2026-06-12 修正为独立运维管理员登录：`/api/admin/ops/login` 签发 `cf_ops_token`，不再依赖游戏玩家登录 token。
- 当前安全残余：`npm install`/`npm audit` 仍报告 3 个依赖漏洞（2 moderate，1 high），需要后续做依赖安全评审。

需要接手人优先注意：早前服务器 post-receive/deploy hook 出现过 `server hook anomaly`: `tests 590 / pass 586 / fail 4`。续查已定位为 `/www/wwwroot/h5` gate 环境缺少 root/backend `node_modules`，具体缺 `xlsx` 与 `better-sqlite3`；`scripts/pre-deploy-gate.sh` 已改成默认 auto 模式，缺依赖或 lockfile hash 变化时自动 `npm ci`。当前本地与服务器 architecture gate 均为 613/613 通过。

## 二、本次删除的旧阶段文档

本次新增本文档作为 6 月 11 日统一交接记录，并删除以下已被替代的阶段性重构、问题和交接文档：

- `docs/2026年6月10日白天公司改动详情.md`
- `docs/current_codex_transfer_2026-06-09.md`
- `docs/current_refactor_progress_2026-06-09.md`
- `docs/问题汇总_2026-06-11.md`

没有删除仍被官方文档 guard 依赖的权威文档，例如：

- `docs/current_product_design_2026-06-09.md`
- `docs/current_gameplay_design_2026-06-09.md`
- `docs/current_technical_architecture_2026-06-09.md`
- `docs/long_term_architecture_refactor_plan_2026-06-08.md`
- `docs/architecture_module_responsibility_index_2026-06-08.md`
- `docs/production_engineering_roadmap_2026-06-09.md`
- `docs/stable_block_promotion_matrix_2026-06-09.md`

本文已登记进 `scripts/verify-refactor-plan-doc.js` 的官方文档集合，后续不要再新建散落的 handoff、问题汇总、重构进度文档；如果必须新增，应同步注册到 guard 和责任索引。

## 三、生产工程化改动

### 1. CI 与部署门禁

新增 `.github/workflows/architecture-gate.yml`，GitHub push/PR 会运行同一套 architecture gate。

新增 `scripts/pre-deploy-gate.sh`，`deploy.sh` 在发布前默认运行 `npm run test:architecture`。部署侧可通过 `SKIP_DEPLOY_GATE=1` 显式跳过，但正式发布不建议跳过。

`scripts/run-architecture-smoke.js` 被扩展为统一门禁入口，当前包含：

- 大量前后端 `node --check` 语法检查。
- 613 个 focused architecture tests。
- 自动发现 `*Contract.test.js` 和 `*.contract.test.js`。
- stable block manifest guard。
- repository hygiene guard。
- frontend script manifest guard。
- shell script syntax guard。
- config pipeline baseline diff guard。
- official document guard。
- `git diff --check`。

新增 `scripts/run-architecture-smoke.test.js`，锁定 contract test 自动发现、重复测试去重等行为。

### 2. 部署身份与回滚入口

`deploy.sh` 支持部署 branch/tag/commit ref，并写入部署状态：

- `/opt/wxgame-workspace/.wxgame/current-deploy.json`
- `/opt/wxgame-workspace/.wxgame/deploy.log`
- 前端 web root 的 `.wxgame-deploy-version.json`

新增：

- `scripts/verify-deploy-hook.sh`: 只读验证服务器 bare repo hook 是否正确连接到部署工作树。
- `scripts/rollback-deploy.sh <branch|tag|commit>`: 显式部署指定 ref/commit，用同一套 deploy 边界完成回滚。

剩余预期：在真实服务器上完成一次 `verify-deploy-hook` 记录和一次非破坏性回滚演练证据。

### 3. Shell 环境补齐

本地 Windows 环境没有 Bash 时，已接入 Git for Windows fallback。新增：

- `scripts/check-shell-scripts.js`
- `scripts/check-shell-scripts.test.js`

该 guard 会对 deploy、pre-deploy、hook verify、rollback、backup、restore、cron install、backup verify 脚本执行 `bash -n`。本地和架构门禁中都已通过，实际使用的是 `C:/Program Files/Git/bin/bash.exe`。

### 4. 仓库卫生与前端脚本清单

新增：

- `scripts/check-repository-hygiene.js`
- `scripts/check-frontend-script-manifest.js`

前者阻止 `.bak`、数据库、`.env`、密钥证书等文件进入 Git，并删除了已跟踪的 `backend/server.js.bak2`。

后者锁定 `frontend/index.html` 当前 193 个本地脚本、`?v=` 参数和关键加载顺序，作为未来迁移 bundler/content-hash 前的短期防线。

## 四、后端运行时与安全改动

### 1. 版本接口与更新体验

新增 `backend/routes/versionRoutes.js`，`/api/version` 支持：

- `ETag`
- `If-None-Match`
- 304 响应
- 部署 manifest metadata

`VersionService` 忽略本地 SQLite/log/runtime artifacts，避免本地 playtest 或运行态文件误触发版本变化。

前端 `GameAPI.getVersion()` 复用 ETag 缓存，`UpdateChecker` 添加失败退避和恢复逻辑。

### 2. API 请求稳定性

`frontend/js/api/GameAPI.js` 增加：

- 默认 10s timeout。
- request id。
- 结构化错误。
- GET 短重试。

`GameStateSync` 和 `UpdateChecker` 在失败后使用指数退避窗口，避免在线异常时持续打满接口。

### 3. 安全配置与管理员授权

新增 `backend/config/SecurityConfig.js`：

- production 要求 `JWT_SECRET`。
- production 要求 `CORS_ORIGINS`。
- development 保持本地可运行。

新增 `backend/middleware/adminMiddleware.js`：

- 登录认证与管理员授权分离。
- development 默认允许 `codexqa`。
- production 必须显式配置 `ADMIN_USERS`。

管理员配置发布和指标接口均走管理员权限边界。

## 五、观测、指标与客户端事件

新增 `backend/services/ObservabilityService.js`，当前是进程内滚动窗口，不是持久化 APM。它记录：

- 非跳过 API 路由的请求数、状态码、路径维度延迟。
- p95/max latency。
- slow request。
- 5xx server error。
- `/api/game/action` 和 task claim 的 action failure。
- frontend client events。
- 性能预算超限事件。

新增接口：

- `POST /api/client-events`
- `GET /api/metrics`

`H5LoadTrace` 通过 `GameAPI.reportClientEvent()` best-effort 上报 phase/API/资源加载失败。`/api/health` 增加 compact observability summary。

剩余预期：

- 持久化指标导出或 APM 接入。
- 真实告警投递。
- 告警演练。
- 跳过热路径的采样策略校准。

## 六、备份与恢复

新增：

- `scripts/backup-runtime-state.sh`
- `scripts/restore-runtime-state.sh`
- `scripts/install-runtime-backup-cron.sh`
- `scripts/verify-runtime-backup.sh`

当前覆盖：

- SQLite 存档 DB。
- shared config。
- deploy state。
- manifest。
- archive + checksum。
- retention pruning。
- 强确认恢复。
- cron 安装。
- 最近备份健康检查。

恢复脚本默认创建 pre-restore backup，且只有 `RESTORE_DEPLOY_STATE=1` 时才恢复 deploy state。

真实 host 证据：

- 已安装 root crontab：`17 3 * * * BACKUP_ROOT='/opt/wxgame-workspace/backups' RETENTION_DAYS='14' bash '/www/wwwroot/h5/scripts/backup-runtime-state.sh' >> '/opt/wxgame-workspace/.wxgame/backup.log' 2>&1 # WXGAME_RUNTIME_BACKUP`
- 已创建并验证备份：`/opt/wxgame-workspace/backups/wxgame-runtime-20260611T145310Z-drill.tar.gz`
- 已完成非生产恢复演练：`/opt/wxgame-workspace/restore-drill/20260611T145324Z`
- 演练恢复 DB size `415838208`，shared config version `2.3`，恢复前后生产 health 保持 OK。
- `CONFIG_RELEASE_GATE=required` 后的备份 `/opt/wxgame-workspace/backups/wxgame-runtime-20260611T152149Z-post-config-required.tar.gz` 已通过 `scripts/verify-runtime-backup.sh`，并确认包含 `./deploy-state/config-release/configReleases.json` 与 `./deploy-state/config-release/configActiveRelease.json`。

## 七、性能与容量

新增 `backend/services/PerformanceCapacityBudget.js`，覆盖：

- API/action latency。
- request/response body size。
- save size。
- total world-map tile count。
- mission count。
- world-map window/chunk size。

扩展 `frontend/js/domain/WorldMapPerformanceBudget.js`，覆盖：

- visible entries。
- actors。
- hit targets。
- frame pixels。
- active chunks。
- per-chunk entries。

`ObservabilityService` 会记录 `PERFORMANCE_BUDGET_EXCEEDED`；`GameStateRepository.save()` 写入 `saveMetadata.performanceCapacity`。

新增浏览器性能证据脚本：

- `scripts/profile-h5-performance.js`
- `npm run profile:h5-performance`
- `npm run profile:h5-phone-sim`

`npm run profile:h5-phone-sim` 使用 Chromium CPU throttling、移动 viewport/DPR/touch、navigator CPU/memory 注入、V8 heap cap 和 SwiftShader/low-end GPU flag 近似 2026 low/mid/flagship 手机。该模拟可作为无真机时的保守手段，但不等同真实手机热、驱动和浏览器差异。

最新本地模拟结果：

- `.local-logs/h5-performance/2026-06-11T09-23-29-025Z/profile.json`
- hard budgets 通过。
- low-end ready 14790ms。
- mid ready 8920ms。
- flagship ready 4175ms。
- 仍保留 RAF/long task warning，需要后续校准。

同时已优化启动归因：

- H5 启动资源加载只等待图片可用。
- 世界地图 tile metrics/mask/dry-template cache prewarm 改为 `worldMapRenderer.scheduleWorldTileCachePrewarm()` 后台分片。
- `H5LoadTrace` 记录 `assets:prewarm:deferred`。
- 低/中端移动设备通过 `CanvasGameShellWorldMapRuntimePolicy` 增加水面/探索刷新下限，降低 ready 后重绘压力。

## 八、配置发布与 GameplayConfigRuntime

本段是 P12-007 的核心结果。关键词：P12-007、GameplayConfigRuntime。

新增：

- `backend/services/config/ConfigPipeline.js`
- `scripts/validate-config-pipeline.js`
- `docs/config_registry_snapshot_2026-06-11.json`
- `backend/services/config/ConfigReleaseService.js`
- `backend/services/config/ConfigRuntimeLoader.js`
- `backend/services/config/GameplayConfigRuntime.js`
- `frontend/tools/config-release-console.html`
- `frontend/tools/config-release-console.test.js`

当前 registry 覆盖 7 个配置域：

- game-config
- era-config
- tutorial-flow-config
- battle-config
- tech-tree-config
- building-config
- task-definitions

`npm run config:validate` 和 architecture gate 会比较 `docs/config_registry_snapshot_2026-06-11.json`，拦截 schema/hash/entry/version drift 和缺失版本 bump 的配置变化。

`ConfigReleaseService` 提供：

- preview
- publish
- active release pointer
- rollback audit
- runtime drift status
- startup release gate policy

默认运行态文件：

- 生产：`/opt/wxgame-workspace/.wxgame/config-release/configReleases.json`
- 生产：`/opt/wxgame-workspace/.wxgame/config-release/configActiveRelease.json`
- 本地/测试：`data/config-release/configReleases.json`
- 本地/测试：`data/config-release/configActiveRelease.json`

`ConfigRuntimeLoader` 只在 active release 与当前 registry 匹配后构建只读 runtime bundle，并校验 payload hash。

`GameplayConfigRuntime` 是玩法侧配置读取入口，当前覆盖：

- `GameConfig`
- `BuildingConfig`
- `EraConfig`
- `TutorialFlowConfig`
- `TechTreeConfig`

生产 required 模式下，active runtime bundle 不 ready 会阻止 gameplay 初始化；development/test warn/off 模式保留 module fallback 用于本地引导和诊断。

已改为通过 `GameplayConfigRuntime` 消费配置的主要模块包括：

- building calculators/modules/domain/routes/validator。
- resource tick。
- era advancement。
- `GameStateService`。
- `ClientGameStateAssembler`。
- `BuildingUnlockService`。
- `MilitaryService`。
- `TaskCenterService`。
- `TutorialService`。
- task definition reward resolver。
- tutorial service 子模块。
- worldExplorer service 子模块。
- `TechTreeService`。

直接 import 原始 `backend/config/GameConfig|BuildingConfig|EraConfig|TutorialFlowConfig|TechTreeConfig` 的扫描结果现在只剩：

- `GameplayConfigRuntime` 自己的 fallback 边界。
- `ConfigRegistryContract.test.js` 的 registry contract 测试。

真实 host 证据：

- 已通过管理员 API 发布 release A `20260611T150609576Z-68505e575ff6-bb42d010` 和 release B `20260611T150609717Z-c084d0e8c91e-b227985c`。
- 已完成 B -> A rollback，回滚记录 `20260611T150910544Z-68505e575ff6-b7fd69a6`。
- 已再通过 rollback 记录恢复到 B，当前 active release 为 `20260611T150910561Z-c084d0e8c91e-7a01510b`。
- `CONFIG_RELEASE_GATE=required` 在生产 PM2 重启后通过健康检查：`configRuntime.status=matched`、`gatePolicy.mode=required`、`loader.ready=true`、`gameplay.source=active-release-bundle`。
- 未来新增配置域继续先接 `ConfigPipeline`，再通过 runtime facade 暴露给 gameplay。

## 九、世界地图、Canvas 与教程链路

本次补强了世界地图 layer ownership：

- `worldMap` 只负责 terrain/static/site/map-owned hit targets。
- `worldActor` 负责 actor drawing 和 actor hit targets。
- `mainHud` 负责 march command HUD。

新增 `frontend/js/platform/renderers/WorldMapLayerOwnershipContract.test.js` 并纳入 `npm run test:architecture`。

前端 H5 启动链路也有若干稳定性改动：

- `frontend/app.js` 加载调度相关依赖。
- `frontend/index.html` 加入必要脚本顺序。
- `CanvasAssetRenderer` 支持资源加载状态和预热拆分。
- `CanvasGameAppRenderScheduler` 复用 world-map runtime policy。
- `CanvasGameShellWorldMapRuntimePolicy` 根据移动档位降低刷新压力。

教程和世界探索链路已通过聚焦测试：

- `GameRoutesTutorial.test.js`
- `TutorialProgressService.test.js`
- `WorldExplorerService.test.js`
- `TerritoryActionTutorial.test.js`
- `GameStateServiceSplit.test.js`
- `TaskCenterService.test.js`

## 十、状态一致性与仓储

`GameStateRepository` 增加更强的状态保存边界：

- `saveAtomic()` 同事务写入主状态和共享世界派生占有记录。
- 新增 revision 乐观版本检查。
- `resetPlayerState()` 同事务清理玩家旧共享世界占有记录。
- 保存时写入 performance capacity metadata。

GET/read 路径继续收敛为只读 projection，不应推动在线进度或保存状态。

相关测试：

- `backend/tests/GameStateRepository.test.js`
- `backend/tests/GameStateProjectionArchitecture.test.js`
- `backend/tests/GameStateServiceSplit.test.js`

## 十一、文档同步

已同步更新：

- `docs/current_technical_architecture_2026-06-09.md`
- `docs/production_engineering_roadmap_2026-06-09.md`
- `docs/long_term_architecture_refactor_plan_2026-06-08.md`
- `docs/architecture_module_responsibility_index_2026-06-08.md`
- `backend/README.md`
- `scripts/verify-refactor-plan-doc.js`

本文档取代旧阶段性问题/重构/交接文档，并纳入官方文档集合。

## 十二、验证记录

本地验证：

```powershell
npm run test:architecture
```

结果：

- 613 tests
- 613 pass
- 0 fail
- stable-blocks passed
- repository-hygiene passed
- frontend-script-manifest passed
- shell-scripts passed
- config-pipeline baseline changed=0 added=0 removed=0
- official-docs passed
- `git diff --check` 仅有 Windows CRLF warning，不是失败

聚焦验证：

```powershell
node --test backend/tests/GameplayConfigRuntime.test.js backend/tests/ConfigRuntimeLoader.test.js backend/tests/ConfigReleaseService.test.js backend/tests/ConfigPipeline.test.js
```

结果：19 tests 全部通过。

```powershell
node --test backend/tests/GameStateServiceSplit.test.js backend/tests/GameRoutesTutorial.test.js backend/tests/TaskCenterService.test.js backend/tests/TutorialProgressService.test.js backend/tests/WorldExplorerService.test.js backend/tests/TerritoryActionTutorial.test.js
```

结果：48 tests 全部通过。

配置 baseline 验证：

```powershell
node scripts/validate-config-pipeline.js --baseline docs/config_registry_snapshot_2026-06-11.json
```

结果：

- registries: 7
- changed=0
- added=0
- removed=0

编码风险扫描：

```powershell
python C:/Users/Administrator/.codex/skills/encoding-safe-review/scripts/scan_encoding_risks.py docs/...
```

结果：`No encoding risks found.`

最新本地与线上验证：

- 本地 `main`、GitHub `origin/main`、服务器 `private/main` 已同步到 `08639bab086d5d87ebb7445a043ffb72cc88754c`。
- 服务器 deploy hook 已完成部署，`/api/health` 返回 `status=ok`。
- 部署后 `configRuntime.status=matched`，`configRuntime.gatePolicy.mode=required`，`configRuntime.loader.ready=true`，`configRuntime.gameplay.source=active-release-bundle`。
- 服务器 architecture gate 当前为 613/613 pass。

服务器 hook 注意事项：

- `08725064` 首次推送时，`origin` post-receive/deploy hook 接受提交并运行 architecture gate。
- 当时远端输出出现 `server hook anomaly`: `tests 590 / pass 586 / fail 4`。
- 该 hook 没有阻断 push。
- 当时本地同一提交 architecture gate 为 609/609 pass。
- 当时需要查看服务器部署日志和远端 test output，定位缺失 19 个测试与 4 个失败测试是否来自服务器依赖、Node 版本、文件同步、环境变量或生产 required gate 差异。
- 2026-06-11 续查结论：远端失败由 `/www/wwwroot/h5` 的 gate 环境缺少 root/backend `node_modules` 引起，具体缺 `xlsx` 与 `better-sqlite3`；手动安装依赖后服务器 `npm run test:architecture` 恢复通过。`scripts/pre-deploy-gate.sh` 已改为默认 auto 模式，缺依赖或 lockfile hash 变化时自动 `npm ci`。后续部署在 `08639bab086d5d87ebb7445a043ffb72cc88754c` 已确认服务器 architecture gate 613/613 pass。

## 十三、下一步建议

优先级 1：推进 P12-008 stable promotion CI。

当前 P0-P11 当前范围已完成，P12 的主干标准化能力已经覆盖 CI/deploy gate、deploy identity、health、rollback entry、backup/restore drill、config release audit、active pointer、required gate 和 runtime bundle。下一步可以把 candidate -> stable promotion 的机器检查继续补齐，减少“文档说稳定、代码未真正稳定”的风险。

优先级 2：继续 P12-003/P12-009 运维标准化。

- 把当前进程内 metrics 接到持久化导出或 APM。
- 做真实告警投递和告警演练。
- 汇总 deploy、rollback、health、backup/restore、config publish/rollback、emergency gate switch 到简明 operations runbook。

优先级 3：继续 P12-005 性能警告收敛。

重点是 simulated phone profile 中的 RAF p95 与 long task warnings。当前 hard budgets 通过，但 warning 仍需要校准阈值、拆分首屏重任务或引入更细粒度采样。

优先级 4：P12-006 security/access control。

重点是管理员发布链路、部署凭据、服务器登录、secret rotation 和依赖安全。Node 版本风险已在 2026-06-11 续更中收敛：生产 Node 为 `20.20.2`，`backend/package.json` 要求 `node >=20.0.0`，`better-sqlite3@12.10.0` 已按 Node 20 重建。当前残余风险转为 `npm audit` 仍有 3 个漏洞（2 moderate，1 high），以及运维硬停/开服还需要独立 `ops-agent` 或主机控制面。

## 十四、接手注意事项

- 不要再让 gameplay 代码直接 import 原始核心配置模块；应使用 `GameplayConfigRuntime`。
- 不要把 admin config publish 写成热加载 gameplay runtime；admin 只负责审计和 active pointer。
- 不要把新的运行时 `.bak`、DB、`.env`、密钥证书提交进仓库；repository hygiene guard 会拦截。
- 中文文档继续按 UTF-8 处理；如出现乱码风险，先跑 encoding scanner，不要用 PowerShell `Set-Content` 大范围重写中文文档。
- 改 Canvas 真实玩家输入、hit target、教程高亮、部署路径、在线资源加载时，需要补浏览器或 playtest 证据。
- 继续提交前至少跑 `npm run test:architecture`；涉及 config 内容变化时同步更新 registry version 或 baseline。
