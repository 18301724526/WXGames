# 20260527 分层立绘 2.0

## 当前结论

名人分层立绘已经切回正确方向：PNG 位图美术资源负责画面，代码只负责资源处理和运行时拼接。

不要再走“用 JS/Canvas 程序画立绘资源”的路线。上一版错误点就是资源看起来像矢量/程序图，无法作为正式美术基准继续扩展。

当前功能提交：

- `dcc0e1e Replace famous portrait art01 PNG pipeline`
- `6167919 Document portrait hair base issue`

当前公网实验台入口：

- `https://www.kodagame.top/tools/famous-portrait-lab.html`

## 当前资源状态

当前只保留五层锚定方案实际会用到的 `art01` 资源，用来作为第一套可手调基准。

正式分层 PNG：

- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-outfitBack-guardian-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-head-base-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-hairBase-bound-topknot-filled-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-bangs-bound-topknot-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-bangs-bound-topknot-short-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-bangs-bound-topknot-parted-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-bangs-bound-topknot-swept-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-art01-outfitFront-guardian-01.png`
- `frontend/assets/art/famous-person/layers/fp-layer-v2-manifest.json`

旧七层资源、完整束发试验图和旧 source sheet 已删除，避免检查素材和后续生成时混淆。

## 分层定义

当前层顺序：

```text
outfitBack -> head -> hairBase -> bangs -> outfitFront
```

层含义：

- `outfitBack`：衣服后层。
- `head`：脸、头、脖子和基础身体。
- `hairBase`：发型底层，负责刘海下方自然发根和后侧发量。
- `bangs`：可切换刘海变体。
- `outfitFront`：衣服前层。

## 代码边界

允许代码做的事：

- 在实验台和游戏里按 manifest + transform 拼层。
- 后续新资源应直接以透明 PNG 分层产出，并按实际透明边界裁掉多余空白。
- 写出 `fp-layer-v2-manifest.json`。

不允许代码做的事：

- 用 JS/Canvas/矢量方式画正式立绘资源。
- 恢复 `scripts/generate-famous-portrait-v2-assets.js`。
- 恢复旧的 `frontCutY`、`backCutY`、split/crop 逻辑。

旧七层 source sheet 提取脚本和本地七层合成预览脚本已经删除，不要再用它们生成旧资源。

## 实验台和游戏同步点

实验台：

- `frontend/tools/famous-portrait-lab.html`
- `frontend/tools/famous-portrait-lab.js`

游戏共用布局：

- `frontend/js/config/FamousPortraitLayout.js`

后端名人生成人物外观：

- `backend/services/FamousPersonService.js`

关键点：实验台和游戏必须使用同一套坐标模型。实验台从 `fp-layer-v2-manifest.json` 读 base 坐标，游戏从 `FamousPortraitLayout.js` 读 base 坐标，所以更新资源后必须把 manifest 里的 base 同步到 `FamousPortraitLayout.js`。

当前 `APPEARANCE_VERSION` 是：

```text
famous-portrait-v2.1
```

这会让旧存档里的 v1.0/v0.9/v0.2/unversioned 名人在 normalize 时刷新到 `art01` 新资源。

## 下一步怎么继续

1. 回家后打开公网实验台。
2. 在实验台里手动微调 `global`、层级顺序、每层 `scale/x/y/visible`。
3. 调到游戏实显区域满意后，复制实验台导出的 JSON。
4. 把导出 JSON 的 `global`、`order`、`layers` transform 同步到：
   - `frontend/tools/famous-portrait-lab.js`
   - `frontend/js/config/FamousPortraitLayout.js`
5. 如果要让已有人物强制刷新，继续提升 `backend/services/FamousPersonService.js` 里的 `APPEARANCE_VERSION`。
6. 跑测试，提交并推双远端。

## 当前默认参数说明

现在默认参数不是最终美术定稿，只是为了让实验台打开后不再出现“衣服盖脸、4x2 sheet 坐标错位、游戏和实验台各算各的”。

当前默认拼接已经满足：

- 层级使用五层锚定结构。
- 游戏和实验台使用同一套分层文件。
- 游戏和实验台使用同一套绘制顺序。
- 后端生成和旧人物刷新都会指向 `art01` 新资源。

但美术上仍需要人工继续调：

- `hairBase` 目前只是临时补洞底图，美术结构不合格，需要优先重做。
- 刘海位置、大小和变体质量。
- 头部与盔甲肩线。
- 后续其他衣服、发型必须以这套规格为基准重新产出。

## 验证命令

核心测试：

```bash
node --test frontend/tests/famous-portrait-lab.test.js backend/tests/famous-person-service.test.js frontend/tests/shared-canvas-renderer.test.js frontend/tests/ui-state-presenter.test.js
```

入口和资源测试：

```bash
node --test frontend/tests/h5-canvas-runtime.test.js frontend/tests/resource-art.test.js frontend/tests/version-number.test.js frontend/tests/stage5-version.test.js
```

空白检查：

```bash
git diff --check
```

上一次已验证结果：

- 核心测试 `115/115` 通过。
- H5/资源/版本测试 `64/64` 通过。
- `git diff --check` 通过，仅有 Windows 换行提示。

## 双远端推送

项目要求推两个远端：

```bash
git push origin main
git push github main
```

如果 GitHub DNS 解析失败，使用固定 IP SSH：

```powershell
$env:GIT_SSH_COMMAND='ssh -o HostName=140.82.113.3 -o HostKeyAlias=github.com -o BatchMode=yes'
git push github main
```

推送后核对：

```bash
git rev-parse main
git ls-remote origin refs/heads/main
git ls-remote github refs/heads/main
```

## 不要提交的本地文件

这些是本地临时文件，不要带进提交：

- `temp_test.js`
- `temp_test2.js`
- `tmp/`
