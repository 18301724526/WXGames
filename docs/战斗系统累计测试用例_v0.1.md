# 战斗系统累计测试用例 v0.1

日期：2026-05-29

## 用途

本文用于把战斗系统每批实现后的人工测试点累计到一个地方。你可以暂时不测，后续有时间时按本文从上到下集中验收。

## 当前覆盖版本

| 版本 | 提交内容 | 是否需要人工当场测试 |
| --- | --- | --- |
| `0.1.151` | 正式战斗内核 v2：速度排序、条件释放、冷却按自身行动推进、战前被动属性修正。 | 可延后 |
| `0.1.152` | 技能生成器接口收口：seed/source/效果池/预算检查/技能描述写入 `abilityKit`。 | 可延后 |
| `0.1.153` | 名人技能面板展示：卡片展示主动战法、被动、内政技能和斥候特质详情。 | 可延后 |
| `0.1.154` | 旧技能效果迁移：旧概率效果收口到第一批确定性效果原子。 | 可延后 |
| `0.1.155` | 旧已生成名人升级：已加入名人和候选名人自动补齐当前完整 `abilityKit`。 | 可延后 |
| `0.1.156` | 名人卡片展示 v2：两条技能徽章、悬停/点击详情、右侧六维蛛网图，并移除名人属性中的工巧口径。 | 可延后 |

## 前置条件

- 使用服务器正式页面和有账号数据的账号测试。
- 账号最好已经进入古典时代，能打开世界/军事地图并派兵出征。
- 至少拥有或可寻访到几名名人：军事型、内政/魅力型、斥候型各一名最好。

## 用例 1：有主地点进入自动战斗

步骤：

1. 登录服务器账号。
2. 进入世界/军事地图。
3. 选择一个有主地点。
4. 选择一名军事型名人领队，配置兵力后出征。
5. 等行军完成，进入或查看战斗结算。

预期：

- 有主地点不再只按“兵力大于防御”硬判断直接结算。
- 战斗会生成结构化战报和战斗场景播放数据。
- 战斗结束后地点、己方兵力、战报结果同步更新。

## 用例 2：速度决定先后手

步骤：

1. 派速度较高的名人打一场有主地点战斗。
2. 再派速度较低的名人打一场相近战斗。
3. 对比战斗首轮行动顺序。

预期：

- 速度高的一方优先行动。
- 同速时当前规则默认己方先行动。
- 速度只决定每轮先后手，不出现追击、额外行动或行动条。

## 用例 3：技能释放和冷却

步骤：

1. 使用有主动战法的军事型名人进入战斗。
2. 观察技能名和立绘 cut-in。
3. 继续观察后续几次自身行动。

预期：

- 技能只在自身回合、冷却为 `0` 且释放条件满足时释放。
- 冷却未结束时执行普攻。
- 冷却结束但释放条件不满足时也执行普攻，下次自身行动重新判断。
- 技能 cut-in 只出现在技能行动，普攻不显示技能名和名人立绘。

## 用例 4：技能 cut-in 表现

步骤：

1. 使用会释放主动战法的名人进入战斗场景。
2. 观察释放技能瞬间。

预期：

- 战斗普通节奏保持较快。
- 技能释放前会暂停战斗时间播放表现。
- 名人立绘从左侧进入，技能名从右侧进入，中间短暂停留后飞出。
- 玩家能看清立绘和技能名。

## 用例 5：内政/魅力型名人上战场

步骤：

1. 使用内政型或魅力型名人作为领队。
2. 派其进入有主地点战斗。
3. 观察战斗行动。

预期：

- 内政/魅力型名人不释放战斗技能。
- 系统不会给它们注入 fallback 战斗技能。
- 战斗中只执行普攻循环。
- 名人自身仍可在名人数据中保留两个内政技能，当前阶段只展示不生效。

## 用例 6：斥候型名人

步骤：

1. 使用斥候型名人作为领队。
2. 派其进入有主地点战斗。
3. 观察首轮和后续行动。

预期：

- 斥候拥有轻战斗主动战法。
- 若技能带 `firstOwnAction` 条件，只在第一次自身行动时释放。
- 斥候的侦查/行军特质当前只作为存档和展示能力，不影响侦查实际收益。

## 用例 7：寻访生成技能结构

步骤：

1. 在名人系统中连续寻访几次。
2. 分别查看军事型、内政/魅力型、斥候型候选。

预期：

- 军事型候选显示主动战法和战斗被动特质。
- 内政/魅力型候选显示两个内政技能，不显示战斗技能。
- 斥候候选显示一个轻战斗主动战法和一个斥候特质。
- 同一候选被接纳后，技能、立绘和来源信息保持不变。

## 用例 8：名人技能面板展示

步骤：

1. 打开名人面板。
2. 查看已加入名人与候选名人的卡片。
3. 分别观察军事型、内政/魅力型、斥候型名人的技能徽章。
4. 将鼠标移到技能徽章上，或在触屏设备点击技能徽章。
5. 查看右侧六维蛛网图。

预期：

- 军事型名人卡片只常驻显示 `主动战法：技能名` 和 `战斗被动：技能名`。
- 主动战法的冷却和释放条件，例如 `冷却3次自身行动`、`冷却就绪 / 目标存活`，只在悬停/点击详情中显示。
- 内政/魅力型名人卡片只常驻显示 `内政主技：技能名` 和 `内政副技：技能名`。
- 当前未接入实际生效入口的内政或斥候特质在详情中显示 `当前仅展示`。
- 六维蛛网图固定展示统帅、武力、智力、政治、魅力、速度，速度必须出现。
- 名人卡片和蛛网图不出现 `工巧`。
- 卡片文字不挤出边界，不遮挡接纳/放弃按钮。

## 用例 9：旧名人技能兼容

步骤：

1. 使用已有老账号或老存档登录。
2. 打开名人面板，查看旧版本生成过的名人技能。
3. 选择旧名人进入有主地点战斗。

预期：

- 面板不再出现 `combo`、`ambush`、`morale`、`counter` 这类旧英文效果。
- 旧连击口径表现为 `二段伤害`。
- 旧伏击口径表现为 `先手`。
- 旧士气增强口径表现为 `属性修正`。
- 旧反击口径当前不会显示为已生效技能效果。
- 战斗仍能正常播放和结算。

## 用例 10：旧名人完整技能机制升级

步骤：

1. 使用已有老账号或老存档登录。
2. 打开名人面板，分别查看旧版本已加入名人和仍在候选列表中的名人。
3. 对比军事型、内政/魅力型、斥候型的技能行。
4. 选择旧军事型或旧斥候型名人进入有主地点战斗。

预期：

- 旧军事型名人显示 `主动战法` 和 `战斗被动`，原主动战法尽量保留并迁移效果。
- 旧内政/魅力型名人显示 `内政主技` 和 `内政副技`，不显示战斗主动技能，上战场只普攻。
- 旧斥候型名人显示 `主动战法` 和 `斥候特质`。
- 旧候选名人与旧已加入名人都能自动升级，不需要重新寻访或手动修档。
- 技能详情仍有冷却、触发或 `当前仅展示` 等元信息，卡片显示不空白。

## 自动化测试记录

- `0.1.151`：`npm.cmd test` 通过 `517/517`。
- `0.1.152`：目标测试通过 `97/97`；`npm.cmd test` 通过 `518/518`。
- `0.1.153`：目标测试通过 `121/121`；`npm.cmd test` 通过 `518/518`。
- `0.1.154`：目标测试通过 `74/74`；`npm.cmd test` 通过 `519/519`。
- `0.1.155`：目标测试通过 `30/30`；`npm.cmd test` 通过 `521/521`。
- `0.1.156`：目标测试通过 `137/137`；`npm.cmd test` 通过 `522/522`。
