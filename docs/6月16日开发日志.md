# June 16 Development Log

## Current State

- Workspace: `F:\AI Project\WXGamesLocal`
- Branch: `codex/real-world-spawn-step1`
- Last deployed baseline before this entry: `8f623fe57a094a12bf85da83785ec6d532f4732d`
- Public H5: `http://47.116.32.216/wxgame/`
- API base: `http://47.116.32.216:3000/api`
- QA account used for browser tests: `codexqa / 123456`

## Latest Handoff Snapshot

- Latest pushed commit before this snapshot: `145762f7 docs add latest handoff snapshots`.
- Current pushed branch: `codex/real-world-spawn-step1` on both `origin` and `private`.
- Status rule:
  - Earlier mid-document `Required Next Verification` and `Current Manual Test Objective` blocks are historical unless superseded by this snapshot or a later numbered step.
- Latest in-app-browser public-H5 reachability proof:
  `tmp/verification/iab-public-h5-user-demand/2026-06-16T03-37-58-389Z/`
- Latest reset-spawn public-H5 proof:
  `tmp/verification/user-demand-reset-spawn-visible/2026-06-16T03-38-18-163Z/`
- Previous public-H5 reachability proof:
  `tmp/verification/public-h5-current-doc-audit/2026-06-16T03-29-58-463Z/`
- Latest post-reset tutorial completion proof:
  `tmp/verification/user-demand-post-reset-tutorial-complete/2026-06-16T03-40-21-226Z/`
- Previous full gameplay proof:
  `tmp/verification/online-post-reset-tutorial-smoke/2026-06-16T03-20-54-068Z/`
- Latest gameplay result:
  - public H5/API
  - reset-spawn moved `codexqa` from `tile_-8_-25` to `tile_23_18`
  - reset-spawn visible tile count = `25`
  - old owned outside city `site_-6_-23` was released for `codexqa`
  - tutorial closure evidence continued from the same reset account after a short max-action run stopped at step `16`
  - `stopReason = tutorial-completed`
  - `finalStep = 36`
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`
- Current manual target:
  - Do not keep retesting the same `codexqa` reset/tutorial path unless product code changes.
  - Next narrow target should be legacy-account audit: identify whether any older live accounts still carry the old `(0,0)` origin/capital state, then design a separate migration/repair step before rewriting data.
  - If browser evidence is needed for that target, use the public H5 and a known affected account; do not use a local temporary server.
- Evidence boundary:
  - Reset-spawn and post-reset tutorial closure have focused public-H5 proof for `codexqa`.
  - Migration/repair of old live accounts born at `(0,0)` remains unproven and should be a separate step.

## Completed Before This Entry

- Fixed world-map camera recentering after panels/account reset to use render-space origin.
- Verified the deployed H5 could complete the guided tutorial once on commit `8f623fe57a094a12bf85da83785ec6d532f4732d`.
- Documented the remaining Step 17 target: free long-distance manual march and active return-home.

## New Online Blocker Found

- While preparing Step 17 verification, the real online H5 tutorial rerun failed before reaching free manual march.
- Evidence path: `tmp/verification/online-step17-rerun-tutorial/2026-06-15T23-53-52-487Z/`.
- Failure point: tutorial step 25, first-city conquest.
- Frontend clicked the correct target and sent `action = startConquest`, `territoryId = site_3_-25`.
- API returned HTTP 500.
- Dev-server PM2 logs under user `www` showed `Error: Game state revision conflict` from `GameStateRepository.save`.
- Diagnosis: gateway `/api/game/action` and `wxgame-world-worker` can save the same player concurrently. This blocks tutorial completion but is not a frontend map rendering proof.

## Narrow Fix Prepared

- `backend/routes/gameRoutes.js`
  - Extracted game-action execution into a retryable helper.
  - On `GAME_STATE_REVISION_CONFLICT`, reloads latest player state and retries the same action once.
  - If the retry also conflicts, returns HTTP 409 with `retryable: true` instead of HTTP 500.
- `backend/tests/GameRoutesTutorial.test.js`
  - Added a route test that simulates the worker saving a newer revision first.
  - Expected result: route retries once and the action succeeds.

## Local Verification

- `node --check backend/routes/gameRoutes.js`
- `node --test backend/tests/GameRoutesTutorial.test.js backend/tests/GameStateRepository.test.js backend/tests/WorldWorkerService.test.js`
- Result: 41 pass.
- `git diff --check`
- Result: only CRLF working-copy warnings.

## Required Next Verification

- Commit `402048df285641293c513bf9b739bb46f216a08a` was deployed to GitHub branch `codex/real-world-spawn-step1` and development server `main`.
- Remote pre-deploy architecture gate passed: 925 pass.
- Public `/api/version` confirmed `deployedCommit = 402048df285641293c513bf9b739bb46f216a08a`.
- Real online tutorial rerun then failed earlier than step 25: first `POST /api/player/login` returned HTTP 500.
- PM2 server logs under user `www` showed `GAME_STATE_REVISION_CONFLICT` from `backend/routes/playerRoutes.js` during `AuthService.loginPlayer`.

## Second Narrow Fix Prepared

- `backend/routes/playerRoutes.js`
  - Login now retries once when saving the player state hits `GAME_STATE_REVISION_CONFLICT`.
  - If the retry also conflicts, the route returns HTTP 409 with `retryable: true` instead of HTTP 500.
- `backend/tests/GameStateProjectionArchitecture.test.js`
  - Added `login route retries once when worker saves a newer revision first`.

## Second Local Verification

- `node --check backend/routes/playerRoutes.js`
- `node --test backend/tests/GameStateProjectionArchitecture.test.js backend/tests/AuthServiceBotAccounts.test.js backend/tests/GameRoutesTutorial.test.js`
- Result: 29 pass.
- `npm.cmd run test:architecture`
- Result: 926 pass.

## Next Required Verification

- Commit `e5e39b9d0c49cb5077cb29d390b74b8debb182b2` was deployed to GitHub branch `codex/real-world-spawn-step1` and development server `main`.
- Remote pre-deploy architecture gate passed: 926 pass.
- Public `/api/version` confirmed `deployedCommit = e5e39b9d0c49cb5077cb29d390b74b8debb182b2`.
- Real online H5 tutorial automation passed from reset/login to completion.
- Evidence path:
  `tmp/verification/online-login-action-revision-retry/2026-06-16T00-17-51-677Z/`
- Result:
  `stopReason = tutorial-completed`, `finalStep = 36`, `actionCount = 51`, `evidenceCount = 49`, `verificationReportCount = 53`, `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verificationFailures = 0`, `visualFindings = 0`.
- Key screenshots:
  - `highlight-conquer-43-before-full.png`
  - `highlight-conquer-43-after-step-26-full.png`
  - `highlight-claimConquest-44-after-step-27-full.png`
  - `close-advisor-step-35-after-step-36-full.png`
- In-app browser public-H5 screenshot:
  `tmp/verification/in-app-public-e5e39b9d/public-h5-current.png`

## Next Manual Test Objective

- Only test Step 17 free long-distance manual march and active return-home.
- Use the deployed public H5 at `http://47.116.32.216/wxgame/`.
- Use an account after tutorial completion with a saved formation.
- Start a longer manual march from the capital, click return-home while the actor is still moving, and observe only this route.
- Pass condition: actor does not skip the final outbound tile, return starts from the current backend tile instead of flashing backward, route preview stays visible, and actor reaches the capital/home tile without stopping one tile early or snapping early.

## Step 17C Online Manual March Verification

- Added repeatable command: `npm.cmd run playtest:online-manual-march`.
- The command uses the public H5/API, real canvas clicks, screenshots, and JSON state snapshots.
- First calibration evidence:
  `tmp/verification/online-manual-march-return/2026-06-16T00-34-02-581Z/`
  - Failed before opening the formation picker because the harness clicked canvas-local coordinates as page coordinates.
  - This was a harness issue, not product evidence.
- Second calibration evidence:
  `tmp/verification/online-manual-march-return/2026-06-16T00-35-54-702Z/`
  - Real canvas flow opened the picker and started a march.
  - Route was only 1 step because formation slot 1 was already parked outside the capital from prior testing.
  - Harness now chooses the target relative to the current formation position.
- Passing evidence:
  `tmp/verification/online-manual-march-return/2026-06-16T00-38-20-015Z/`
- Passing deployed version:
  `deployedCommit = 556f3abe8d610c8f13e7468b16e86e09e4cbebcf`
- Passing result:
  `routeCount = 5`, target `tile_26_7`, active return clicked at `tile_27_8`, first return frame stayed `tile_27_8`, final home/capital `tile_28_9`, `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Key screenshots:
  - `05-before-return-click-full.png`
  - `07-return-command-visible-full.png`
  - `08-click-return-home-after-full.png`
  - `10-first-return-frame-full.png`
  - `11-final-return-home-full.png`
- Boundary:
  - Active return-home long-route behavior passed for the tested route.
  - No-return outbound final-step snapping is not independently proven by this run; if it recurs, make it the next separate target.

## Next Test Objective After Step 17C

- Step 17D below is now the current no-return outbound final-step evidence.

## Step 17D Online Manual March Complete Verification

- Added repeatable command: `npm.cmd run playtest:online-manual-march-complete`.
- The command uses the public H5 at `http://47.116.32.216/wxgame/`, public API `http://47.116.32.216:3000/api`, QA account `codexqa / 123456`, real browser canvas clicks, screenshots, and JSON snapshots. It does not use a local temporary server.
- Pre-fix failure evidence:
  `tmp/verification/online-manual-march-complete/2026-06-16T00-56-43-644Z/`
  - `routeCount = 6`, target `tile_26_7`.
  - Failed with `active actor disappeared during final route phase for 2 samples`.
  - Backend still had the mission active at `tile_27_8`, while frontend actor hit targets were missing.
- Fix deployed:
  - commit `141440eb2c37b2c9a206042e66bff36af3b53e63`
  - public `/api/version` confirmed `deployedCommit = 141440eb2c37b2c9a206042e66bff36af3b53e63`
  - `deployedAt = 2026-06-16T01:05:31Z`
- First passing evidence after fix:
  `tmp/verification/online-manual-march-complete-after-fix/2026-06-16T01-06-36-051Z/`
  - `routeCount = 7`, target `tile_33_14`.
  - `activeActorDisappearances = []`.
  - Saw penultimate and final route indices.
  - Final position reached `tile_33_14`.
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`.
- User-complaint rerun evidence:
  `tmp/verification/online-manual-march-complete-rerun-user-complaint/2026-06-16T01-09-59-784Z/`
  - `routeCount = 8`.
  - start/current formation origin `tile_33_14`.
  - selected target `tile_25_6`.
  - pre-final position `tile_26_7`.
  - final position `tile_25_6`.
  - final sampled route indices `[6, 6, 6, 7]`.
  - `activeActorDisappearances = []`.
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Key screenshots:
  - `05-before-final-route-steps-full.png`
  - `complete-final-phase-step-06-1-full.png`
  - `complete-final-phase-step-06-2-full.png`
  - `complete-final-phase-step-06-3-full.png`
  - `complete-final-phase-step-07-4-full.png`
  - `06-final-route-complete-full.png`
- Local verification after the deployed fix:
  - `node --test frontend/js/domain/WorldActorProjection.test.js frontend/js/domain/WorldMarchProgressSnapshot.test.js`
  - `node --test frontend/js/platform/renderers/WorldMapLayerCanvasRenderer.test.js frontend/js/platform/renderers/WorldMapActorHudRenderer.test.js`
  - `npm.cmd run test:architecture`
  - Result: 928 pass.

## Historical Manual Test Objective After Step 17D

- Test only long-route return-home plus known-tile visual stability.
- Use deployed H5 at `http://47.116.32.216/wxgame/`.
- Start one visibly longer free manual march, ideally at least 4 route steps.
- Click return-home while the actor is on a non-city/non-site tile.
- Watch only two things:
  - the same already-visible terrain/edge/transition/resource/city visuals do not change while leaving and returning;
  - the actor starts return from its current tile and ends on the capital.
- Ignore route-ahead six-neighbor completeness in this pass; Step 17F exposed one missing neighbor and that is the next separate target.
- Status note:
  - This objective was superseded by Step 17F, Step 17G, Step 18, Step 19, and Step 20 evidence below.
  - The latest manual target is the Step 20 post-reset tutorial closure check, not this historical Step 17D follow-up.

## Step 17E Online Known Tile Visual Template Stability Verification

- Added repeatable command: `npm.cmd run playtest:online-manual-march-templates`.
- The command uses the public H5 at `http://47.116.32.216/wxgame/`, public API `http://47.116.32.216:3000/api`, QA account `codexqa / 123456`, real browser canvas clicks, screenshots, and JSON snapshots. It does not use a local temporary server.
- The test captures actual frontend render-context tile signatures and compares known rendered tiles across the march flow.
- Calibration evidence:
  `tmp/verification/online-manual-march-templates-short/2026-06-16T01-39-11-670Z/`
  - Failed because the first harness version included `renderReady` in the visual signature.
  - The changed field was state-only render lifecycle data, not tile visual identity.
- Passing evidence:
  `tmp/verification/online-manual-march-templates-visual/2026-06-16T01-41-59-372Z/`
  - `routeCount = 1`, selected target `tile_29_9`.
  - `knownTileChangeCount = 0`.
  - `stateOnlyChangeCount = 9`, all from `renderReady` changing.
  - route render-ahead coverage for `tile_29_9`: target rendered and all 6 neighboring tiles rendered.
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Key screenshots:
  - `04-active-mission-started-full.png`
  - `05-template-short-route-complete-full.png`
  - `iab-public-h5-visible.png`
- Result:
  - Known rendered tile visual signatures stayed stable for the tested online flow.
  - The harness now separates actual visual changes from render lifecycle noise.

## Step 17F Online Long Route Return With Known Tile Visual Stability

- Improved repeatable command: `npm.cmd run playtest:online-manual-march-templates`.
- Harness fixes:
  - target selection now records and filters by estimated `routeStepCount`, not only hex distance;
  - return-home actor clicks now avoid known site tiles when choosing the route step for the return command.
- Calibration evidence:
  `tmp/verification/online-manual-march-templates-long-return/2026-06-16T01-47-34-484Z/`
  - selected target had `distance = 4`, but actual backend route only had `routeCount = 2`;
  - `knownTileChangeCount = 0`;
  - this proved hex distance was not enough for long-route evidence.
- Calibration evidence:
  `tmp/verification/online-manual-march-templates-long-return-routecount/2026-06-16T01-49-56-804Z/`
  - selected target had `routeStepCount = 4`;
  - return click was attempted while the actor overlapped capital tile `tile_28_9`;
  - screenshot `06-select-active-actor-after-full.png` showed the city/capital panel opened instead of the troop command panel;
  - this was a harness click-target issue.
- Passing evidence:
  `tmp/verification/online-manual-march-templates-long-return-safe-step/2026-06-16T01-54-21-135Z/`
  - `routeCount = 4`, selected target `tile_28_7`, `routeStepCount = 4`;
  - return clicked from `tile_28_8`;
  - first return frame stayed at `tile_28_8`;
  - final return position `tile_28_9` matched home/capital;
  - `knownTileChangeCount = 0`;
  - `stateOnlyChangeCount = 25`, all render lifecycle noise such as `renderReady`;
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Key screenshots:
  - `05-before-return-click-full.png`
  - `07-return-command-visible-full.png`
  - `08-click-return-home-after-full.png`
  - `10-first-return-frame-full.png`
  - `11-final-return-home-full.png`
- Result:
  - Long-route return-home plus known-tile visual stability passed for the tested deployed public-H5 flow.
  - New separate verifier risk exposed: route render-ahead six-neighbor coverage was not sampled at every route-index transition, so a suspected missing neighbor could not yet be classified as a product bug.

## Step 17G Online Before-Entry Render Coverage Verification

- Updated repeatable command: `npm.cmd run playtest:online-manual-march-templates`.
- Harness refinement:
  - every `routeIndex` transition now captures a full render snapshot such as `outbound-step-00-render-full.png`;
  - summary output now includes `routeBeforeEntryCoverage` and `missingBeforeEntryCoverage`;
  - the pass condition checks the frame before actor entry, not only sparse start/end snapshots.
- Calibration evidence:
  `tmp/verification/online-manual-march-templates-before-entry-coverage/2026-06-16T02-06-55-163Z/`
  - reported missing neighbor coverage, but the evidence was still based on sparse snapshots;
  - result was not strong enough to prove a product bug.
- Passing evidence:
  `tmp/verification/online-manual-march-templates-before-entry-route-snapshots/2026-06-16T02-11-40-255Z/`
  - `routeCount = 4`, selected target `tile_24_7`;
  - `knownTileChangeCount = 0`;
  - `missingBeforeEntryCoverage = []`;
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Fresh visible-browser retest evidence:
  `tmp/verification/online-manual-march-templates-before-entry-visible-retest/2026-06-16T02-16-15-396Z/`
  - public URL confirmed by screenshot `iab-public-h5-after-retest.png`: `http://47.116.32.216/wxgame/`;
  - page title: `文明火种`;
  - `routeCount = 4`, selected target `tile_24_7`;
  - final return position `tile_28_9` matched home/capital;
  - `knownTileChangeCount = 0`;
  - `missingBeforeEntryCoverage = []`;
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Before-entry coverage from the fresh retest:
  - step 0 `tile_27_8`: before `04-active-mission-started`, self + 6 neighbors rendered;
  - step 1 `tile_26_7`: before `outbound-step-00-render`, self + 6 neighbors rendered;
  - step 2 `tile_25_7`: before `outbound-step-01-render`, self + 6 neighbors rendered;
  - step 3 `tile_24_7`: before `outbound-step-02-render`, self + 6 neighbors rendered.
- Key screenshots:
  - `outbound-step-00-render-full.png`
  - `outbound-step-01-render-full.png`
  - `outbound-step-02-render-full.png`
  - `05-before-return-click-full.png`
  - `07-return-command-visible-full.png`
  - `11-final-return-home-full.png`
  - `iab-public-h5-after-retest.png`
- Result:
  - Before-entry render coverage passed on deployed public H5 for the tested 4-step route.
  - The earlier suspected missing-neighbor issue is now classified as a verifier sampling flaw, not confirmed product behavior.
  - No product code change was made in this step.
- Manual test target for user:
  - only test one 4-step-ish free manual march on `http://47.116.32.216/wxgame/`;
  - pass if each next tile and its adjacent ring are already rendered before actor entry;
  - pass if visible tile identity does not change while actor passes;
  - pass if return-home starts from the current tile and ends on the capital.

## Step 17H Online Complete-Mode Final-Step Retest With Hit-Target Layers

- Updated repeatable command: `npm.cmd run playtest:online-manual-march-complete`.
- Harness refinement:
  - `getState()` now records `hitTargetLayers.renderer`, `hitTargetLayers.worldMapRuntime`, and `hitTargetLayers.actorLayer`;
  - position samples now include `layerActorCounts`;
  - normal click selection still uses the renderer hit target surface, while the extra layers are diagnostic-only.
- Calibration evidence:
  `tmp/verification/online-manual-march-complete-final-step-visible-retest/2026-06-16T02-26-33-167Z/`
  - `routeCount = 5`, selected target `tile_33_13`;
  - old verifier failed with `active actor disappeared during final route phase for 1 samples`;
  - screenshots still showed the actor visually present, so this was insufficiently instrumented evidence, not enough to prove a visual disappearance.
- Calibration evidence:
  `tmp/verification/online-manual-march-complete-hit-target-layers/2026-06-16T02-34-08-382Z/`
  - stopped before marching at `wait-open-picker`;
  - selected target `tile_24_7` was near the top HUD area;
  - this was a verifier precondition miss, not final-step behavior evidence.
- Passing evidence:
  `tmp/verification/online-manual-march-complete-hit-target-layers/2026-06-16T02-36-00-505Z/`
  - public H5, visible browser, no local server;
  - `routeCount = 9`, selected target `tile_24_6`;
  - sampled final route indices `[7, 7, 7, 8]`;
  - final position `tile_24_6` matched target;
  - `activeActorDisappearances = []`;
  - final phase renderer actor target count stayed at `2`;
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`, `verdict.pass = true`.
- Key screenshots:
  - `05-before-final-route-steps-full.png`
  - `complete-final-phase-step-07-1-full.png`
  - `complete-final-phase-step-07-2-full.png`
  - `complete-final-phase-step-07-3-full.png`
  - `complete-final-phase-step-08-4-full.png`
  - `06-final-route-complete-full.png`
- Result:
  - Complete-mode final-step behavior passed on deployed public H5 for the tested 9-step route.
  - Step 17H strengthens Step 17D with layer-specific hit-target diagnostics.
  - No product code change was made in this step.
- Manual test target for user:
  - only test one long no-return free manual march on `http://47.116.32.216/wxgame/`;
  - pass if the actor remains visible/clickable through the penultimate step;
  - pass if the actor enters the final target tile and completes on that tile.

## Step 18 Evidence-First Online Render And Return Retest

- Reason:
  - User explicitly rejected a text-only "passed" report without test steps, browser screenshots, or computer/browser verification.
  - This entry records the exact public-H5 proof used for the current handoff.
- Visible browser proof:
  - In-app browser was opened visibly to:
    `http://47.116.32.216/wxgame/?codexEvidence=public-h5-visible-20260616`
  - Screenshot:
    `tmp/verification/public-h5-browser-visible-20260616/public-h5-visible.png`
  - Browser info JSON:
    `tmp/verification/public-h5-browser-visible-20260616/browser-info.json`
  - This is public H5 evidence, not localhost.
- Automated online test command:
  ```powershell
  $env:PLAYTEST_OUTPUT_DIR='F:\AI Project\WXGamesLocal\tmp\verification\online-visible-step18-render-return-proof'
  $env:PLAYTEST_HEADLESS='0'
  $env:PLAYTEST_USERNAME='codexqa'
  $env:PLAYTEST_PASSWORD='123456'
  $env:PLAYTEST_MIN_TARGET_DISTANCE='4'
  $env:PLAYTEST_MAX_WAIT_MS='150000'
  npm.cmd run playtest:online-manual-march-templates
  ```
- Passing evidence directory:
  `tmp/verification/online-visible-step18-render-return-proof/2026-06-16T02-49-16-094Z/`
- Summary:
  - deployed URL:
    `http://47.116.32.216/wxgame/?codexManualMarchReturn=2026-06-16T02-49-16-094Z`
  - `routeCount = 4`
  - selected target `tile_28_7`, `routeStepCount = 4`
  - return clicked from `tile_27_7`
  - first return frame stayed at `tile_27_7`
  - final return position `tile_28_9` matched home/capital
  - `knownTileChangeCount = 0`
  - `missingBeforeEntryCoverage = []`
  - `badResponses = 0`
  - `requestFailures = 0`
  - `pageErrors = 0`
  - `verdict.pass = true`
- Key screenshots in the evidence directory:
  - `outbound-step-00-render-full.png`
  - `outbound-step-01-render-full.png`
  - `outbound-step-02-render-full.png`
  - `05-before-return-click-full.png`
  - `07-return-command-visible-full.png`
  - `11-final-return-home-full.png`
- Backend Step 5 tutorial replanning verification:
  - Command:
    `node --test backend/tests/WorldExplorerArchitecture.test.js backend/tests/GameRoutesTutorial.test.js`
  - Result: 33 tests passed.
  - This proves the backend contract for guided first-city replanning/materialization guards, not a visual browser flow.
- Result:
  - Public H5 was visibly opened and screenshotted.
  - The online march/render/return verifier passed on a real public-H5 browser run.
  - No product code change was made in this step; this is test evidence and handoff documentation.
- Manual test target for user:
  - Open only `http://47.116.32.216/wxgame/`.
  - Start one about-4-step free manual march.
  - Watch only this slice:
    - before each route entry, the next tile and its adjacent ring are already rendered;
    - already visible tile visuals do not change while the actor passes;
    - return-home starts from the actor's current tile and ends on the capital.
  - Ignore unrelated tutorial/spawn/camera issues during this specific pass unless they block the test.

## Step 19 Online Reset Spawn Visible Verification

- Reason:
  - User correctly rejected a code-only/test-name-only handoff and required concrete test steps, browser screenshots, and visible browser evidence.
  - This step verifies the reset/spawn slice on public H5, not localhost.
- Repeatable command added:
  `npm.cmd run playtest:online-reset-spawn`
- Harness behavior:
  - logs into the public API;
  - opens `http://47.116.32.216/wxgame/` in a visible browser;
  - uses real canvas hit-target clicks for settings, reset request, and confirm reset;
  - writes before/after full-page screenshots, JSON snapshots, action evidence, and `summary.json`;
  - verifies new origin/capital, 25 visible starting tiles, capital render/hit target, and old owned territory release.
- Harness correction:
  - Shared projected territories may appear with `owner: "player"` plus `ownerPlayerId` for another account.
  - The reset verifier now records `ownerPlayerId` and only counts territory as the current account's old ownership when it belongs to the tested player.
- Calibration evidence:
  `tmp/verification/online-reset-spawn-visible-fixed/2026-06-16T03-11-23-935Z/`
  - Account `test3` was already reset to tutorial step `0`.
  - The tutorial shield correctly blocked settings input, so this run did not reach the reset button.
  - This is an account-state/verifier precondition issue, not reset-spawn product evidence.
- Passing evidence:
  `tmp/verification/online-reset-spawn-visible-fixed-codexqa/2026-06-16T03-15-12-038Z/`
- Account:
  `codexqa / 123456`
- Summary:
  - before origin/capital: `tile_28_9`
  - before owned territories: `capital`, `site_30_11`
  - after origin/capital: `tile_-8_-25`
  - after visible tile count: `25`
  - after owned territories: `capital` only
  - render context contains capital: `true`
  - capital hit target visible: `true`
  - bad responses: `0`
  - request failures: `0`
  - page errors: `0`
  - `verdict.pass = true`
- Key screenshots:
  - `00-loaded-before-reset-full.png`
  - `01-open-settings-after-full.png`
  - `02-request-reset-after-full.png`
  - `03-confirm-reset-before-full.png`
  - `04-after-reset-complete-full.png`
- Local verification for the harness and related contracts:
  - `node --check scripts/playtest-online-reset-spawn.js`
  - `node --test backend/tests/SpawnLifecycleService.test.js backend/tests/GameStateRepository.test.js backend/tests/GameStateProjectionArchitecture.test.js backend/tests/SpawnAllocator.test.js frontend/auth.test.js frontend/js/platform/CanvasShellActionHandlers.test.js frontend/js/platform/CanvasTerritoryActionHandlers.test.js`
  - Result: 73 tests passed.
  - `git diff --check` produced only the existing CRLF working-copy warning for `package.json`.
- Manual test target for user:
  - Use an account that has completed tutorial and has occupied one outside city.
  - Reset the account from the public H5.
  - Check only this slice:
    - the camera/rendered map contains the new capital immediately after reset;
    - the new capital starts with a 5x5 visible area, 25 tiles;
    - the previous outside city is no longer owned by that account.

## Step 20 Post-Reset Tutorial Closure Smoke

- Reason:
  - Step 19 proved reset creates a new capital and 5x5 starting visibility.
  - This step verifies the next player-visible risk: the new spawn/camera state must not deadlock the guided tutorial after reset.
- Command:
  ```powershell
  $env:PLAYTEST_OUTPUT_DIR='F:\AI Project\WXGamesLocal\tmp\verification\online-post-reset-tutorial-smoke'
  $env:PLAYTEST_HEADLESS='0'
  $env:PLAYTEST_USERNAME='codexqa'
  $env:PLAYTEST_PASSWORD='123456'
  $env:PLAYTEST_RESET_ACCOUNT='0'
  $env:PLAYTEST_MAX_ACTIONS='72'
  npm.cmd run playtest:online-tutorial
  ```
- Boundary:
  - This used the public H5 and API.
  - `PLAYTEST_RESET_ACCOUNT=0` means the verifier did not reset again at the start; it continued from the Step 19 post-reset state.
- Deployed public version:
  - `deployedCommit = 141440eb2c37b2c9a206042e66bff36af3b53e63`
  - `deployedAt = 2026-06-16T01:05:31Z`
- Passing evidence:
  `tmp/verification/online-post-reset-tutorial-smoke/2026-06-16T03-20-54-068Z/`
- Summary:
  - initial tutorial step: `0`
  - initial capital coordinate: `-8,-25`
  - final step: `36`
  - stop reason: `tutorial-completed`
  - tutorial completed: `true`
  - action count: `51`
  - evidence count: `49`
  - verification report count: `53`
  - visual findings: `0`
  - verification failures: `0`
  - bad responses: `0`
  - request failures: `0`
  - page errors: `0`
- Key screenshots:
  - `00-start-full.png`
  - `highlight-selectWorldMarchTarget-35-before-full.png`
  - `highlight-conquer-43-after-step-26-full.png`
  - `highlight-claimConquest-44-after-step-27-full.png`
  - `close-advisor-step-35-after-step-36-full.png`
- Result:
  - The post-reset new-spawn account completed the full guided tutorial on public H5.
  - This proves the tested reset state did not leave the camera pointed at the wrong city, did not make the capital unclickable, and did not block guided first-city exploration/conquest.
- Manual test target:
  - After resetting a completed account, follow only the guided tutorial path from the new capital.
  - Pass if the tutorial reaches the first-city conquest and completion flow without the canvas pointing at an old city or losing the new capital.

## Step 21 Documentation State Audit And Public-H5 Reachability

- Reason:
  - The long-running handoff had stale mid-document headings such as `Current Manual Test Objective` that could mislead the next Codex into testing an old Step 17 target instead of the latest Step 20 target.
  - The world-map authority plan ended at Step 18 even though Step 19 and Step 20 already had public-H5 evidence in the development log.
- Documentation cleanup:
  - Renamed the old Step 17D heading to `Historical Manual Test Objective After Step 17D`.
  - Added an explicit status note that Step 17D's manual target was superseded by later Step 17F, Step 17G, Step 18, Step 19, and Step 20 evidence.
  - Added Step 19 and Step 20 formal evidence sections to `docs/world_map_authority_render_plan_2026-06-16.md`.
- Public-H5 reachability proof:
  - URL:
    `http://47.116.32.216/wxgame/?codexDocAudit=2026-06-16T03-29-58-463Z`
  - Evidence path:
    `tmp/verification/public-h5-current-doc-audit/2026-06-16T03-29-58-463Z/`
  - Screenshot:
    `public-h5-current-full.png`
  - Browser info:
    `browser-info.json`
- Result:
  - Public H5 loaded to the visible login canvas.
  - Canvas hit targets included username, password, remember-account, and login.
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`.
- Boundary:
  - This Step 21 screenshot proves online H5 reachability and current visible UI, not gameplay correctness.
  - Gameplay correctness evidence remains Step 19 reset-spawn and Step 20 post-reset tutorial completion.
- Current manual target:
  - Continue with the Step 20 target: after resetting a completed account, follow only the guided tutorial path from the new capital and verify it reaches first-city conquest/completion without losing the new capital.

## Step 22 User-Requested Real Browser Retest And Evidence Sync

- Reason:
  - User explicitly objected to undocumented testing and asked for real browser/public online evidence, screenshots, and clear test steps.
  - This step records the exact public-H5 browser evidence generated after that request.
- In-app browser public-H5 screenshot:
  - URL:
    `http://47.116.32.216/wxgame/?codexEvidence=visible-browser-qa-20260616`
  - Evidence path:
    `tmp/verification/iab-public-h5-user-demand/2026-06-16T03-37-58-389Z/`
  - Screenshot:
    `public-h5-visible.png`
  - Browser info:
    `browser-info.json`
  - Result:
    - page title: `文明火种`
    - visible loading/login canvas rendered from the public H5
    - console errors: `0`
    - console warnings: `3` H5 load trace warnings
- Reset-spawn command:
  ```powershell
  $env:PLAYTEST_OUTPUT_DIR='F:\AI Project\WXGamesLocal\tmp\verification\user-demand-reset-spawn-visible'
  $env:PLAYTEST_HEADLESS='0'
  $env:PLAYTEST_USERNAME='codexqa'
  $env:PLAYTEST_PASSWORD='123456'
  npm.cmd run playtest:online-reset-spawn
  ```
- Reset-spawn evidence:
  - Path:
    `tmp/verification/user-demand-reset-spawn-visible/2026-06-16T03-38-18-163Z/`
  - Before origin/capital:
    `tile_-8_-25`
  - Before owned territories:
    `capital`, `site_-6_-23`
  - After origin/capital:
    `tile_23_18`
  - After visible tile count:
    `25`
  - After owned territories:
    `capital` only
  - Render context contains capital:
    `true`
  - Capital hit target visible:
    `true`
  - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`
  - `verdict.pass = true`
- Post-reset tutorial short run:
  ```powershell
  $env:PLAYTEST_OUTPUT_DIR='F:\AI Project\WXGamesLocal\tmp\verification\user-demand-post-reset-tutorial'
  $env:PLAYTEST_HEADLESS='0'
  $env:PLAYTEST_USERNAME='codexqa'
  $env:PLAYTEST_PASSWORD='123456'
  $env:PLAYTEST_RESET_ACCOUNT='0'
  $env:PLAYTEST_MAX_ACTIONS='20'
  npm.cmd run playtest:online-tutorial
  ```
  - Evidence path:
    `tmp/verification/user-demand-post-reset-tutorial/2026-06-16T03-39-18-835Z/`
  - Boundary:
    - This intentionally capped actions at `20`.
    - It exited with `max-actions-reached`, not a product failure.
  - Result:
    - reached `finalStep = 16`
    - `visualFindings = 0`
    - `verificationFailures = 0`
    - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`
- Post-reset tutorial completion continuation:
  ```powershell
  $env:PLAYTEST_OUTPUT_DIR='F:\AI Project\WXGamesLocal\tmp\verification\user-demand-post-reset-tutorial-complete'
  $env:PLAYTEST_HEADLESS='0'
  $env:PLAYTEST_USERNAME='codexqa'
  $env:PLAYTEST_PASSWORD='123456'
  $env:PLAYTEST_RESET_ACCOUNT='0'
  $env:PLAYTEST_MAX_ACTIONS='72'
  npm.cmd run playtest:online-tutorial
  ```
  - Evidence path:
    `tmp/verification/user-demand-post-reset-tutorial-complete/2026-06-16T03-40-21-226Z/`
  - Boundary:
    - This continued the same QA account from tutorial step `16`.
    - The combined Step 22 evidence is a split-session proof from reset to completion, not a single uninterrupted command.
  - Result:
    - `stopReason = tutorial-completed`
    - `finalStep = 36`
    - `tutorialCompleted = true`
    - `actionCount = 31`
    - `evidenceCount = 28`
    - `verificationReportCount = 33`
    - `visualFindings = 0`
    - `verificationFailures = 0`
    - `badResponses = 0`, `requestFailures = 0`, `pageErrors = 0`
- Key screenshots reviewed:
  - reset complete 5x5/new-capital view:
    `tmp/verification/user-demand-reset-spawn-visible/2026-06-16T03-38-18-163Z/04-after-reset-complete-full.png`
  - tutorial world target near the new capital:
    `tmp/verification/user-demand-post-reset-tutorial-complete/2026-06-16T03-40-21-226Z/highlight-selectWorldMarchTarget-14-before-full.png`
  - tutorial completion state:
    `tmp/verification/user-demand-post-reset-tutorial-complete/2026-06-16T03-40-21-226Z/close-advisor-step-35-after-step-36-full.png`
- Result:
  - Public H5 was visibly opened in the in-app browser.
  - Reset-spawn preserved the expected 5x5 starting visibility and moved the capital to a new spawn.
  - The guided tutorial path on the same reset account reached completion after a short capped run plus a continuation run.
  - No local temporary server was used.
- Next narrow target:
  - Stop treating the `codexqa` reset/tutorial path as the main unknown unless related product code changes.
  - Do a read-only legacy-account spawn audit next, focused on whether old live accounts still have `(0,0)` origin/capital state and what migration/repair contract is needed.
  - Any user-facing verification for that target must use `http://47.116.32.216/wxgame/`, not a local server.

## Step 23 Read-Only Legacy Account Spawn Audit

- Reason:
  - Step 22 proved reset/new tutorial flow for `codexqa`, but old live accounts may still carry the historical `(0,0)` origin/capital state.
  - This step only audits production-like development server data. It does not rewrite any database rows.
- Data source:
  - Host: `47.116.32.216`
  - Database: `/opt/wxgame-workspace/backend/civilization.db`
  - Access mode: `better-sqlite3` readonly connection plus `sqlite3 -readonly` table checks
  - Relevant tables present:
    - `players`
    - `game_states`
    - `player_spawn_allocations`
    - `shared_world_territories`
- Fields audited:
  - `game_states.worldMap.origin`
  - capital coordinate in `game_states.territories`
  - `player_spawn_allocations.q/r/spawnKey/status`
- Result at `2026-06-16T03:49:39.259Z`:
  - total players: `37`
  - total game states: `37`
  - spawn allocation rows: `3`
  - accounts with `worldMap.origin = (0,0)`: `34`
  - accounts with capital at `(0,0)`: `34`
  - accounts with both origin and capital at `(0,0)`: `34`
  - accounts missing spawn allocation: `34`
  - spawn allocation mismatches with origin: `0`
- Recent account examples:
  - `test1`: origin/capital `(-6,28)`, spawn allocation `-6,28`
  - `codexqa`: origin/capital `(23,18)`, spawn allocation `23,18`
  - `test3`: origin/capital `(26,-13)`, spawn allocation `26,-13`
  - `test2`: origin/capital `(0,0)`, no spawn allocation
- Conclusion:
  - The new spawn flow is active for reset/newly repaired QA accounts.
  - Legacy account repair is still real remaining work: most existing saved accounts still use the old `(0,0)` origin/capital and have no spawn allocation row.
  - This is data-state debt, not a contradiction of the Step 22 reset-spawn proof.
- Next narrow target:
  - Design a migration/repair contract before writing data.
  - Required policy decisions for that contract:
    - whether to repair all legacy accounts automatically or only on next login/reset;
    - how to release or preserve their old `(0,0)` owned territories;
    - whether completed/tutorial-progress accounts should be moved or only accounts still in early tutorial;
    - what public-H5 account should be used for one manual browser proof after repair.
  - Do not execute database writes until the contract and one focused test target are documented.
