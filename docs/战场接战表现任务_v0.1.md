# 战场接战表现任务 v0.1

日期：2026-05-29

## 目标

把战斗小人从“左右站桩 + 每回合程序前冲”的传统回合制表现，改成“开局双方接战 + 接战后原地按序列帧攻击”的战场表现。

本任务只调整前端视觉层，不改变后端战斗结算、回合顺序、伤害、兵力变化和战报结构。

## 表现规则

- 开局第一手的 `move` 阶段作为双方接战阶段，己方与敌方小兵都从各自布阵点移动到中部接战区。
- 接战后小兵保持在中部战场位置，不在每回合攻击时退回左右两边。
- 每个可见小兵拥有独立延迟、目标 lane 和轻微站位差，避免整队像一个整体平移。
- `attack/skill` 阶段只播放攻击 PNG 序列帧，不再额外叠加程序 x 方向前冲。
- `hit` 使用 `idle` 帧 + 抖动 + 闪白，抖动只作为受击反馈。
- `die/defeated` 只播放死亡序列帧，不做额外死亡位移；如果该手结算后兵力归零，即使分组为空也保留一个视觉单位播完死亡。
- 每次攻击在 `impact` 阶段根据当前战报 turn 飘出伤害文字；伤害值优先读取 `turn.damage`，没有显式字段时按受击方 `soldiersBefore - soldiersAfter` 推导。该表现只读战报，不参与结算。
- 普通战报 turn 保持快节奏，基础播放时长约 0.9 秒，不因技能演出需求整体拖慢战斗。
- 技能 turn 在攻击动作前额外插入 `cutin` 暂停阶段，时长约 2.2 秒；`cutin` 阶段不结算伤害、不扣兵，双方小兵保持待机。
- 技能 cut-in 表现为名人立绘从左侧飞入、技能名从右侧飞入，在中间减速停顿后，立绘继续向右飞出、技能名继续向左飞出；演出结束后才进入快速移动、攻击、伤害飘字阶段。
- 普攻不显示立绘和技能名，只走基础的准备、移动、攻击、收束节奏。

## 实施进度

- [x] 移除攻击阶段程序前冲。
- [x] 增加接战位置计算与独立单位移动延迟。
- [x] 让首个移动阶段双方同时进入接战区，后续回合保持接战站位。
- [x] 补充测试覆盖：攻击不再改变单位位置、不同单位接战进度不同、死亡空组仍播放死亡帧。
- [x] 增加战斗伤害飘字，技能伤害展示 `damageLabel`，普通攻击展示负数兵力损失。
- [x] 拆分战斗播放节奏：普通手约 0.9 秒，技能手额外插入约 2.2 秒 cut-in 暂停演出。
- [x] 跑全量测试并提交推送。

## 测试记录

- 2026-05-29：首轮战场相关测试发现接战延迟断言误用了横坐标比较，已改为比较独立接战进度比例。
- 2026-05-29：`npm.cmd test` 全量通过，504/504。
- 2026-05-29：补充伤害飘字局部测试，覆盖 `damage` 字段和 `soldiersBefore/soldiersAfter` 推导两种来源。
- 2026-05-29：战斗伤害飘字版本完成后，`npm.cmd test` 全量通过，505/505。
- 2026-05-29：`0.1.149` 修复技能 cut-in 一闪而过的问题；新增播放节奏测试，锁定 4 秒/手和技能 impact 至少 2 秒。`npm.cmd test` 全量通过，513/513。
- 2026-05-29：`0.1.150` 修正战斗节奏：取消全局 4 秒/手，恢复普通手快节奏；技能手在攻击前独立暂停播放 cut-in，立绘从左飞入后向右飞出，技能名从右飞入后向左飞出。`npm.cmd test` 全量通过，514/514。
