# 战斗系统解耦任务计划 v0.1

日期：2026-05-29

## 目标

把战斗规则从领土扩张流程中拆出来，让后续武将属性、技能、速度和战斗表现可以独立迭代。

本计划只处理结构解耦，不重新设计战斗数值。

## 当前问题

- `backend/services/TerritoryService.js` 同时负责侦察、出征、占领、战斗模拟、战报生成、战后归附，职责过重。
- `speed`、攻击力、技能冷却、守军模板和战场资源选择都写在领土服务里，后续会阻碍属性和技能系统重做。
- 前端已经有战斗场景播放链路，短期不需要改播放结构。

## 目标结构

```text
backend/services/TerritoryService.js
  只负责领土、任务、出征、占领和战后结算。

backend/services/BattleService.js
  负责自动战斗模拟、双方战斗快照、战报结构和视觉资源字段。

backend/config/BattleConfig.js
  负责战斗常量、守军模板、战场资源、fallback 领队和 fallback 技能配置。

backend/services/FamousPersonService.js
  保留名人生成、候选、接纳、立绘和早期技能数据。
```

## 分阶段任务

| 阶段 | 状态 | 内容 | 验证 |
| --- | --- | --- | --- |
| 1 | 已完成 | 新增 `BattleService`，把当前战斗公式、守军模板、战报生成从 `TerritoryService` 抽出；公式保持不变。 | `node --test backend/tests/battle-service.test.js backend/tests/territory-service.test.js backend/tests/famous-person-service.test.js`; `node --test frontend/tests/ui-state-presenter.test.js frontend/tests/shared-canvas-renderer.test.js` |
| 2 | 已完成 | 为 `BattleService` 增加独立单元测试，锁定战报 schema、100 兵显示组、开局技能和 cooldown 字段。 | `backend/tests/battle-service.test.js` |
| 3 | 已完成 | 设计并落地 `BattleConfig`，把守军模板、战场资源、fallback 技能从代码迁到配置层。 | `backend/tests/battle-service.test.js` + `backend/tests/territory-service.test.js` |
| 4 | 已推进 | 重做武将属性、`speed`/先手、技能模板，不再直接沿用早期名人属性公式。已落地速度排序、条件释放、冷却、技能生成器接口、技能面板展示和旧技能迁移。 | `docs/战斗系统设计_v0.2.md` + 回归测试 |

## 本轮执行记录

- 2026-05-29：完成第一批结构解耦。
  - 新增 `backend/services/BattleService.js`。
  - `TerritoryService` 不再保存战斗公式，只在出征时获取名人快照、在结算时调用 `BattleService.simulateConquestBattle()`。
  - 新增 `backend/tests/battle-service.test.js`，覆盖战报系统名、开局技能、501 兵显示 6 组和历史 fallback。
  - 本轮没有重做武将属性、`speed` 或技能数值。
- 2026-05-29：完成第二批配置解耦。
  - 新增 `backend/config/BattleConfig.js`。
  - 守军模板、战场地图、战斗表现资源、fallback 领队和 fallback 技能迁出 `BattleService`。
  - `BattleService` 保留战斗流程和公式实现，后续重做 `speed`/技能时优先改配置和规则层。
  - 新增配置副本测试，避免调用方修改全局模板。
- 2026-05-29：开始战斗系统 v0.2 设计。
  - 新增 `docs/战斗系统设计_v0.2.md`。
  - 已确认武将六维属性：统帅、武力、智力、魅力、政治、速度。
  - 已确认初始属性 `1-100`，面板属性不设硬上限。
  - 已确认每 `10` 级固定获得 `10` 点自由属性点，品质只影响自动成长数值。
  - 已确认四档品质：一般、良才、英杰、传奇；自动成长总点数暂定为每 `10` 级 `4/6/8/10`。
  - 已确认经验来自战斗表现，胜败都有经验，主要按敌军损失计算，己方战损提供少量苦战经验。
  - 已确认速度暂时只决定每轮先后手，不影响行动频率。
  - 已确认无上限等级的平滑经验曲线、按武将定位分配自动成长点数、兵刃/谋略伤害公式方向和结构化详细战报表现。
  - 已确认士气第一版只保留字段不影响公式；技能 v1 采用 `1` 主动战法 + `1` 被动特质；战斗流程和战后结算进入可实现状态。
- 2026-05-29：`0.1.151` 落地第一批正式战斗内核。
  - `BattleConfig` 战斗系统号升级为 `attribute-auto-battle-v2`。
  - 战报增加 `ruleVersion`、`actionOrder`、`actionDecision`、自身行动计数和冷却推进字段。
  - 技能释放按 `castConditions` 决定，不再开局无条件必放；冷却只在自身行动推进。
  - 谋略伤害改为攻击方智力对目标智力，兵刃伤害仍为武力对统帅。
  - `preBattle` 被动特质中的属性修正会在准备阶段生效并写入战报。
- 2026-05-29：`0.1.152` 落地技能生成器接口收口。
  - `abilityKit` 写入 `source`、`seed`、`generatorInput`、`availableEffectPool`、`budgetChecks` 和 `budgetStatus`。
  - 技能模板按定位效果池过滤，内政型只生成内政技能，斥候只使用轻战斗和侦查/行军池。
  - 同一 seed 可以稳定复现同一组技能模板，便于后续排查和回放。
  - 技能能力补充 `description`，为下一步名人技能面板详情展示做准备。
- 2026-05-29：`0.1.153` 落地名人技能面板展示。
  - `UIStatePresenter` 新增 `skillDetails`，按槽位输出主动战法、战斗被动、内政主技、内政副技和斥候特质。
  - Canvas 名人卡片从只显示一条技能摘要改为显示两条紧凑技能详情。
  - 内政/斥候存档展示型能力会明确标注未接入状态；当前最终展示口径见 `0.1.159` 的 `暂未接入实际收益`。
  - 本批只改展示，不改战斗数值和战斗结算。
- 2026-05-30：`0.1.156` 落地名人卡片展示 v2。
  - 名人卡片改为名称、称号、两条技能徽章和右侧六维蛛网图，详情通过悬停或点击技能徽章显示。
  - 六维口径固定为统帅、武力、智力、政治、魅力、速度；速度是正式六维之一。
  - `工巧/craft` 不属于武将或名人属性，只保留人口/工匠等历史系统语境，不再参与名人生成和展示。
  - 卡片不常驻展示归附、招募等过程性文本，避免挤压技能和属性展示。
- 2026-05-30：`0.1.157` 修复名人技能详情关闭交互。
  - 点击同一个技能徽章会切换收起已固定的技能详情。
  - 固定详情后，点击名人面板内部空白区域只收起详情，不关闭名人面板。
  - 关闭名人面板、切换 Canvas 页签或回到资源页时同步清理技能详情状态，避免旧 tooltip 残留。
- 2026-05-30：`0.1.158` 修正名人技能说明和列表翻页。
  - 主动战法详情初步去掉 `冷却就绪 / 目标存活 / 自身行动` 这类工程描述，最终玩家口径由 `0.1.159` 统一收口。
  - 新生成的主动技能 `description` 初步改为玩家说明，最终不再把释放间隔写进效果主体。
  - 名人面板已加入列表增加上一页/下一页，超过当前可见数量后可以继续查看更早加入的名人。
- 2026-05-30：`0.1.159` 修复名人翻页点击链路并重写技能说明口径。
  - `changeFamousPersonsPage` 接入共享 action dispatcher、Canvas app 和 H5 shell 状态同步，上一页/下一页点击后会实际切换已加入名人列表页。
  - 技能详情主体改为完整效果句，例如 `施加倒戈（倒戈：将敌方本次损失兵力的一部分转换为自己的兵力）`，不再展示 `直接伤害 / 属性修正 / 吸血` 等内部效果标签。
  - 新生成和旧存档 normalize 都会重写旧技能 `description`，前端同时兜底过滤旧文案中的工程口径和旧未接入提示。
- 2026-05-30：`0.1.160` 修复技能详情规则行内部条件名泄漏。
  - `UIStatePresenter.formatFamousPersonCastCondition()` 对已知但不应展示的基础条件返回空字符串，不再因为空字符串被 `||` 回退成 `cooldownReady / targetAlive`。
  - 未知 cast condition 不再直接把原始 key 展示给玩家，避免后续新增内部条件时再次漏出英文。
- 2026-05-30：`0.1.161` 技能详情字段改成玩家口径。
  - Tooltip 将 `说明：` 改为 `效果：`，移除 `规则：` 这一层标签。
  - 主动战法展示 `冷却：N 回合 · 发动率：X%`；当前确定性技能无概率字段时按 `发动率：100%` 展示。
  - 有真实概率字段的后续技能可直接读取 `castRate / triggerRate / probability / chance / rate`。
- 2026-05-30：`0.1.162` 战斗状态系统第一批落地。
  - `BattleService` 接管守御、破甲、灼烧和中毒的真实状态结算，战斗规则版本升级为 `battle-rules-v3`。
  - 每个战报 turn 输出 `statusEvents / floatingTexts / statusesBefore / statusesAfter`，前端只按战报播放状态飘字，不参与结算。
  - 本批不扩大普通技能随机池，先让已有 `shield` 真实生效，`armorBreak / burn / poison` 作为可结算状态供测试和后续模板接入。
- 2026-05-30：`0.1.163` 战斗状态可视化第二批落地。
  - `UIStatePresenter` 按战斗播放阶段输出双方状态徽标和技能冷却状态。
  - `CanvasGameRenderer` 在双方阵列上方常驻显示战法名、冷却状态和状态徽标，让自动战斗过程更容易验收。
  - 本批只做表现层，不改状态结算、伤害公式和技能释放规则。
- 2026-05-30：`0.1.164` 武将经验等级第一批落地。
  - `FamousPersonService` 新增等级经验曲线、成长字段归一化和战斗经验写回接口。
  - `TerritoryService` 在征服战结算后把战报经验写回出征主将，并在战报保存 `leaderGrowth` 摘要。
  - 名人面板展示等级、当前经验、下级经验和可分配属性点；候选名人继续不展示成长字段。
  - 本批只记录和展示自由属性点，手动分配 UI 与品质自动成长属性留到后续批次。
- 2026-05-29：`0.1.154` 落地旧技能效果迁移。
  - `SkillGeneratorService.normalizeEffect()` 统一迁移旧效果。
  - `combo -> secondHit`、`ambush -> firstStrike`、`morale -> attributeBonus(command)`。
  - `counter` 第一版移除，不进入战斗效果，避免没有反击系统时误导玩家。
  - `FamousPersonService.normalizeSkill()` 改为调用同一迁移口径，旧存档 normalize 后不再带旧概率效果进入战斗。
- 2026-05-30：`0.1.155` 落地旧名人完整机制升级。
  - `SkillGeneratorService.normalizeAbilityKit()` 不再只清洗已有技能，而是按定位补齐当前机制要求的完整槽位。
  - 旧军事型名人保留/迁移原主动战法，同时补齐 `passiveTrait`。
  - 旧内政/魅力型名人统一转为两个内政能力，`battlePolicy` 为 `basicAttackOnly`，不再从旧战斗技能回退。
  - 旧斥候型名人保留/迁移轻战斗主动战法，同时补齐 `scoutTrait`。
  - 已加入名人和候选名人都通过 `normalizeState` 自动升级，并写入最新 `generatorInput`、预算检查和描述字段。
