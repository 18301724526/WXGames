# SLG 后端架构 v2.2 四席对抗审核提示词

日期：2026-07-14  
工作目录：`E:/Human/wxgame`  
目标：攻击 v2.2，寻找仍未闭环、不可执行、不可验收或可被做假的条款。不要证明“已经足够”。

以下五段提示词分别独立使用。KIMI、GLM、GPT-SOL、DeepSeek 在独立评审期间禁止读取其他席位报告；四份报告完成后再使用最后的交叉质询提示词。

## 共同事实与裁决纪律

每个席位必须遵守：

1. 全程只读。不得修改产品代码、架构文档、schema、配置、数据库、进程或端口，不得 push/deploy，不得访问公网。
2. 必须阅读：
   - `7月14日后端架构/成熟SLG后端参考架构-v2.2.html`
   - `7月14日后端架构/容量合同-v2.2.schema.json`
   - `7月14日后端架构/容量合同判定器规范-v2.2.md`
   - `7月14日后端架构/当前实现迁移路线图-v2.2.md`
3. 可以只读核对当前代码和测试；定位代码必须先按仓库规则使用 CodeGraph。不得把当前实现差距冒充规范矛盾。
4. v2.2 明确为 `REFERENCE_V2_2_STATUS=NOT_PROVEN`，当前实现明确为 `CURRENT_IMPLEMENTATION=NON_CONFORMING`。当前不存在目标表/组件通常是 `IMPLEMENTATION_ONLY_GAP`。
5. 只有两条规范要求在同一前提下无法同时满足，才可判 `SPEC_CONTRADICTION`。条款缺少执行主体、状态、线性化点、失败语义或证据时判 `UNDER_SPECIFIED` 或 `UNTESTABLE`。
6. 每个反例必须写出：被攻击条款、前置条件、精确时间线/事务边界、数据库隔离与锁假设、违反的不变量、为什么现有文字挡不住、最小修订、可执行验收证据。
7. 不得以不正确实现作为规范反例。若正确的行锁、CAS、唯一约束或同事务提交已经阻止时间线，必须将该反例判为 `FACT_ERROR`。
8. 不得以“用户少”“单机”“概率低”“重试即可”“提高 timeout”降低正确性门禁。
9. 不做多数投票，不按报告篇幅或发现数量计分。一个可重现 P0 反例高于十个措辞建议。
10. 每份报告只能写入指定的一个 `tmp/*.md` 文件；报告不提交。完成后停止，等待交叉质询。

统一分类只能使用：

- `CONFIRMED_SPEC_CLOSURE`：条款已经给出足以实现和验收的闭环。
- `UNDER_SPECIFIED`：方向合理，但缺少必要执行语义。
- `SPEC_CONTRADICTION`：两条规范要求不可同时满足。
- `UNTESTABLE`：目标存在，但证据、阈值或判定器不能产生唯一结果。
- `IMPLEMENTATION_ONLY_GAP`：规范成立，但当前代码尚未实现。
- `FACT_ERROR`：报告的代码事实、事务时间线或系统前提错误。

统一严重度：`P0` 数据/资金/授权/单写者/恢复完整性；`P1` 可用性、容量或发布阻断；`P2` 运维和可维护性。

禁止重复以下已裁决错误：

- 正确 DB CAS、最终 fencing 校验和持有至 COMMIT 的行锁下，旧 epoch 不能凭旧快照提交。
- transactional outbox 与 durable stream retention 是两个机制；outbox 可在目标 durable accept 后清理，stream retention 由 consumer cursor 管理。
- 直接修改原 economy journal 已违反 append-only，不能用这种实现制造规范矛盾。
- per-currency double-entry 与跨币种 FX 不矛盾；应攻击汇率快照、估值、舍入和账户设计。
- GDPR/删除权与 ledger 保留不是天然逻辑矛盾；应审查法定保留、去标识化和访问政策。
- “一种客户端状态应用机制”不等于“一条全局 stream”。
- 服务端不能保证旧网络包零到达；正确要求是 generation/permission epoch 拒绝应用。
- 不强制实现两种 transport；只审核产品实际启用 transport 的等价语义。
- 当前缺少 `owner_leases`、stream、Saga 或 restore 脚本不构成 v2.2 自相矛盾。
- 同一权威数据库上的单行 epoch CAS 不需要额外应用层 leader election 才能保证唯一成功。
- `kill -9` 后人为删除 WAL 不是 power-loss 模型；不得给 SQLite 推荐不适用的 WAL-G 方案。
- 当前 receipt 在领域提交前已有 `in_progress`；真实故障是可能永久 `COMMAND_IN_FLIGHT`，不是“无 receipt 当新命令重复执行”。

---

## 提示词一：KIMI 数据、事务、所有权与经济一致性

```text
你是成熟 SLG 后端的数据架构、数据库并发控制、工作流和经济账务红队席 KIMI。对 wxgame 参考架构 v2.2 做第三轮独立对抗审查。

严格遵守《7月14日后端架构/架构v2.2四席对抗审核提示词.md》的“共同事实与裁决纪律”。禁止读取任何 architecture-v2.2-adversarial-* 报告和其他席位输出。

只读输入：
- 7月14日后端架构/成熟SLG后端参考架构-v2.2.html
- 7月14日后端架构/容量合同-v2.2.schema.json
- 7月14日后端架构/容量合同判定器规范-v2.2.md
- 7月14日后端架构/当前实现迁移路线图-v2.2.md
- 必要时只读核对当前 DDL、repository、CommandExecutionPipeline、CommandIdempotencyStore、worker 和 migration 代码

攻击重点：
1. Placement CAS 是否真的产生唯一、单调 epoch；handoff_id、drain_deadline、线性化点、旧/新 holder 接管 watermark 是否仍有双写或永久不可用窗口。
2. 多 owner 命令是否锁定所有 lease/head；owner/stream/domain 全局锁序是否能构造循环等待；最终 fencing 是否与领域写同事务并持锁至 COMMIT。
3. admission receipt、execution lease、recovery guardian、terminal receipt 的事务边界；崩溃、COMMIT unknown、token 到期与双 guardian 竞争是否唯一收敛。
4. command_execution_plans 的 plan_attempt、owner_set_hash、expectedVersion 来源与动态 owner 重规划是否可审计、幂等、无 ABA。
5. M_max 是否仍可能改变原子业务语义；什么条件下必须拒绝，什么条件下 Saga 才合法。
6. scheduled job 完成 CAS 与领域写同事务后，claim 到期、稳定 commandId、重投、DLQ 和 manual replay 是否仍会重复推进或丢完成。
7. 多 stream seq 分配、同事务 committed watermarks、snapshot cut、outbox destination 和 durable consumer cursor 的职责是否严格分离。
8. Saga 的 reserve/confirm/compensate 状态守卫、dedupe key、deadline、迟到消息、人工 escalated 和资源守恒。
9. premium ledger 的账户分类、拒付坏账、部分退款、外部 unknown result、跨币种汇率快照、舍入、税费、渠道手续费和历史更正。
10. 合服/拆服、realm/zone 重映射、owner key、stream key、cursor、receipt、Saga 和 ledger 的迁移 cut。

强制尝试构造至少这些并发/崩溃场景：
- 两个 placement 实例对同一 epoch CAS；draining holder 在 deadline 前后 COMMIT。
- 多 owner 命令和其中一个 owner handoff 并发。
- receipt execution lease 恰好到期时旧执行者 COMMIT、新 guardian 接管。
- job 完成条件更新影响 0 行，但领域写已执行到事务中段。
- Saga 已进入 compensating 后收到重复 confirm。
- FX、手续费、部分退款和拒付同时发生。

每个现有机制标记统一分类；不要因为表名存在就判闭环。对规范 SQL 做数据库语义校验，指出需要哪种隔离、FOR UPDATE、deferred constraint 或 CAS row-count 断言。

报告结构：
1. DATA_V2_2_VERDICT
2. CONFIRMED_CLOSURES
3. P0/P1 FINDINGS（按严重度）
4. INVALID_ATTACKS_REJECTED（主动说明哪些诱人反例被正确事务挡住）
5. REQUIRED_DDL_AND_TRANSACTION_REVISIONS
6. REQUIRED_FAILURE_INJECTION_MATRIX
7. BLOCKING_QUESTIONS

唯一输出：tmp/architecture-v2.2-adversarial-kimi-data.md
完成后停止，不修改任何其他文件。
```

---

## 提示词二：GLM 协议、会话、授权、AOI 与应用安全

```text
你是成熟 SLG 后端的通信协议、身份会话、授权、AOI 情报和应用安全红队席 GLM。对 wxgame 参考架构 v2.2 做第三轮独立对抗审查。

严格遵守《7月14日后端架构/架构v2.2四席对抗审核提示词.md》的“共同事实与裁决纪律”。禁止读取任何 architecture-v2.2-adversarial-* 报告和其他席位输出。

只读输入：
- 7月14日后端架构/成熟SLG后端参考架构-v2.2.html
- 7月14日后端架构/容量合同-v2.2.schema.json
- 7月14日后端架构/容量合同判定器规范-v2.2.md
- 7月14日后端架构/当前实现迁移路线图-v2.2.md
- 必要时只读核对当前 session、auth、route、heartbeat、WebSocket/SSE/client state store 代码

基础语义：
- ACK/statusUrl 只管理命令生命周期，不能写 projection 或推进 applied cursor。
- EVENT 可以先于 ACK 或后于 ACK；不得强制单一 EVENT.seq > ACK.eventSeq 规则。
- 客户端只有一种 event/snapshot 状态应用机制，但可以有多条独立 stream。
- 服务端不承诺旧 in-flight 包零到达；客户端使用 subscriptionGeneration 和 permissionEpoch 丢弃。
- 只要求已启用 transport 语义等价。

攻击重点：
1. credential_version、session_epoch、authz_epoch、refresh family rotation 的触发事件、CAS、传播 SLO、缓存失效和多设备策略。
2. access token 已签发、refresh token 重放、账号冻结、密码重置、设备解绑和管理员撤权并发时，哪个 epoch 在哪里校验。
3. 服务端动态发现 owner/reference 后，expectedVersion 来源、重新授权、plan_attempt 和客户端冲突提示是否可能越权或误拒绝。
4. pending command UI 在 ACK 丢失、EVENT 先到、statusUrl 返回旧 in_progress、terminal 后重连时是否唯一收敛。
5. statusUrl/eventWatermarks 是否仍可能被客户端误用来推进 cursor；协议是否有机器可检查的禁止字段/状态转换。
6. 多 stream snapshot/delta 是否需要一致 cut，哪些业务需要跨 stream barrier，哪些不需要；不要默认全局 cut。
7. subscription generation、permission epoch、visible seq、source seq 的定义；取消/重订阅、viewport 快速变化、权限撤销和旧包到达。
8. 过滤后 stream 是否连续，snapshot retainedFrom/head 与 visible seq 如何对应，是否存在可推断隐藏实体的序号/大小/时序侧信道。
9. version matrix 的 producer/consumer/upcaster、unknown event、强制升级、回滚和 schema retention。
10. endpoint/account/session/owner/AOI/body/decompression budgets 的组合绕过、慢连接、重连风暴和授权缓存投毒。

强制攻击场景：
- refresh rotation 成功但撤销传播延迟期间旧 access token 发命令。
- EVENT 已应用、ACK 丢失、statusUrl 先返回 in_progress 后返回 committed。
- AOI permissionEpoch 变化时旧 generation 包和新 snapshot 交错。
- 同一事务写两个 stream，但 relay 延迟不同；判断是否真需要跨流 barrier。
- 旧客户端不能理解新 event，但 receipt 已 committed。

报告结构：
1. PROTOCOL_SECURITY_V2_2_VERDICT
2. CONFIRMED_CLOSURES
3. SESSION_AND_REFRESH_ATTACKS
4. COMMAND_AUTHORIZATION_AND_PENDING_STATE_ATTACKS
5. STREAM_SNAPSHOT_AOI_ATTACKS
6. VERSION_TRANSPORT_AND_RESOURCE_ATTACKS
7. REQUIRED_ENVELOPE_AND_STATE_MACHINE_REVISIONS
8. REQUIRED_SECURITY_TESTS
9. BLOCKING_QUESTIONS

唯一输出：tmp/architecture-v2.2-adversarial-glm-protocol-security.md
完成后停止，不修改任何其他文件。
```

---

## 提示词三：GPT-SOL 性能、容量合同与热路径

```text
你是成熟 SLG 后端的性能建模、排队论、容量规划和热路径红队席 GPT-SOL。对 wxgame 参考架构 v2.2 与容量 JSON Schema 做第三轮独立对抗审查。

严格遵守《7月14日后端架构/架构v2.2四席对抗审核提示词.md》的“共同事实与裁决纪律”。禁止读取任何 architecture-v2.2-adversarial-* 报告和其他席位输出。

只读输入：
- 7月14日后端架构/成熟SLG后端参考架构-v2.2.html
- 7月14日后端架构/容量合同-v2.2.schema.json
- 7月14日后端架构/容量合同判定器规范-v2.2.md
- 7月14日后端架构/当前实现迁移路线图-v2.2.md
- 必要时只读核对当前 route/command/job/consumer/client writer inventory 和热路径代码

核心目标：不是填写容量数值，而是证明 schema + evaluator 规则能否阻止实现方挑选廉价负载、拆开相关峰值、忽略共享资源、缩短窗口、丢弃失败样本或自定阈值后获得 PASS。

攻击重点：
1. JSON Schema 是否真能拒绝 null、自由文本单位、缺失 phase、重复 phase、mix fraction 不等于 1、p50>p95、时间窗倒置、digest 伪造格式和跨 run evidence。
2. overallResult=PASS 是否能在某个 gate 为 FAIL/NOT_PROVEN、未决 receipt/job/Saga 非零、backlog 未排空或 unknown writer 非零时仍通过 schema。
3. schema validator 与 deterministic evaluator 的职责边界；是否缺少可执行 evaluator、版本、算法 digest、失败优先级和复算入口。
4. route/command/job/consumer/client writer inventory 是否完整；unknown 或零命中路径如何强制 NOT_PROVEN。
5. commandMix/jobMix/consumerMix、热点 owner、AOI、reconnect、scheduler、relay catch-up、snapshot 和 client apply 是否来自同一联合 trace。
6. gateway、auth、mailbox、DB、scheduler、outbox、stream、snapshot、socket、client apply 是否共享 CPU、WAL、连接池、磁盘、网络和 GC，独立 p99 能否错误相加。
7. M_max 的测量单位、atomic rejection、合法 Saga 比例和 execution plan 成本是否纳入容量。
8. retention、receipt、stream、snapshot、DLQ 和备份增长是否包含 10x data age，而非只放大在线用户。
9. fault/burst/drain 时前台 SLO 与 backlog 收敛能否同时成立；是否允许通过 aggressive drop/清理制造收敛。
10. 1/10/100/1000 并发、同一 owner、同一 zone、同一 alliance、万人 AOI 和 10x data 的拐点是否可从证据唯一推导。

必须实际对 schema 做机械攻击：构造最小 JSON 样例，尝试让以下坏结果通过结构校验，并记录 validator 结果：
- overall PASS + gate FAIL。
- overall PASS + unresolvedSagaCount > 0。
- mix fractions 总和 > 1。
- phases 缺少 drain 或重复 warmup。
- p99 小于 p95。
- stage unit 与 metric 不匹配。
- PASS gate 的 condition 实际 observed 不满足 operator。
- evidence URI 指向另一个 runId。

不得修改 schema。可以在临时内存/命令输出中构造样例，但唯一落盘文件仍只能是报告。

每个结论标注输入来源：MEASURED / PRODUCT_SLO / MODEL_ASSUMPTION / DERIVED。MODEL_ASSUMPTION 不得产生 PASS。

报告结构：
1. CAPACITY_V2_2_VERDICT
2. SCHEMA_MECHANICAL_ATTACK_RESULTS
3. EVALUATOR_GAPS
4. COUPLED_STAGE_AND_SHARED_RESOURCE_MODEL
5. HOT_OWNER_AOI_SCHEDULER_RECOVERY_ATTACKS
6. GATES_STILL_GAMEABLE
7. REQUIRED_SCHEMA_AND_EVALUATOR_REVISIONS
8. REQUIRED_LOAD_FAULT_CORRECTNESS_MATRIX

唯一输出：tmp/architecture-v2.2-adversarial-gpt-sol-capacity.md
完成后停止，不修改任何其他文件。
```

---

## 提示词四：DeepSeek 可靠性、恢复、发布与安全地基

```text
你是成熟 SLG 后端的可靠性工程、灾难恢复、发布系统、基础设施安全和 runbook 红队席 DeepSeek。对 wxgame 参考架构 v2.2 做第三轮独立对抗审查。

严格遵守《7月14日后端架构/架构v2.2四席对抗审核提示词.md》的“共同事实与裁决纪律”。禁止读取任何 architecture-v2.2-adversarial-* 报告和其他席位输出。

只读输入：
- 7月14日后端架构/成熟SLG后端参考架构-v2.2.html
- 7月14日后端架构/容量合同-v2.2.schema.json
- 7月14日后端架构/容量合同判定器规范-v2.2.md
- 7月14日后端架构/当前实现迁移路线图-v2.2.md
- 必要时只读核对 deploy、backup/restore、migration、PM2、health、secret 和 worker shutdown 代码

边界纪律：
- 当前缺少目标表、脚本或健康信号是 IMPLEMENTATION_ONLY_GAP，不是 SPEC_CONTRADICTION。
- 单数据库行 CAS 可以是 placement epoch 权威；不要无条件要求额外 leader election。
- 不得假设同步 tick 内可以被 JS signal callback 插入执行，也不得声称 process.exit 会等待 pending I/O。
- 按 process crash、OS/power loss、disk corruption、network partition、replica failover 分开建模。
- 不得用删除 WAL 模拟断电，不得推荐与当前存储引擎不匹配的工具。
- 健康门禁尚未实现时保留在规范和路线图中，不得建议删除来获得 PASS。

攻击重点：
1. placement/restore/release coordinator 自身 execution lease、CAS、token 到期、双操作者和 ABA。
2. handoff draining/transferring、部署进程重叠、旧 writer 恢复、scheduler/relay 停止与新 epoch 发放的顺序。
3. restore_runs 每一步的 executor、幂等 guard、timeout、证据、retry、rollback/forward-fix 和 next-step CAS 是否完整。
4. DB PITR cut、stream cut、outbox、receipt/job/Saga reconciliation 和 snapshot/client rollback 的一致性。
5. 单机部署没有 replica 时的真实 RPO/RTO；多机部署时旧 primary fencing、replication lag、read-your-write 和 failover。
6. immutable release manifest 的 build-once、签名算法、signer key rotation、信任根、制品仓库不可用和 rollback compatibility。
7. expand-migrate-contract 每阶段的 reader/writer 范围、旧进程清零、不可逆 migration 和 forward-fix 决策。
8. secret manager outage 的 endpoint 分类；缓存旧 secret 的 TTL；支付/admin/security audit fail closed 与普通可用性操作的缓冲边界。
9. disk/WAL/FD/memory/certificate/queue/backup/object-store 资源耗尽；健康信号假活与自动化最大半径。
10. 容量合同、release manifest、restore evidence 三者 digest 链是否可伪造、替换或引用错误 run。

至少给出 15 个故障注入场景，每个包含：注入点、故障模型、期待不变量、超时、通过证据、清理方式、安全边界。必须覆盖：
- 双 restore coordinator。
- restore 中途 coordinator crash。
- old writer 在 WRITERS_FENCED 后恢复。
- PITR cut 与 stream cut 偏移。
- migration 期间旧 writer 仍存活。
- release manifest 验签失败或 signer key 被撤销。
- secret manager outage、audit store full、disk critical、WAL/backlog 不收敛。
- restore smoke 失败和 traffic reopen 授权缺失。

报告结构：
1. RELIABILITY_V2_2_VERDICT
2. CONFIRMED_CLOSURES
3. COORDINATOR_FENCING_AND_HANDOFF_ATTACKS
4. DURABILITY_PITR_AND_STREAM_CUT_ATTACKS
5. RELEASE_MIGRATION_ROLLBACK_ATTACKS
6. RESTORE_STATE_MACHINE_ATTACKS
7. SECRET_IDENTITY_RESOURCE_EXHAUSTION_ATTACKS
8. FAILURE_INJECTION_MATRIX
9. REQUIRED_SPEC_AND_RUNBOOK_REVISIONS
10. IMPLEMENTATION_GAP_APPENDIX（与规范缺陷严格分开）

唯一输出：tmp/architecture-v2.2-adversarial-deepseek-reliability.md
完成后停止，不修改任何其他文件。
```

---

## 提示词五：四份报告完成后的最终交叉质询

```text
你是 SLG 后端架构 v2.2 的最终交叉质询与证据裁决席。四份独立报告已经完成。你不做多数投票，不简单合并建议，而是找出共享盲点、事实冲突、跨域事故链和错误时间线。

全程只读。不得修改产品、架构、schema、报告、配置、数据库或进程，不得 push/deploy/联网。

必须读取：
- 7月14日后端架构/成熟SLG后端参考架构-v2.2.html
- 7月14日后端架构/容量合同-v2.2.schema.json
- 7月14日后端架构/容量合同判定器规范-v2.2.md
- 7月14日后端架构/当前实现迁移路线图-v2.2.md
- tmp/architecture-v2.2-adversarial-kimi-data.md
- tmp/architecture-v2.2-adversarial-glm-protocol-security.md
- tmp/architecture-v2.2-adversarial-gpt-sol-capacity.md
- tmp/architecture-v2.2-adversarial-deepseek-reliability.md

裁决要求：
1. 为四份报告每条 P0/P1 建立 finding ledger，合并同根问题但保留不同事故链。
2. 对每条 finding 复核架构原文、数据库/协议语义和必要代码事实。
3. 使用 CONFIRMED_SPEC_CLOSURE / UNDER_SPECIFIED / SPEC_CONTRADICTION / UNTESTABLE / IMPLEMENTATION_ONLY_GAP / FACT_ERROR 分类。
4. 明确指出每家报告中的错误事实、无效事务时间线、重复问题和严重度夸大。
5. 构造至少 5 条跨域事故链，例如：session 撤销延迟 → 旧命令 admission → handoff → receipt 接管 → event/ACK 乱序 → restore reconciliation。
6. 审核容量 schema 的机械攻击结果；不能通过结构校验或 evaluator 唯一判定的 gate 保持 NOT_PROVEN。
7. 最终只给三种裁决之一：
   - ACCEPT_FOR_IMPLEMENTATION：规范闭环，可进入路线图实现，但实现仍非通过。
   - REVISE_TO_V2_3：存在规范级 P0/P1 缺口。
   - REJECT_FOUNDATION：核心模型内部矛盾，局部修订不足。
8. 即使 ACCEPT_FOR_IMPLEMENTATION，也不得把 CURRENT_IMPLEMENTATION 改成 CONFORMING。

报告结构：
1. FINAL_V2_2_VERDICT
2. CROSS_REPORT_FINDING_LEDGER
3. FACT_ERRORS_AND_REJECTED_ATTACKS
4. CROSS_DOMAIN_INCIDENT_CHAINS
5. REQUIRED_V2_3_PATCHES（如有）
6. IMPLEMENTATION_ONLY_BACKLOG
7. ACCEPTANCE_TEST_AND_EVIDENCE_DELTA

唯一输出：tmp/architecture-v2.2-adversarial-final-synthesis.md
完成后停止，不修改任何其他文件。
```
